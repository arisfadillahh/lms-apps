# Error Fixing Log - August 2026

## INTERNAL-20260812-01 Ekskul lesson editor loaded the wrong record

- Symptom: editing a lesson such as Scratch 13 could show the title and fields from another lesson or plan.
- Root cause: modal state depended on stale component props and reused component identity while the selected plan changed.
- Changed: `EditEkskulLessonButton.tsx`, `EkskulSplitViewClient.tsx`, `/api/admin/ekskul/lessons/[id]`, `EditEkskulPlanButton.tsx`, and `/api/admin/ekskul/plans/[id]`.
- Fix: reset form state on open, fetch the selected lesson by its immutable ID, validate `planId` server-side, and key editor components by lesson/plan ID.
- Preserve: edits and deletes must always be scoped to both the immutable record ID and its current plan. Do not restore prop-only modal initialization.
- Verification: TypeScript and production build passed; production endpoint and PM2 were healthy after deployment.

## INTERNAL-20260812-02 Admin users page schema crash

- Symptom: `/admin/users` could not load.
- Root cause: extending a refined Zod schema with `.extend()` caused a runtime failure.
- Changed: `src/app/(admin)/admin/users/CreateUserForm.tsx`.
- Fix: use `.safeExtend()` for the active-state field.
- Preserve: coder-specific cross-field validation in the base schema must remain active.
- Verification: TypeScript and production build passed.

## INTERNAL-20260812-03 Story overlay scroll lock on iOS

- Symptom: trial/weekly story content was cropped on iPhone and swiping scrolled the report behind the overlay.
- Root cause: only the overlay was constrained; the document body remained scrollable and mobile viewport units were unstable.
- Changed: `TrialStoryReport.tsx`, `ReportStoryExperience.tsx`, and `ReportStoryExperience.layoutFix.ts`.
- Fix: lock `html` and `body`, preserve/restore scroll position, use stable mobile viewport sizing, and keep overflow inside the active slide.
- Preserve: cleanup must restore all original document styles when the story closes or unmounts.
- Verification: responsive source checks, TypeScript, and production build passed.

## INTERNAL-20260812-04 Admin modal actions clipped on small viewports

- Symptom: edit lesson/plan and permanent schedule modals were clipped above or below a small browser window, making actions inaccessible.
- Root cause: fixed backdrops forced vertical centering while modal height exceeded the viewport.
- Changed: `EditEkskulPlanButton.tsx`, `EditEkskulLessonButton.tsx`, and `ChangeClassScheduleModal.tsx`.
- Fix: align modals from the visible top edge, add safe-area padding, cap height with `100svh`, enable contained scrolling, and wrap fields/actions.
- Preserve: new long modals should use the same viewport-safe pattern and must not rely on `alignItems: center` without overflow handling.
- Verification: TypeScript, `git diff --check`, production build, route smoke check, and PM2 status passed.

## INTERNAL-20260812-05 Permanent class schedule change

- Symptom: admins could change one session but not the recurring schedule for the remainder of a class.
- Root cause: no class-level operation propagated a new weekday/time to future scheduled sessions.
- Changed: `ChangeClassScheduleModal.tsx`, `/api/admin/classes/[id]/schedule`, and the admin class detail page.
- Fix: update class schedule metadata and only future sessions still in `SCHEDULED` status, preserving completed/cancelled history.
- Preserve: never move completed, cancelled, or otherwise historical sessions during a recurring schedule change.
- Verification: TypeScript and production build passed; route was generated in Next.js output.

## INTERNAL-20260812-06 Coach and Coder issue reporting

- Symptom: Coach and Coder had no structured way to submit LMS errors with screenshots and reproducible context.
- Root cause: reports were handled informally and had no durable database record linked to admin troubleshooting.
- Changed: `issue_reports` migration/types, `/api/issue-reports`, global `IssueReportButton`, admin `/admin/issue-reports`, storage helper, WhatsApp image helper, role layouts, admin navigation, and this documentation.
- Fix: add authenticated report submission with title, description, optional screenshot, optional user-entered related page, automatic user-agent/viewport capture, admin status and resolution workflow, and WhatsApp notification to the configured issue number.
- Preserve: database persistence is authoritative; storage or WhatsApp failure must never delete or reject a valid report. If screenshot storage fails, the in-memory image is still sent to WhatsApp and the user receives a warning. Screenshot types are limited to PNG/JPEG/WebP and 5 MB. Technical Markdown must not contain sensitive report contents.
- Verification: linked Supabase migration applied; 5 focused unit tests, TypeScript, local and production builds passed; unauthenticated API requests return `401`; the protected admin route redirects to the healthy `/login` page; PM2 restarted online. The WhatsApp delivery path reuses the production Baileys sender and was not exercised with a dummy report to avoid sending a false alert. The related-page field is manual and optional; blank input is omitted from the WhatsApp message rather than guessed from the current browser URL.

## INTERNAL-20260812-07 Admin issue reporting

- Symptom: admins could encounter LMS bugs but could not submit them through the same structured report flow.
- Root cause: the report role constraint and global report button only covered Coach and Coder.
- Changed: admin layout, issue report role handling, admin report filter/labels, generated database types, and the role constraint migration.
- Fix: show the floating report button in the admin layout and accept `ADMIN` as a reporter role. Admin reports use the same optional related-page field, screenshot support, database persistence, and WhatsApp notification.
- Preserve: the admin inbox remains an operational follow-up page; submitting a report from admin must not grant access to report-management APIs beyond the existing admin authorization.
- Verification: migration applied to the linked Supabase project; 6 focused tests, TypeScript, production build, and production smoke checks passed. `/login` returned `200`, unauthenticated `/api/issue-reports` returned `401`, and PM2 was online after restart.

## INTERNAL-20260812-08 PWA push fallback for WhatsApp logout

- Symptom: WhatsApp logout cannot notify the team through WhatsApp itself.
- Root cause: there was no device notification channel tied to an authenticated LMS account.
- Changed: PWA manifest/service worker, per-user push subscription migration/API, admin notification controls, and WhatsApp auth-disconnect handling.
- Fix: admins can install the LMS and activate web push. Auth-related WhatsApp disconnects create an internal admin notification and send a push notification to each subscribed active admin. Stale push subscriptions are removed on `404/410` delivery responses.
- Preserve: manual WhatsApp disconnect and force reset do not generate a false logout alert; the existing internal notification bell remains the fallback when push permission is unavailable.
- Verification: push-subscription migration applied; VAPID keys configured only in production environment; 6 focused tests and TypeScript passed; production build passed after restoring dev dependencies needed by PostCSS; PM2 online; `/manifest.webmanifest` and `/sw.js` return `200`; unauthenticated subscription requests return `401`. Actual OS push delivery remains dependent on an admin granting notification permission on a device.

## INTERNAL-20260812-09 Admin notification bell and report inbox

- Symptom: the admin bell appeared but its list could be hidden by the sticky header, and new issue reports were not guaranteed to appear in the internal notification list.
- Root cause: the dropdown was rendered inside the header stacking context; issue reports only persisted the report and sent WhatsApp.
- Changed: `src/components/layout/NotificationDropdown.tsx`, `src/app/api/issue-reports/route.ts`, and `src/components/pwa/PwaNotificationButton.tsx`.
- Fix: render the notification menu in a high-level body portal with viewport-aware positioning and accessible button state; create an admin notification for every saved issue report; separate the PWA install icon from the notification bell and provide an iPhone Add to Home Screen fallback.
- Preserve: notification/report persistence remains best-effort around delivery side effects; saving a report does not fail because notification creation, screenshot storage, or WhatsApp delivery has a problem.
- Verification: local TypeScript and production build passed. Production deployment and authenticated click-through verification are still required after this change.

## INTERNAL-20260812-10 WhatsApp disconnect alert debounce

- Symptom: repeated auth disconnect events could notify admins too frequently.
- Root cause: the in-process disconnect alert guard used a one-minute window.
- Changed: `src/lib/services/whatsappClient.ts`.
- Fix: repeated WhatsApp logout/invalid-auth alerts are suppressed for five minutes; manual disconnect and force reset remain excluded by the existing suppression window.
- Preserve: this debounce is process-local and protects against event bursts; the notification inbox remains the durable record.
- Verification: source change is ready for production build and one controlled notification test.

## INTERNAL-20260812-11 Admin and Ekskul mobile responsiveness

- Symptom: the Ekskul plan and lesson pages were difficult to use on phones because fixed-width tables required horizontal scrolling, the plan selector compressed the detail panel, and long modal actions could fall outside a short viewport.
- Root cause: desktop table and two-column layouts remained active at mobile widths, while bulk-edit and CRUD dialogs had no narrow-screen interaction layout.
- Changed: global admin responsive styles, Ekskul plan overview, lesson list, bulk spreadsheet editor, and add/edit/delete/import lesson dialogs.
- Fix: render dedicated mobile lesson cards with visible actions, stack the plan overview, make controls at least 44px tall, keep modal action bars within the device safe area, and turn bulk-edit rows into labeled single-column forms.
- Preserve: desktop continues to use the existing tables and spreadsheet workflow; mobile cards must use the same lesson IDs and update/delete components so both layouts mutate identical records.
- Verification: `git diff --check` reported no whitespace errors and the local production build passed, including TypeScript and route generation.

## INTERNAL-20260812-12 Curriculum report board mobile layout

- Symptom: the lesson-report board rendered four narrow columns on mobile, causing long lesson titles, coach names, and controls to wrap or become difficult to read.
- Root cause: the board's mobile CSS targeted `.kanban-grid`, but the grid element had no matching class; the intended responsive rule never applied.
- Changed: `KanbanBoard.tsx` and the global responsive stylesheet.
- Fix: add the missing board/column/card hooks, show one full-width status column at a time on small screens, and add status tabs with live counts so admins can switch columns without horizontal scrolling.
- Preserve: desktop drag-and-drop remains available; mobile keeps the existing select-based status update for reliable touch interaction.
- Verification: local production build passed after the change.

## INTERNAL-20260812-13 PWA startup splash branding

- Symptom: opening the installed LMS PWA showed the generic favicon/icon treatment instead of a branded startup experience.
- Root cause: the app had no standalone-only startup layer; the existing manifest icon was designed for a square launcher surface rather than the full Clevio wordmark.
- Changed: `src/components/pwa/PwaStartupSplash.tsx` and the root layout.
- Fix: add a short animated standalone splash with the full Clevio Innovator Camp logo, progress line, loading status, reduced-motion support, and automatic fade-out. Browser sessions are unaffected.
- Preserve: the splash only renders in PWA standalone mode and does not block browser navigation or authenticated content after startup.
- Verification: local production build passed. Production deployment and installed-PWA visual verification are pending device reopen.

## INTERNAL-20260812-14 Native PWA splash icon branding

- Symptom: Android showed the old square/favicon-like icon before the React splash loaded.
- Root cause: the operating system renders the manifest icon before JavaScript starts, so the in-app splash cannot control that first frame.
- Changed: new full-wordmark PWA icon assets, `public/manifest.webmanifest`, and `public/sw.js` cache/icon references.
- Fix: use a safe-area square asset containing the complete Clevio Innovator Camp wordmark for the native launch surface and push notifications, and bump the service-worker cache version.
- Preserve: old icon files remain available for compatibility; the manifest now points only to the new full-wordmark assets.
- Verification: new 192px/512px assets visually inspected, local build pending, and installed PWA must be removed/reinstalled because Android caches launcher icons at install time.

## INTERNAL-20260813-01 Coach report assessment link

- Symptom: Coach could open a draft rapor, but had no direct link back to the lesson assessment used to produce it.
- Root cause: `block_reports` aggregates scores by class, block, and coder without storing a single `session_id`; the report review page therefore had no assessment URL to render.
- Changed: `src/app/(coach)/coach/reports/[id]/page.tsx`, `src/app/(coach)/coach/reports/[id]/ReportReviewClient.tsx`, `src/app/(coach)/coach/rubrics/[sessionId]/page.tsx`, and `src/app/(coach)/coach/rubrics/[sessionId]/EvaluationFormClient.tsx`.
- Fix: resolve the latest completed session in the report's block that has an evaluation for the coder, expose a `Buka Penilaian` action on report review, and preload saved scores when the form opens.
- Preserve: report aggregation remains unchanged; the link only uses sessions authorized by the existing Coach route and does not invent a session when no matching evaluation exists.
- Verification: `git diff --check` and local `npm run build` passed, including TypeScript and route generation. Production deployment and authenticated click-through verification are pending.

## INTERNAL-20260813-02 Class-scoped lesson extension

- Symptom: a lesson estimated for a fixed number of meetings could need one extra meeting because of class-specific conditions, but Coach had no safe way to insert that part without editing the global curriculum.
- Root cause: `class_lessons` supported per-class scheduling but had no transactional extension history or marker that distinguished an intentional class override from template-owned parts. The existing curriculum synchronizer would also delete any extra row above `lesson_templates.estimated_meeting_count`.
- Changed: migration `20260813090000_lesson_extensions.sql`, generated Supabase types, `/api/coach/lessons/extend`, `lessonExtension` service, Coach attendance page/wrapper, `lessonRebalancer`, and focused contract tests. No environment setting changed.
- Fix: add a service-role-only PostgreSQL transaction that locks one WEEKLY class, verifies the source is the lesson's current final part, creates one `is_extended` class lesson in the next non-cancelled meeting, shifts only later lessons of that class, records the reason and actor in `lesson_extensions`, and notifies active admins. The button is available in Coach attendance only on the eligible final part; a responsive confirmation dialog requires a reason. Future curriculum sync includes and preserves marked extension rows.
- Preserve: the transaction never updates or inserts `lesson_templates`, never touches a different `class_id`, preserves source and earlier session assignments, refuses extension when a later class session is already completed, ignores cancelled meetings when selecting the target, does not alter attendance/evaluation/invoice data, and prevents duplicate extension from the same source part. Another extension remains possible from the newly created final part.
- Regression risks: a class must have a valid future active session; the service calls the existing 16-week rolling session generator first. Session generation remains WEEKLY-only and uses the class's current permanent schedule. Admin notification failure is best-effort and does not roll back a successful extension.
- Verification: six focused contract tests and `tsc --noEmit` passed; local and VPS production builds passed; linked Supabase migration `20260813090000` applied; production schema probes confirmed `lesson_extensions` and `class_lessons.is_extended`; an invalid-ID RPC probe reached validation while class-lesson and global-template row counts stayed unchanged; unauthenticated production API returned `401`; `/login` and `/` returned `200`; PM2 `lms` recovered online after a complete rebuild and fresh public requests added no error-log lines. No real class was extended during verification.

## INTERNAL-20260813-03 Admin operational notification push fan-out

- Symptom: several events that required admin action only appeared in their respective dashboard queue or notification bell; admins using the installed PWA did not consistently receive a device notification.
- Root cause: admin notification producers used a mix of direct `notifications` inserts, `createNotification`, and explicit `sendPushToUsers` calls. Direct inserts skipped PWA delivery, while centralizing naively would have duplicated existing WhatsApp-disconnect and lesson-extension pushes.
- Changed: notification DAO, issue-report route, lesson-report route, free-trial registration route, trial-assessment submission route, coach-leave route, coach-report submission route, WhatsApp disconnect service, lesson-extension service, and focused notification contract tests. No database schema or environment setting changed.
- Fix: add one active-admin fan-out that batch-inserts authoritative website notifications and then sends best-effort PWA push with an event-specific admin URL and stable tag. It now covers new free trials, trial assessments ready for review, coach leave requests, coach-submitted reports, lesson reports, user issue reports, class lesson extensions, and WhatsApp disconnects. Existing parallel push calls were removed to prevent duplicates.
- Preserve: saving the business event and website notification remains authoritative; a missing VAPID configuration, device subscription, or push-delivery failure cannot roll back the source event. Coach/coder reminders and bulk broadcasts keep their existing recipient behavior and do not trigger admin push. Generic single-recipient notifications only perform the admin-role lookup when explicit push metadata is supplied.
- Regression risks: device delivery still requires an active push subscription and browser/OS permission. The service worker opens the supplied admin route when the notification is tapped; authenticated middleware remains responsible for access control.
- Verification: 23 focused tests, the final notification contract test, `tsc --noEmit`, `git diff --check`, local production build, and VPS production build passed. Production `/login` returned `200`; protected notification and Coach action APIs returned `401` while signed out; PM2 `lms` restarted online; the error log timestamp remained older than the final restart. No dummy operational event or push was created during verification.

## INTERNAL-20260813-04 Ekskul example project links

- Symptom: RnD could not store or manage a link to an example project for an Ekskul lesson through the lesson tools.
- Root cause: the database and single-lesson API already had `example_url`, but the admin add/edit forms, overview/list views, bulk editor, CSV import, CSV export, and bulk-update API did not carry the field consistently.
- Changed: Ekskul lesson add/edit/list/overview/spreadsheet/import UI, `/api/admin/ekskul/lessons/bulk-update`, and `/api/admin/ekskul/plans/[id]/export`. No database schema or environment setting changed.
- Fix: add an optional URL input, show the link in desktop/mobile lesson views, preserve it during edit and bulk edit, accept `example_url` in CSV import, and include it in CSV export.
- Preserve: the existing lesson editor invariant remains intact: the editor fetches by immutable lesson ID plus `planId`; existing CSV files without `example_url` remain valid; lesson sync behavior is unchanged.
- Regression risks: invalid URLs are rejected by the existing Zod URL validation; users must provide a publicly reachable project URL for other roles to open it.
- Verification: focused example-project contract tests passed (2 files, 5 tests), `tsc --noEmit` passed, local production build passed, VPS production build passed, production `/login` returned `200`, the protected Ekskul lesson API returned `401` while signed out, and PM2 `lms` is online after restart. Authenticated visual click-through could not be completed because the available browser session returned `ERR_TOO_MANY_REDIRECTS`.

## INTERNAL-20260813-05 Weekly-style Ekskul lesson management UI

- Symptom: `/admin/ekskul` used a simplified lesson table that did not expose the checklist, bulk editor, material columns, and responsive lesson cards used by the Weekly lesson manager.
- Root cause: the plan overview maintained a second, reduced lesson table instead of reusing the complete `EkskulLessonList` manager already used by the detail page.
- Changed: `EkskulSplitViewClient.tsx`, `EkskulLessonList.tsx`, responsive styles in `globals.css`, and focused UI contract tests. No API, database schema, migration, or environment setting changed.
- Fix: render one shared lesson manager from both Ekskul entry points, align the selected row/card state with Weekly, and key the manager by plan ID so checklist and modal state reset when the selected plan changes.
- Preserve: selecting a lesson only changes client-side UI state; no bulk-delete or automatic save path was added. Existing lesson edits remain scoped by immutable lesson ID plus `planId`, and data changes still require an explicit add/edit/import/spreadsheet action followed by save confirmation.
- Regression risks: the overview now renders the wider complete lesson table on desktop and responsive cards on mobile; any future lesson column should be added to the shared manager rather than duplicated in the overview.
- Verification: 3 focused test files passed (7 tests), `tsc --noEmit`, `git diff --check`, and local/VPS production builds passed. Production `/login` returned `200`, signed-out `/admin/ekskul` returned the expected `307`, and PM2 `lms` is online. Authenticated screenshot and click-through validation remain blocked because the available browser session returns `ERR_TOO_MANY_REDIRECTS`.
## INTERNAL-20260813-06 Separate Ekskul plan index and lesson manager

- Symptom: the Ekskul lesson-plan overview still showed the plan list beside the selected plan's lesson manager, while Weekly uses a list page followed by a separate management page.
- Root cause: `/admin/ekskul` kept selected-plan state and rendered `EkskulLessonList` inline even though `/admin/ekskul/[id]` already owned the complete lesson-management workflow.
- Changed: `src/app/(admin)/admin/ekskul/EkskulSplitViewClient.tsx`, admin responsive styles, and Ekskul UI contract tests.
- Fix: make `/admin/ekskul` a responsive plan index only; each `Kelola Lesson` action navigates to `/admin/ekskul/[id]`, where checklist, add/edit, import/export, and spreadsheet controls remain available.
- Preserve: plan and lesson IDs, database queries, mutation routes, ordering, example-project links, and all lesson data remain unchanged. The overview must never render or mutate lesson records directly.
- Regression risks: plan-level edit/delete controls and narrow-screen action layout must remain usable; the detail route must retain its back navigation and full lesson manager.
- Verification: 4 focused Vitest contracts passed, `npx tsc --noEmit` passed, `npm run build` passed with 103 generated routes, and `git diff --check` found no whitespace errors.
## INTERNAL-20260813-07 Cross-device agent and test boundaries

- Symptom: a fresh checkout could not infer the complete LMS engineering workflow from repository files, and the root Vitest command also collected tests from a separately nested `event-manager` repository. Two invoice WhatsApp assertions still described behavior that had already changed to a plain `wa.me` contact link.
- Root cause: `AGENTS.md` only documented error-log mechanics, the root Vitest config used its broad default include pattern, and the invoice source contracts had not been updated with the latest no-prefilled-message requirement.
- Changed: `AGENTS.md`, `vitest.config.ts`, `src/lib/__tests__/invoiceWhatsapp.contract.spec.ts`, and this error-fixing log.
- Fix: document the repository architecture, user interaction rules, investigation workflow, data/deployment safety, verification standard, and Git publication rules; constrain root Vitest discovery to `src/**`; and align the invoice contract with the current configured admin number and plain `wa.me` URL.
- Preserve: production invoice behavior is unchanged, `event-manager` remains an independent nested repository, and no environment values, credentials, sessions, or user data are committed.
- Regression risks: future LMS tests must stay under `src/**`; a separately checked-out package must run its own test command; future invoice contact changes must update both implementation and the focused contract.
- Verification: the final root suite passed 34 test files and 161 tests, `npx tsc --noEmit` passed, `git diff --check` passed, and `npm run build` generated all 103 routes successfully.

## INTERNAL-20260814-01 Atomic deploy build rejected shared runtime symlink

- Symptom: the first isolated production release failed during `next build` before any PM2 switch, while the existing production process remained online.
- Root cause: the deploy script attached `.data` to shared persistent storage before compilation. Turbopack rejects project-directory symlinks that point outside its filesystem root when `invoicePaymentStore` resolves runtime invoice JSON files.
- Changed: `scripts/deploy-production.sh`, its focused deployment contract, and this error-fixing log. No application route, database schema, environment value, runtime data, or active PM2 process changed.
- Fix: remove runtime-directory links before compilation and attach `.uploads`, `baileys_auth_info`, `.data`, and `public/uploads` only after the release build succeeds. `.env` remains available during build.
- Preserve: candidate build and smoke test still complete before the atomic current-link switch; the existing deployment lock, exact-production-SHA check, persistent runtime storage, post-switch health checks, and automatic rollback remain unchanged.
- Regression risks: future build-time code must not require files from these runtime directories; candidate startup remains the first point where shared runtime data is attached.
- Verification: the failed candidate stopped before switch; `/root/lms/current` still resolved to `/root/lms/lms-apps`, PM2 remained online on its original PID, and public `/login` returned HTTP 200. After the fix, Bash syntax, the focused contract, TypeScript, all 35 test files, and the production build passed. The isolated VPS release built successfully, passed its port-3010 smoke test, and cut over to SHA `11451119a4deadd696831a8dd6c832d263efe067`. PM2 was online with zero restarts; `/`, `/login`, and `/event-manager` returned HTTP 200; protected Admin, Coach, and Coder routes returned signed-out redirects; Nginx passed config validation; all runtime links resolved under `/root/lms/shared`; no error-log writes occurred after switch; and a same-SHA deployment probe was idempotent without changing the PM2 PID. `/root/lms/lms-apps` remained a valid rollback target.

## INTERNAL-20260814-02 Expired trial remained on Coach dashboard

- Symptom: an Admin report said a trial menu remained visible on the Coach dashboard after its scheduled session had started; the affected area was the Coach dashboard and the corresponding Admin free-trial list.
- Root cause: `listActionableTrialClassesForCoach` filtered scheduled trials from 30 days in the past but had no lower bound at the current time, so past `SCHEDULED` rows stayed actionable in the dashboard.
- Changed: `src/lib/dao/trialClassDao.ts`, Admin free-trial delete route and table, `src/lib/services/adminTrialDeletion.ts`, and focused trial-management tests. No migration, schema, environment setting, or production data changed.
- Fix: only return scheduled trials whose `scheduled_at` is now or later, including an in-memory boundary guard; add an Admin-only `DELETE /api/admin/free-trials/:id` action that refuses any trial with an assessment and removes its Google Calendar event before the database row.
- Preserve: coach ownership and `SCHEDULED`/assessment filtering remain enforced; deletion is unavailable to non-Admin sessions, a draft or progressed assessment is never cascaded away, and a Calendar cleanup failure leaves the database row untouched. The delete action was not invoked against production.
- Regression risks: if the database delete fails after Calendar cleanup succeeds, the trial remains available for retry but its Calendar event must be recreated by the existing Admin scheduling flow. This ordering intentionally prevents deleting application data while leaving an external meeting behind.
- Verification: 35 test files and 165 tests passed; `npx tsc --noEmit`, `git diff --check`, and the production build with safe dummy local build variables passed. Commit `5a186b462843ea94ab106439bae42021dc294b54` was pushed and fast-forwarded from production `f7dbe0297f7037976be220ed00e05e598990fbe0`; the deployment script built and smoke-tested an isolated release, then switched `/root/lms/current` to the exact new SHA. PM2 `lms` is online with zero restarts on that release, the previous release remains available as rollback target, and public smoke checks returned `/` 200, `/login` 200, and signed-out `/admin/free-trials` 307 to `/login` (IPv4 retry). The recent PM2 log still contains historical ChunkLoadError traces from the old `/root/lms/lms-apps` checkout and repeated pre-existing `short_links` fallback warnings; no deployment rollback was triggered. Authenticated UI click-through was not available, the delete endpoint was not invoked, and no production database mutation was performed.

## INTERNAL-20260814-03 Behavioral safety review for trial time and deletion

- Symptom: the initial implementation had only source-contract assertions for a time boundary and allowed Admin deletion when a trial had a `DRAFT` assessment, which could cascade-delete that assessment.
- Root cause: the dashboard relied on the database lower-bound query without an in-memory boundary check, and the delete preflight treated `DRAFT` as safe even though `trial_assessments.trial_id` uses `ON DELETE CASCADE`.
- Changed: `src/lib/dao/trialClassDao.ts`, `src/lib/services/adminTrialDeletion.ts`, the Admin delete route, and `src/lib/__tests__/trialClassBehavior.spec.ts`. No migration, schema, environment setting, or production data changed.
- Fix: behavioral tests now exercise before/exact/after schedule boundaries, null/invalid schedules, draft and progressed assessment protection, Calendar-before-database ordering, Calendar failure handling, and missing-record handling with mocked dependencies. Deletion now refuses every trial with an assessment and refuses database deletion when Calendar cleanup fails.
- Preserve: exactly-at-now and future scheduled trials remain visible, draft assessments remain accessible to the Coach dashboard, only Admin route sessions can invoke deletion, and no real production delete request is used for verification.
- Regression risks: a database failure after successful Calendar cleanup can require the Admin to reschedule the event; this is safer than deleting the trial first and leaving an orphan meeting. The route still needs authenticated browser click-through when an Admin is available.
- Verification: behavioral trial tests passed (8 tests); the full suite passed 36 files and 173 tests, TypeScript, `git diff --check`, and the safe-env production build passed. Commit `88def9759dd35e0b29f41b39320575c7b3dc4dec` was rebased onto production `8656650fd2f12235fc1a78b2701f8be36b80770b`, pushed, fast-forwarded without force-push, and deployed from exact `origin/production`. `/root/lms/current` resolves to the new SHA, PM2 is online with zero restarts, five releases are retained, and the previous SHA remains a rollback target. The deployment script's protected/public login smoke passed; a later direct browser-device DNS probe returned `ENOTFOUND` intermittently. No authenticated Admin click-through or delete request was executed, and no production database mutation occurred.

## INTERNAL-20260814-04 Admin bell notifications must reach PWA

- Symptom: an Admin notification could appear in the bell list without a guaranteed PWA attempt when it was created through the generic single-recipient notification helper; this risked report-related alerts being visible only in the website inbox.
- Root cause: `createNotification` persisted every notification but only resolved an active Admin recipient and sent PWA push when explicit delivery metadata was supplied. The shared Admin fan-out already covered issue reports and the other operational events, but the generic helper did not enforce the bell-to-PWA invariant.
- Changed: `src/lib/dao/notificationsDao.ts`, `src/lib/__tests__/notificationsDao.behavior.spec.ts`, `src/lib/__tests__/adminNotificationPush.contract.spec.ts`, and this documentation. No migration, database schema, environment setting, or production data changed.
- Fix: after the durable notification insert, the helper now checks whether the recipient is an active Admin and always attempts PWA delivery. Notifications without event-specific metadata use `/admin/dashboard` and a stable `admin-<type>` tag; report alerts keep their specific route and tag through `createAdminNotifications`.
- Preserve: Coach/Coder notifications are not pushed through the Admin channel; notification persistence remains authoritative; PWA failures are logged and never roll back the saved bell item; event-specific URLs and tags remain unchanged.
- Regression risks: actual device delivery still depends on VAPID configuration, browser permission, active subscription, endpoint health, and network. There is no retry queue for a failed endpoint.
- Verification: behavioral tests cover generic Admin delivery, issue-report fan-out to every active Admin, non-Admin exclusion, insert-before-push ordering, and push failure isolation. Focused notification tests passed (7 tests); the full suite passed 37 files and 177 tests, `npx tsc --noEmit`, `git diff --check`, and the safe-env production build passed. Production was fast-forwarded from `a45888e30a197ab095a11fdd949aea466eaa1323` to `87399bfc82ef6588d023f0d09f5fcadd57576cd5` and deployed by the release script. `/root/lms/current` resolves to the new SHA, PM2 `lms` is online with zero restarts, `/` and `/login` return `200`, signed-out `/api/issue-reports` returns `401`, five releases are retained, and the previous SHA remains available for rollback. The latest error-log write predates the new process and contains only historical duplicate-key and short-links fallback warnings; no new error was logged after restart. Authenticated PWA device click-through remains pending, and no production database mutation or dummy push was performed.

## INTERNAL-20260814-05 Rolling Weekly sessions lost lesson assignments

- Symptom: completed meetings in `Creator Jumat` under Coach Arya Adhi had empty lesson data even though the class schedule continued normally.
- Root cause: rolling session generation intentionally stopped invoking the old lesson rebalancer because that operation reset manual lesson shifts. The preserve-mode replacement still selected every historical unassigned lesson and session, so it was unsafe to call when old curriculum blocks contained gaps.
- Changed: `src/lib/dao/sessionsDao.ts`, `src/lib/services/lessonAutoAssign.ts`, `src/lib/services/lessonAssignmentWindow.ts`, the focused frontier regression test, and this documentation. No schema, migration, environment setting, or global lesson template changed.
- Fix: preserve mode now establishes a forward frontier from the latest session that already owns a lesson, ignores older holes, and fills only unassigned sessions and lessons after that frontier. Template synchronization uses the same immutable block ordering as assignment and is limited to the frontier block and newer blocks, preventing changed global templates or inconsistent legacy dates from rewriting historical copies. Rolling session generation invokes this safe preserve flow only when it actually creates new sessions.
- Preserve: completed mappings, manually shifted lessons, cancelled sessions, stale historical gaps, other classes, and global lesson templates remain untouched. Assignment and the production repair are scoped by class ID.
- Regression risks: a class with intentionally unassigned future sessions after its latest assigned lesson will now be filled by the curriculum sequence. Admins should use the existing cancel/reschedule and manual lesson controls when a future gap is intentional.
- Verification: the focused frontier and lesson-reflow tests passed (7 tests); the full suite passed 38 files and 181 tests; `npx tsc --noEmit`, `git diff --check`, and a production build with safe local build variables passed. Production was fast-forwarded without force-push and deployed at SHA `90136b3d8e9fd4726b3f03d3bef46b7dc2695b5b`; PM2 `lms` is online with zero restarts, `/` and `/login` return `200`, and signed-out `/admin/classes` redirects to login. The class-scoped preserve repair assigned six completed sessions from 3 July through 7 August 2026 and eleven future sessions, left the cancelled 14 August session empty, and reported no overwritten assignment. A second execution returned `assigned: 0`, confirming idempotence. The previous release remains the rollback target. A professional completion notice was sent successfully through the connected LMS WhatsApp service to the configured operational recipient.

## INTERNAL-20260814-06 VPS CPU contention from a crash-looping WhatsApp bot

- Symptom: LMS dashboards became noticeably slow even though the LMS process remained online and public signed-out routes still returned successful responses.
- Root cause: the unrelated PM2 process `jsb` (`whatsapp-dashboard-bot`) could not find its required Puppeteer Chrome binary and restarted every second. It reached 38,591 restarts and consumed up to 200% CPU, competing with the LMS for both VPS cores.
- Changed: no LMS source, schema, environment, database, or deployed release changed. The nonfunctional `jsb` PM2 process was stopped and the PM2 process list was saved so the crash loop does not resume after reboot.
- Fix: keep `jsb` stopped until its matching Chrome binary and Puppeteer cache configuration are repaired. The LMS `clevio-wa` process is separate and remained online throughout.
- Preserve: PM2 `lms`, `clevio-wa`, Event Manager, N8N, Redis, and production data were not restarted or modified.
- Regression risks: the separate `jsb` automation remains unavailable while stopped. Restoring it requires installing the exact Puppeteer Chrome version, validating a stable startup, then explicitly starting and saving the PM2 process again.
- Verification: before mitigation, `jsb` showed 38,591 restarts and transient 200% CPU. After `pm2 stop jsb` and `pm2 save`, it remained stopped at 0 CPU; PM2 `lms` stayed online with zero restarts. Local VPS response time improved from about 54 ms to 9 ms for `/`, `/login` returned in about 21 ms, and signed-out Coach/Admin dashboard routes redirected in about 2 ms. Available memory remained above 5 GiB and disk utilization was 61%.

## INTERNAL-20260814-07 Mobile lesson edit actions

- Symptom: on a phone, Admin could not find the edit action for Weekly or Ekskul lessons.
- Root cause: the active Weekly block manager only rendered a wide desktop table, placing the action column outside the phone viewport. Ekskul already rendered mobile cards, but its edit trigger was a compact generic text control without an icon or explicit accessible lesson label.
- Changed: `src/app/(admin)/admin/curriculum/BlockLessonList.tsx`, `src/app/(admin)/admin/ekskul/[id]/EditEkskulLessonButton.tsx`, `src/app/globals.css`, and `src/lib/__tests__/ekskulLessonManagementUi.contract.spec.ts`. No database schema, API contract, lesson data, or environment setting changed.
- Fix: add a dedicated Weekly mobile lesson list with visible 44px edit/delete actions, links, lesson metadata, and selection state; reuse the existing `editingLessonId` and `EditLessonModal` so desktop and mobile edit the identical lesson record. Make the Ekskul edit trigger an icon-and-text action with a lesson-specific accessible label.
- Preserve: desktop keeps the existing table and spreadsheet workflow; mobile Weekly and Ekskul actions continue to use the same immutable lesson IDs, delete components, and edit APIs as desktop.
- Regression risks: responsive visibility rules must not show both table and cards at the same breakpoint, and edit controls must remain reachable if lesson titles or summaries are long.
- Verification: the focused contract passed 3 tests; the full suite passed 38 files and 182 tests; `npx tsc --noEmit`, `git diff --check`, and a production build with safe local build variables passed with 103 generated routes. Production was fast-forwarded without force-push and the application commit was deployed at SHA `9ea34780424117e9c90814994c6cdb2e185b5836`; PM2 `lms` and `clevio-wa` are online with zero restarts, PM2 `jsb` remains intentionally stopped, `/` and `/login` return `200`, signed-out `/admin/curriculum` redirects with `307`, and recent LMS logs contain no fatal/uncaught errors. Authenticated mobile browser verification remains pending because the available in-app browser cookie session entered a redirect loop while direct public route probes remained healthy.

## INTERNAL-20260815-01 Unassessed trial disappeared after scheduled time

- Symptom: a scheduled trial assigned to an active Coach was no longer visible on the Coach dashboard after its 15.00 WIB start time, even though no assessment had been submitted.
- Root cause: `listActionableTrialClassesForCoach` applied a database lower-bound filter on `scheduled_at >= now`, so every past scheduled trial was removed before the in-memory assessment-status guard could preserve unfinished work.
- Changed: `src/lib/dao/trialClassDao.ts`, `src/lib/__tests__/trialClassBehavior.spec.ts`, `src/lib/__tests__/trialClassManagement.contract.spec.ts`, and this documentation. No migration, schema, or production data mutation was performed.
- Fix: keep `SCHEDULED` trials with no assessment or a `DRAFT` assessment visible until assessment completion; continue excluding invalid schedules, cancelled/failed trials, and submitted/converted assessments. The dashboard query now loads a bounded set of scheduled rows so the in-memory assessment guard can decide visibility.
- Preserve: coach ownership remains enforced by `coach_id`, assessment records are never deleted or reset, and Admin assignment behavior is unchanged. The investigated production trial was already assigned to the requested active Coach, so no reassignment mutation was needed.
- Regression risks: genuinely abandoned scheduled trials remain visible until an assessment or terminal status is recorded; Admin should cancel/fail stale trials through the existing workflow when appropriate.
- Verification: behavioral boundary tests cover past unassessed, past draft, exact-now, future, submitted, cancelled, null, and invalid schedules. Focused trial tests passed (17 tests); the full suite passed 38 files and 182 tests, `npx tsc --noEmit`, `git diff --check`, and the safe-env production build passed. Production was fast-forwarded from `bdb181995d89562c832b35de06fc353ff694d08f` to `67697e5a2da9db740fefd316b50b152033c90522` and deployed by the release script. `/root/lms/current` resolves to the new SHA, PM2 `lms` is online with zero restarts, `/` and `/login` return `200`, signed-out `/coach/dashboard` redirects with `307`, signed-out `/api/issue-reports` returns `401`, five releases are retained, and the previous SHA remains available for rollback. A post-deploy read-only probe confirmed the trial remains assigned to the active requested Coach with no assessment record; no trial or assessment data was deleted or reassigned. The requested detail report was sent successfully through the connected LMS WhatsApp service (HTTP 200). Authenticated Coach click-through remains pending.

## INTERNAL-20260818-01 Coach trial assessment submission rejected

- Symptom: Coach could complete the visible trial assessment form, but submission returned `Data assessment belum lengkap atau tidak valid.` and the assessment remained in `DRAFT`.
- Root cause: the Coach form and API maintained separate validators, so serialized values could pass the client checks but fail the API schema before persistence. The API also discarded field-level validation details, making the mismatch difficult to diagnose.
- Changed: `src/lib/services/trialAssessmentSubmission.ts`, `src/app/(coach)/coach/trials/[id]/TrialAssessmentForm.tsx`, `src/app/api/coach/trials/[id]/assessment/route.ts`, `src/lib/__tests__/trialAssessmentSubmission.spec.ts`, and this documentation. No migration, database data, or environment setting changed.
- Fix: use one shared submission schema on the Coach form and API; safely coerce rubric ratings while retaining the strict 1-4 range; normalize the client payload before sending; and log only validation paths and error codes on the server, never submitted values.
- Preserve: Coach ownership, scheduled-trial checks, draft-only assessment state, UUID level validation, availability enums, text length limits, and all existing report-generation behavior remain enforced server-side.
- Regression risks: malformed or out-of-range ratings remain rejected, and the new diagnostic log intentionally contains only field paths and Zod error codes. No production record is auto-created or modified by validation.
- Verification: focused submission-schema tests passed (2 tests); the full suite passed 39 files and 184 tests; `npx tsc --noEmit`, `git diff --check`, and the safe-env production build passed with 103 generated routes. Production was fast-forwarded and deployed at SHA `518d89cc512a37ce7c10b18a48d4818cbf4318ad`; `/` and `/login` returned `200`, signed-out `/admin/curriculum` returned `307` to `/login`, PM2 `lms` and `clevio-wa` were online with zero restarts, and recent LMS logs contained no fatal or uncaught errors. The completion report was sent through the connected LMS WhatsApp service with HTTP 200. Authenticated Coach click-through remains the only pending device-level verification.

## INTERNAL-20260818-02 Coach trial observation length rejected

- Symptom: the same Coach trial assessment still returned `Data assessment belum lengkap atau tidak valid.` after the validator mismatch fix.
- Root cause: the production validation log identified `personalizedObservation` with Zod code `too_big`; the shared schema still capped the Coach's free-form observation at 800 characters, while the form allowed longer text.
- Changed: `src/lib/services/trialAssessmentSubmission.ts`, `src/app/(coach)/coach/trials/[id]/TrialAssessmentForm.tsx`, `src/lib/__tests__/trialAssessmentSubmission.spec.ts`, and this documentation. No migration or database data changed; the database column is already `text`.
- Fix: raise the shared observation limit to 2,000 characters, apply the same limit to the textarea, and show a live character counter so the Coach can correct the field before submitting.
- Preserve: the minimum 20-character requirement, shared client/server validation, all rubric/level/availability checks, and server-side ownership/status checks remain unchanged.
- Regression risks: longer parent-report insight text may require normal responsive wrapping; values above 2,000 remain rejected to keep reports bounded.
- Verification: production log evidence was read-only; focused submission tests passed (3 tests), the full suite passed 39 files and 185 tests, `npx tsc --noEmit`, `git diff --check`, and the safe-env production build passed with 103 generated routes. Production was fast-forwarded and deployed at SHA `f673cd7cc19cc48ab7389d3ec30bf5e1108468df`; `/` and `/login` returned `200`, signed-out `/admin/curriculum` returned `307` to `/login`, PM2 `lms` and `clevio-wa` were online with zero restarts, and recent LMS logs contained no fatal or uncaught errors. The corrected root-cause report was sent through the connected LMS WhatsApp service with HTTP 200. Authenticated Coach click-through remains the only pending device-level verification.

## INTERNAL-20260818-03 Recurring class schedule control was hard to discover

- Symptom: the recurring schedule feature already existed, but Admin could easily miss it because the action was embedded in the class metadata row beside the coach and schedule text.
- Root cause: the UI did not distinguish a one-session edit from a class-wide recurring change, and the modal did not prominently state which sessions would be affected.
- Changed: `src/app/(admin)/admin/classes/[id]/page.tsx`, `src/app/(admin)/admin/classes/[id]/ChangeClassScheduleModal.tsx`, and this documentation. No schedule data, API behavior, migration, or production setting changed.
- Fix: add a dedicated responsive “Jadwal Berulang” panel with the current schedule, explicit future-session scope, and a clear “Ubah jadwal berulang” action. Improve the modal with current-schedule context, a close control, dialog semantics, and clearer copy.
- Preserve: `/api/admin/classes/[id]/schedule` still updates class metadata and only future `SCHEDULED` sessions; completed, cancelled, and historical sessions remain untouched.
- Regression risks: the panel uses inline styles consistent with this legacy class-detail page; mobile wrapping must remain usable when class names or schedule text are long.
- Verification: full suite passed 39 files and 185 tests; `npx tsc --noEmit`, `git diff --check`, and the safe-env production build passed with 103 generated routes. Production was fast-forwarded and deployed at SHA `5a6eab4d764da8c23198a5dfe50b62548f7597ee`; `/` and `/login` returned `200`, signed-out `/admin/classes` returned `307` to `/login`, PM2 `lms` and `clevio-wa` were online with zero restarts, and recent LMS logs contained no fatal or uncaught errors. Authenticated Admin visual click-through remains pending because the available session was not used for production mutation.

## INTERNAL-20260818-04 Ekskul class progress stayed at zero

- Symptom: an active Ekskul class showed `0%` progress in the Admin class list even though its sessions had already been completed.
- Root cause: the Admin list always looked for a current `class_blocks` record; Ekskul classes use `ekskul_lesson_plan_id` and session rows instead, so no block meant an unconditional zero. A read-only production check found completed Ekskul sessions across active classes.
- Changed: `src/lib/services/classCycleProgress.ts`, `src/app/(admin)/admin/classes/page.tsx`, `src/lib/__tests__/classCycleProgress.spec.ts`, and this documentation. No migration or production data changed.
- Fix: calculate Ekskul progress from completed versus non-cancelled class sessions; Weekly progress continues using its current-cycle lesson/block calculation.
- Preserve: cancelled sessions do not count toward the denominator, Weekly behavior is unchanged, and the calculation is read-only.
- Regression risks: an Ekskul class with no generated sessions correctly remains at `0%`; future session generation must continue to create the expected session rows.
- Verification: focused progress tests passed (8 tests); full suite passed 39 files and 187 tests; `npx tsc --noEmit`, `git diff --check`, and the safe-env production build passed with 103 generated routes. Production was fast-forwarded and deployed at SHA `daca8c76f755e507398700423e67b0af0bda73ea`; `/` and `/login` returned `200`, signed-out `/admin/classes` returned `307` to `/login`, PM2 `lms` and `clevio-wa` were online with zero restarts, and recent LMS logs contained no fatal or uncaught errors. No production data was mutated.

## INTERNAL-20260819-01 Trial outcome and paid conversion gate

- Symptom: Admin Trial management had scheduling states but no clear result for “belum trial”, “trial selesai belum daftar”, “trial selesai sudah daftar”, or “tidak jadi”; the trial conversion flow also created a coder account before the registration invoice was paid.
- Root cause: schedule status and conversion outcome were being treated as one lifecycle, while the payment hook had not been the boundary for creating the coder and Weekly payment period. The existing assessment status alone was not sufficient proof of registration because it can represent an unpaid invoice flow.
- Changed: `src/lib/services/trialLifecycle.ts`, `src/lib/dao/trialClassDao.ts`, `src/app/(admin)/admin/free-trials/page.tsx`, `src/app/(admin)/admin/free-trials/TrialClassTable.tsx`, `src/app/(admin)/admin/trial-assessments/page.tsx`, `src/lib/services/trialConversion.ts`, related regression tests, and this documentation. No migration or production data was changed.
- Fix: derive a separate outcome badge from the schedule, assessment, and linked invoice; count “sudah daftar” only when the linked invoice is `PAID`; defer trial coder creation and invoice-item linkage until the verified paid hook; create the CCR and an unassigned active Weekly payment period only after payment. Admin still assigns the actual Weekly class because trial recommendations contain level and mode, not a class schedule.
- Preserve: existing schedule statuses and Coach visibility rules remain unchanged; unpaid invoices remain “trial selesai, belum daftar”; no enrollment is created before payment; no global class or lesson data is modified.
- Regression risks: a paid webhook that fails during the post-payment provisioning step can leave the invoice paid while the account provisioning must be retried from the idempotent hook; class placement remains an explicit Admin action after payment.
- Verification: focused lifecycle and trial management tests passed (22 tests); full suite passed 40 files and 192 tests; `npx tsc --noEmit`, `git diff --check`, and the safe-env production build passed with 103 generated routes. Production was fast-forwarded and deployed at SHA `f130b21feb522dcb1c1aa6ab755bfc50ac6e1743`; signed-out `/admin/free-trials` returned `307` to `/login`, `/login` returned `200`, PM2 `lms` and `clevio-wa` were online, and recent LMS logs contained no fatal or uncaught errors. No production data was mutated during verification.

## INTERNAL-20260819-02 Ekskul access and paid trial conversion notifications

- Symptom: Coder Ekskul could see and open lessons that had not been passed yet. After a trial conversion invoice was paid, the operational Admin/PWA and WhatsApp group notifications did not clearly identify the paid trial and its LMS follow-up. The Trial Management action column also pushed actions out of reach on narrower tables.
- Root cause: the Ekskul list path hard-coded every lesson as `isAccessible: true`, while the detail path allowed access for any active enrollment. The Midtrans paid hook provisioned the account but did not fan out a trial-conversion notification after a newly completed conversion.
- Changed: `src/lib/services/coderLessonAccess.ts`, `src/lib/services/coder.ts`, `src/lib/services/trialConversion.ts`, `src/lib/services/trialClassNotifications.ts`, `src/app/api/midtrans/notification/route.ts`, `src/app/(admin)/admin/free-trials/TrialClassTable.tsx`, `src/components/admin/ActionDropdown.tsx`, focused tests, and this documentation. No migration, schema, class, lesson, or production data changed.
- Fix: Ekskul list and detail access now require the matching ordered session to have status `COMPLETED`; a paid trial conversion now creates/confirms the LMS account first, then creates an Admin notification with PWA delivery and sends a formatted conversion summary to the configured trial Admin WhatsApp group. Replayed paid webhooks do not resend the conversion notification. Trial actions are grouped behind an accessible three-dot menu so the action column stays compact.
- Preserve: Weekly access rules are unchanged; cancelled and future Ekskul sessions remain locked; account creation still cannot happen from an unpaid registration invoice; manual Weekly class assignment remains an Admin action; notification delivery is best-effort and cannot turn a successful payment callback into a failure.
- Regression risks: Ekskul lesson-to-session mapping remains order-based because the current Ekskul schema does not store a direct lesson-session foreign key. A completed-session count therefore assumes the existing sequential curriculum model.
- Verification: focused Ekskul access, trial notification, and Trial Management contract tests passed (25 tests); full suite passed 41 files and 195 tests; `npx tsc --noEmit`, `git diff --check`, and a safe-env production build passed with 103 generated routes. GitHub `production` was fast-forwarded to `77f11c90057feab22e6da19ef0fb545bb97136ea`; the previous deployed SHA remains the rollback target. Deployment was not completed because the configured internal SSH target timed out and the fallback host rejected the available public key. Authenticated production smoke checks remain pending.

## INTERNAL-20260819-03 Paid trial must wait for manual LMS account creation

- Symptom: the paid-trial Midtrans webhook automatically created an LMS Coder account, CCR, and Weekly payment period even though the requested operational flow is for Admin to review the paid trial and create the account manually.
- Root cause: `markTrialConversionPaidFromInvoice` treated payment confirmation and account provisioning as one transaction and called the account, CCR, payment-period, and invoice-item provisioning helpers from the webhook path.
- Changed: `src/lib/services/trialConversion.ts`, `src/lib/services/trialClassNotifications.ts`, `src/app/api/midtrans/notification/route.ts`, related regression tests, and this documentation. No migration or production data was changed.
- Fix: a verified paid invoice now only marks the trial assessment as `PAID` and retains the assessment's account/class fields for Admin action. The Admin PWA notification and WhatsApp group message explicitly instruct Admin to create the LMS account manually and then assign the coder to the appropriate Weekly class. The notification contains no generated username or CCR. Replayed paid webhooks remain idempotent and do not resend the notification.
- Preserve: invoice creation remains payment-gated; no account, CCR, payment period, enrollment, or class assignment is created by the payment callback; Weekly and Ekskul data are not modified; notification delivery remains best-effort.
- Regression risks: an Admin must complete account creation and class assignment after payment; a paid assessment can therefore remain without `coder_id` or `payment_period_id` until that manual workflow is completed.
- Verification: focused trial conversion and management contract tests passed (23 tests); full suite passed 41 files and 195 tests; `npx tsc --noEmit`, `git diff --check`, and a safe-env production build passed with 103 generated routes. `production` was fast-forwarded and pushed at SHA `64049654a1c879bf6dea0d4d18a17da6322b39f0`; the application change is in its parent commit `3960d6d`. Deployment completed through the configured public VPS route at SHA `255b3a116a0e4e8d525a9d164a9c7297b40d6c7f`; PM2 `lms` was online, `/` and `/login` returned HTTP 200, recent LMS error logs were empty, and rollback target was `87062410e4dc4add00a3bf947df1c8d68aa3ebd4`. No production data was mutated during verification.

## INTERNAL-20260820-01 Trial metrics, Admin shell, and Ekskul lesson reports

- Symptom: Admin Review Trial showed 9 total assessments while Trial Management showed 5 trials that had actually reached a completed outcome. The Admin sidebar scrolled away with the document, and Coaches could not see the lesson-report action on Ekskul lesson pages.
- Root cause: the Review Trial total included four `DRAFT` assessments that had not been submitted by a Coach; the sidebar used a sticky grid item that was not reliably pinned in the shell; and the Ekskul lesson detail intentionally excluded the shared report button while the report API only resolved Weekly lesson templates.
- Changed: `src/lib/services/trialLifecycle.ts`, `src/app/(admin)/admin/trial-assessments/page.tsx`, `src/lib/__tests__/trialLifecycle.spec.ts`, `src/app/globals.css`, `src/app/(coach)/coach/lesson/[id]/page.tsx`, `src/app/(coach)/coach/lesson/[id]/ReportLessonButton.tsx`, `src/app/api/coach/lesson-reports/route.ts`, `src/app/(admin)/admin/curriculum/reports/page.tsx`, and this documentation. No production data or environment settings changed.
- Fix: Review Trial now counts only non-draft Coach assessments, matching the five submitted/completed trial outcomes. The Admin sidebar is fixed to the viewport with an independently scrollable menu and a mobile grid override. Ekskul lesson pages show the report action; submissions validate the Ekskul lesson, store the report without misusing the Weekly foreign key, and expose the lesson title in the Admin report board.
- Preserve: draft assessments remain excluded from the review queue but are not deleted; Weekly lesson reports still use their existing foreign-key path; no global lesson or class data is modified; sidebar navigation remains available on desktop and through the mobile sheet.
- Regression risks: Ekskul report titles are persisted in the report description marker because the existing `lesson_reports` schema has no Ekskul lesson reference column; Admin rendering strips that marker for display. The report API still uses the existing Coach role boundary.
- Verification: read-only production counts showed 9 assessments (`DRAFT 4`, `PENDING_ADMIN_REVIEW 2`, `PAID 3`) and 5 non-draft assessments, all linked to already-started trials. Focused lifecycle tests passed (5 tests); the full suite passed 41 files and 196 tests; `npx tsc --noEmit`, `git diff --check`, and the safe-env production build passed with 103 generated routes. Production was fast-forwarded and deployed at SHA `157b87b00a6b1bf015a66bb3f32427697db4e2df`; `/` and `/login` returned `200`, signed-out `/admin/free-trials` returned `307` to `/login`, PM2 `lms` and `clevio-wa` were online with zero restarts, and the previous release `2e5c417ced35071d7526552fa8739991aa96a211` remains the rollback target. Production data was not mutated.

## INTERNAL-20260820-02 Coder portfolio creation and stable public sharing

- Symptom: Coder had no structured place to document projects during or outside block evaluation, no stable public showcase link, and no review boundary that kept unfinished edits away from an already shared page.
- Root cause: the LMS only stored block reflection and report records; it had no coder-owned project model, media ordering, immutable program-origin snapshot, approved public snapshot, or lightweight Coach review workflow.
- Changed: additive `coder_portfolio_profiles`, `coder_portfolios`, and `coder_portfolio_screenshots` tables; Coder `Rapor & Portofolio` pages and evaluation shortcut; Coach review queue and dashboard alert; Admin moderation list; public `/portfolio/[slug]` page; storage helpers; portfolio APIs, lifecycle helpers, behavioral tests, and this documentation. Existing user, enrollment, class, lesson, evaluation, and report rows are not rewritten.
- Fix: allow many projects per Coder and block with draft/edit/submit/revision/approve states; retain ownership by stable `coder_id` while storing `WEEKLY` or `EKSKUL` as the project-origin snapshot; keep the last approved JSON snapshot publicly visible during later edits; use one stable non-personal slug; expose school only with Coder consent; and accept one cover plus up to four PNG/JPEG/WebP screenshots below 1 MB each.
- Preserve: switching between Weekly and Ekskul never retags or removes old works, enrollments, learning material, or reports. Coach can approve or request a revision but cannot delete. Coder may delete only an owned project and Admin may delete for moderation; deletion requires the exact title, removes storage before the database row, affects exactly one portfolio, and stops without database mutation if storage cleanup fails. No destructive migration or automatic database rollback is used.
- Regression risks: the additive migration must be applied before application rollout; the configured report-storage bucket must accept portfolio image paths; authenticated Coach/Coder navigation and real device upload behavior need post-deploy smoke verification. Public pages deliberately read only `published_snapshot`, so a new draft cannot leak and an approved version remains stable while edits await reapproval.
- Verification: behavioral lifecycle, draft boundary, approved-snapshot, deletion ordering, URL, authentication-status, and image-signature tests pass; the full suite passes 42 files and 209 tests. `npx tsc --noEmit`, `git diff --check`, and a safe-dummy-env production build pass with 109 generated routes. The public experience was compared visually with the supplied reference in the in-app browser at desktop size: the real Clevio logo, responsive hero, identity/program badges, project grid, and dialog semantics render without console errors. The additive migration completed successfully in one transaction; read-only post-migration counts exactly matched the pre-migration baseline for `users` (218), `enrollments` (119), `classes` (31), `blocks` (29), and `block_reports` (30), while all three new portfolio tables contained zero rows. Production was fast-forwarded and deployed atomically at SHA `93cf77b506f7fc419ca5c170c05bfa9757dd16f7`; PM2 `lms` was online from that release with zero restarts, `/` and `/login` returned `200`, signed-out Coder/Coach/Admin portfolio routes redirected to `/login`, protected portfolio APIs returned `401`, and an unknown public slug rendered the Next.js not-found boundary without portfolio content. Recent LMS error logs contained no fatal, uncaught, unhandled, missing-relation, or portfolio errors; five releases were retained and `0c1489856fd52151257e8d165c18fd23238fc9e0` remained the rollback target. No production row or file was created, updated, or deleted during migration or smoke verification. Authenticated Coder/Coach mobile click-through and real-device image upload remain manual verification items because production test records were intentionally not created.

## INTERNAL-20260820-03 Evaluation card priority and portfolio shortcut window

- Symptom: a Coder who had already submitted a historical block evaluation still saw `Isi Portofolio` on the dashboard outside an evaluation session. Opening the old evaluation URL also re-entered the realtime interactive flow instead of acknowledging the saved submission.
- Root cause: `BlockEvaluationCard` selected the latest `completed` evaluation session with no time boundary and kept it visible until a linked portfolio existed. The evaluation page also discarded the authenticated Coder ID and did not check `block_evaluations` before mounting the polling client for a completed submission.
- Changed: `src/app/(coder)/coder/dashboard/BlockEvaluationCard.tsx`, `src/app/(evaluations)/coder/evaluation/[classId]/[blockId]/[sessionId]/page.tsx`, `src/app/api/coder/block-evaluations/route.ts`, `src/lib/coderEvaluationDashboard.ts`, focused behavioral tests, and this documentation. No migration, production row, or runtime configuration is changed.
- Fix: an unfinished reflection always renders only `Masuk Evaluasi`, so the portfolio shortcut cannot replace or sit beside the required evaluation action. After submission, `Isi Portofolio` is available only during the four-hour window that starts when Coach opened that evaluation session and disappears once a linked portfolio exists. A submitted evaluation URL now shows a stable completion acknowledgement instead of remounting the realtime flow.
- Preserve: an unfinished evaluation remains reachable even if its Coach session was left completed or became old; portfolio creation remains available at any time from `Rapor & Portofolio`; historical evaluation answers and final submissions are not modified; all checks use stable Coder, class, block, session, and evaluation-session IDs.
- Regression risks: the shortcut window uses `block_evaluation_sessions.created_at` as the authoritative Coach-open time and the authenticated evaluation API's server timestamp as the comparison clock. A temporary API failure safely falls back to the evaluation action instead of exposing a stale portfolio shortcut.
- Verification: read-only production inspection confirmed the affected account had one internally consistent completed evaluation with eight final answers and no missing lesson relation; no data was changed. Behavioral tests cover an unfinished in-progress evaluation, an unfinished old completed evaluation, before/at/after shortcut-window boundaries, an already-started portfolio, invalid time data, and a stale submitted session. The full suite passed 43 files and 213 tests; `npx tsc --noEmit`, `git diff --check`, and a safe-dummy-env production build passed with 109 generated routes. Production was fast-forwarded and deployed atomically at SHA `534229833a624500512f7add3e215b0fb70c79f7`; PM2 `lms` was online from that release with zero restarts, `/` and `/login` returned `200`, signed-out `/coder/dashboard` redirected to `/login`, and the protected evaluation API returned `401`. Smoke requests appended zero bytes to the LMS error log, five releases were retained, and `8bba6529a984027bb200281afdda3fd037ae6130` remained the rollback target. A post-deploy read-only evaluation-state probe resolved the historical submitted session to `HIDDEN`, matching the new dashboard rule. No production record was inserted, updated, or deleted. Authenticated responsive browser click-through remains manual because the available in-app Browser timed out on both `Page.navigate` and `Runtime.evaluate` against the production tab.

## INTERNAL-20260820-04 Portfolio workspace used a missing class-block order column

- Symptom: opening `Rapor & Portofolio`, creating a new portfolio, or editing an existing portfolio produced a server error for Coders with at least one class enrollment.
- Root cause: `getCoderPortfolioWorkspace` ordered the `class_blocks` query by `order_index`, but production `class_blocks` has no such column. Production logs confirmed Postgres error `42703`; the ordering metadata belongs to the related `blocks.order_index` column.
- Changed: `src/lib/coderPortfolioServer.ts`, `src/lib/coderPortfolio.ts`, focused behavioral tests, and this documentation. No database migration, production record, storage object, or environment setting is changed.
- Fix: request `blocks.order_index` through the existing block relation, remove the invalid `class_blocks.order_index` order clause, and sort the mapped blocks deterministically in application code. The mapper tolerates empty enrollments, nullable relations, relation arrays, duplicated historical enrollments, and both Weekly and Ekskul histories.
- Preserve: class and block IDs remain authoritative; portfolio ownership, origin, approval snapshots, images, and public links are unchanged. Coder pages without enrollment still render their established empty state, while existing project order remains based on each portfolio's `updated_at`.
- Regression risks: block ordering depends on the existing `blocks.order_index` curriculum metadata. Equal order values use block name and immutable ID as deterministic tie-breakers.
- Verification: the exact corrected relation query passed read-only against production. The population probe covered 198 Coders: 81 without enrollment, 115 with one enrollment, and 2 with multiple enrollments; all 119 enrollment relations and 121 class-block relations were valid. Focused workspace and portfolio behavior tests passed 2 files and 16 tests; the full suite passed 44 files and 216 tests; `npx tsc --noEmit`, `git diff --check`, and a safe-dummy-env production build passed with 109 generated routes. Production release `f54b587ca54461e87c1497f2ab7d32a488203b84` was deployed atomically with rollback target `2630d8f460263dc8814ce63f6f5d7f47cf8fb0d9`: PM2 was online with zero restarts from the exact release directory; `/` and `/login` returned `200`; signed-out Coder portfolio, Coach, and Admin pages redirected to `/login`; portfolio APIs returned `401`; and the PM2 error-log byte size did not change after smoke requests, confirming no new `42703` or portfolio error. Authenticated browser click-through remains a manual check because the available browser session could not reach the LMS domain; no production data was mutated.
## BUG-CCR-20260821-01 CCR assignment reverted after refresh

- Report: Admin issue report `Assign CCR Tidak Berubah` on `Assign ID CCR`; an existing parent changed from `CCR1004` to `CCR079`, but the old value returned after refreshing the page.
- Root cause: the assignment DAO treated an existing `ccr_numbers` row as immutable and returned it before applying the requested code. The assignment page therefore reported the old row as successful even though no update was written.
- Changed: `src/lib/dao/invoicesDao.ts`, `src/app/api/ccr/route.ts`, `src/app/api/ccr/[id]/route.ts`, `src/lib/__tests__/ccrAssignment.spec.ts`, and this documentation. No migration or production data was changed.
- Fix: normalize CCR input, update the existing parent row in the assignment flow, recalculate `ccr_sequence`, and keep the uniqueness guard that rejects a code already owned by another parent. The edit endpoint uses the same input normalization.
- Preserve: CCR record IDs remain stable so existing invoices keep their foreign-key relationship; codes owned by another parent cannot be overwritten; new-parent assignment and the list-page edit flow remain supported.
- Regression risks: changing a CCR code does not rewrite historical `invoice_number` strings, which are immutable invoice identifiers; the related invoice records still point to the same CCR row and display its current code.
- Verification: focused CCR assignment tests passed (2 tests), including update persistence and duplicate-owner rejection; the full suite passed 45 files and 218 tests; `npx tsc --noEmit`, `git diff --check`, and a safe-dummy-env production build passed with 109 generated routes. Production was fast-forwarded and pushed at SHA `e6a3ccb0659362a80eabc3eacb904a87e83b8ec0` and deployed atomically with `f1dd28903f799c9b1b38d95fc4a99b84f93330f5` as the rollback target. `/` and `/login` returned `200`, signed-out `/admin/coders/assign-ccr` returned `307`, PM2 `lms` was online with zero restarts, and the recent LMS log scan contained no `uncaught`, `fatal`, or `exception` entries. No production data was mutated.
## INTERNAL-20260821-02 Public portfolio growth panel used decorative labels

- Symptom: the public Coder portfolio growth panel looked unfinished and showed generic labels such as `Curious`, `Focused`, and `Collaborative` instead of explaining the Coder's actual work.
- Root cause: the reference-inspired illustration was implemented with static decorative copy and a generic growth message, while the public page already had approved project, program, school, level, skill, reflection, and next-step data available.
- Changed: `src/components/portfolio/PublicPortfolioExperience.tsx`, `src/lib/publicPortfolioExperience.ts`, `src/lib/__tests__/publicPortfolioExperience.behavior.spec.ts`, and this documentation. No migration, database row, storage object, or environment setting changed.
- Fix: replace decorative labels with the latest approved project, real program, level and consented school badges, top practiced skills, and project/reflection counts. The growth detail list now derives its labels and copy from the approved snapshot; missing optional data is omitted or shown as an explicit empty state rather than invented.
- Preserve: the public page still reads only approved `published_snapshot` records, keeps existing project fields and stable IDs, and does not expose draft or unapproved edits. Existing portfolio ownership and review boundaries remain unchanged.
- Regression risks: an empty portfolio intentionally shows fewer badges; level and school are only rendered when present and school visibility is enabled. The latest project remains determined by the existing approved-project ordering.
- Verification: behavior tests cover real project, skill, reflection, program, and empty-state derivation; the full suite passed 46 files and 220 tests; `npx tsc --noEmit`, `git diff --check`, and a safe-dummy-env production build passed with 109 generated routes. In-app browser visual QA rendered the public portfolio with the real demo data, confirmed no horizontal overflow, and recorded no console errors or warnings. Production was fast-forwarded from `cb090a4e8b496fd3779f21c62438c7fc52f1d8b8` to `7875bc232b744d95263e0093f89ed2d83f3d05a6` and deployed atomically; `/root/lms/current` resolves to the exact SHA, PM2 `lms` is online with zero restarts, `/`, `/login`, and the public portfolio return `200`, and the previous release remains available for rollback. The recent log tail is dominated by the pre-existing short-links fallback warning; the scan also shows historical `Bad MAC` and URL deprecation entries, but no new portfolio, fatal, or uncaught error appeared after smoke requests. No production data was mutated.
## INTERNAL-20260821-03: Trial parent report story revision

- **Date / reference:** 2026-08-21 / INTERNAL-20260821-03
- **Symptom:** Parent-facing Trial Class Report story copy did not match the requested five-page journey. Activities were shown as short labels, the potential page exposed internal status text, and the Coach strengths section could include rating labels.
- **Root cause:** The story component reused internal assessment status values for visual presentation and used a generic story order/copy. The default activity fallback also contained labels without explanatory detail.
- **Changed:** Updated `src/app/trial-report/[token]/TrialStoryReport.tsx` while keeping five pages: Trial Class Report opening, Kegiatan Trial with activity details, Hasil Trial with parent descriptions, Coach Insight with manual strengths/recommendations, and Continue the Journey with the recommended-level call to action. Added safe rendering helpers for legacy activity and strength values. Updated `src/lib/services/trialAssessmentContent.ts` so generated strengths are status-free and default activities include details. Added focused content regression coverage.
- **Invariants preserved:** Internal Coach/Admin rubric statuses and scores remain available in internal assessment views. Assessment records, registration/invoice flow, stored IDs, and class data are unchanged. Existing stored report content is sanitized at parent-story render time.
- **Regression risks:** Legacy custom activity text is displayed as a title plus a detail derived from its existing text; fallback details describe the activity and do not claim child performance. Long Coach text remains data-driven and uses the existing story layout constraints.
- **Verification:** Focused trial assessment tests passed (4 tests); the full suite passed 46 files and 221 tests; `npx tsc --noEmit`, `git diff --check`, and a safe-dummy-env production build passed with 109 generated routes. After deployment, `/` and `/login` returned `200`, signed-out `/admin/dashboard` returned `307`, `/trial-report/nonexistent-story-token` returned `200` through the dynamic route, `/root/lms/current` resolved to the exact release SHA, and PM2 `lms` was online with zero restarts. The recent log scan still contains pre-existing `Bad MAC`, external fetch timeout, and duplicate lesson-key messages; no data was mutated by this change.
- **Deployment:** Production was fast-forwarded from `a73ac45a9cb87c069984c82241876f08a9bad288` to `ae73d8e604d65248149cb290ab74d3599740e998` and deployed atomically. Previous release `a73ac45a9cb87c069984c82241876f08a9bad288` remains the rollback target.

## INTERNAL-20260821-04: Generic trial activity descriptions

- **Date / reference:** 2026-08-21 / INTERNAL-20260821-04
- **Symptom:** Parent Trial Class Report showed `memecahkan puzzle coding level 1-10 di Blockly`, even though trial activities are not tied to a Blockly test.
- **Root cause:** The reusable fallback description was written around one specific tool instead of the activity's general learning purpose.
- **Changed:** Replaced the four default activity descriptions with tool-agnostic Indonesian copy. Known legacy activity titles are normalized to the generic descriptions at parent-story render time, so old stored `triedToday` values do not reintroduce the Blockly claim.
- **Invariants preserved:** Activity titles, assessment records, Coach/Admin internal views, invoice flow, and report page count remain unchanged. Unknown custom activity details remain available.
- **Verification:** Focused trial assessment tests, full suite, TypeScript, diff check, and safe-dummy-env production build must be recorded after release.
- **Deployment:** Pending release verification.

## INTERNAL-20260821-05: Trial report story journey copy

- **Date / reference:** 2026-08-21 / INTERNAL-20260821-05
- **Symptom:** The recommended level appeared again on the final story slide, while the final slide did not clearly function as a welcome message for a prospective Coder.
- **Root cause:** The level summary was rendered in the final CTA layout instead of being kept with the Coach recommendation content.
- **Changed:** Kept the level and learning focus in the slide 4 Coach recommendation card, removed the duplicate final-slide level summary, and changed slide 5 into a welcome message using the exact recommended-level journey copy and registration CTA.
- **Invariants preserved:** The story remains five pages, recommendation data is unchanged, and invoice/registration behavior is unchanged.
- **Verification:** Focused story/content tests passed (14 tests); the full suite passed 46 files and 221 tests; `npx tsc --noEmit`, `git diff --check`, and a safe-dummy-env production build passed with 109 generated routes. After deployment, `/` and `/login` returned `200`, signed-out `/admin/dashboard` returned `307`, `/root/lms/current` resolved to the exact release SHA, and PM2 `lms` was online with zero restarts. No database or operational data was mutated.
- **Deployment:** Production was fast-forwarded from `b7a418c04b4c60061dea1735aab25b38f7fe8cb5` to `3e2444690c61487d355ddd8ff60d2f1bb5fb25f0` and deployed atomically. Previous release `b7a418c04b4c60061dea1735aab25b38f7fe8cb5` remains the rollback target.

## INTERNAL-20260821-06: Trial report recommendation highlight

- **Date / reference:** 2026-08-21 / INTERNAL-20260821-06
- **Symptom:** The recommended level was included as one item in the Coach focus list, so it was not visually prominent.
- **Root cause:** Recommendation level and learning-focus copy shared one list without a dedicated presentation element.
- **Changed:** Removed the level from the list, kept the learning focus and supporting recommendations there, and added a centered `Level rekomendasi` highlight below the two Coach cards on slide 4.
- **Invariants preserved:** The five-page story, recommended level data, registration CTA, and report content remain unchanged.
- **Verification:** Focused story/content tests passed (14 tests); the full suite passed 46 files and 221 tests; `npx tsc --noEmit`, `git diff --check`, and a safe-dummy-env production build passed with 109 generated routes. After deployment, `/` and `/login` returned `200`, signed-out `/admin/dashboard` returned `307`, `/root/lms/current` resolved to the exact release SHA, and PM2 `lms` was online with zero restarts. No database or operational data was mutated.
- **Deployment:** Production was fast-forwarded from `3e2444690c61487d355ddd8ff60d2f1bb5fb25f0` to `a216af0621460c81c13581762bfd11b9ee7fa293` and deployed atomically. Previous release `3e2444690c61487d355ddd8ff60d2f1bb5fb25f0` remains the rollback target.

## INTERNAL-20260821-07: Coder issue report action

- **Date / reference:** 2026-08-21 / INTERNAL-20260821-07
- **Symptom:** The Coder floating issue-report action competed with the floating chatbot action.
- **Root cause:** The shared issue-report button was mounted at the Coder layout root with fixed positioning, so it could overlap other fixed actions.
- **Changed:** Added a sidebar placement variant to `IssueReportButton`, moved the Coder report action into the bottom of `CoderSidebar`, and kept the floating placement unchanged for Admin and Coach.
- **Invariants preserved:** Issue report fields, screenshot upload, API submission, role value, Admin/Coach placement, and chatbot behavior remain unchanged.
- **Verification:** Focused issue-report placement test passed (7 tests); the full suite passed 46 files and 222 tests; `npx tsc --noEmit`, `git diff --check`, and a safe-dummy-env production build passed with 109 generated routes. After deployment, `/` and `/login` returned `200`, signed-out `/admin/dashboard` returned `307`, `/root/lms/current` resolved to the exact release SHA, and PM2 `lms` was online with zero restarts. No database or operational data was mutated.
- **Deployment:** Production was fast-forwarded from `a216af0621460c81c13581762bfd11b9ee7fa293` to `9badb6aa6c4e436d642328a78d122f485c962f6c` and deployed atomically. Previous release `a216af0621460c81c13581762bfd11b9ee7fa293` remains the rollback target.

## INTERNAL-20260821-08: Coder dark mode and report story overlay

- **Date / reference:** 2026-08-21 / INTERNAL-20260821-08
- **Symptom:** Coder dark mode still showed some low-quality light/amber surfaces, especially the Tugas Susulan status and instruction panels and the Materials timeline gradients. On desktop and mobile, the interactive report story could also leave the underlying detailed report visible at the viewport edge.
- **Root cause:** Dark-mode CSS covered the shared Tailwind surface classes but not gradient variants and the page-specific status/upload surfaces. The report story dialog was rendered inside the report page subtree, so its fixed positioning could inherit a containing layout context instead of being an isolated viewport layer.
- **Changed:** `src/app/globals.css`, Coder Tugas Susulan markers in `src/app/(coder)/coder/makeup/page.tsx` and `MakeUpUploadForm.tsx`, `src/app/(coder)/coder/CoderSidebar.tsx`, `src/app/report/[id]/ReportStoryExperience.tsx`, and `src/app/report/ReportStoryExperience.layoutFix.ts`.
- **Fix:** Add scoped dark tokens for Coder navigation, status/instruction/upload cards, text contrast, and Materials gradient surfaces. Render the report story through a `document.body` portal and enforce a full-viewport isolated layer with no horizontal overflow, while retaining body scroll locking and fullscreen behavior.
- **Invariants preserved:** Coder theme remains scoped to `.coder-app-shell`; Light and Auto behavior, Admin/Coach themes, report data, report page content, navigation, and print behavior remain unchanged. No database, payment, WhatsApp, or production data mutation is involved.
- **Regression risks:** Portal rendering depends on the browser DOM after hydration; the story intentionally appears after the client portal is ready. Authenticated visual verification should cover Coder Materials/Tugas Susulan and report story at desktop and mobile viewports.
- **Verification:** Full suite passed `47` files and `224` tests; `npx tsc --noEmit`, `git diff --check`, and safe-env production build passed with `109` generated routes. After deployment, `/` and `/login` returned `200`, signed-out `/admin/dashboard` returned `307`, `/root/lms/current` resolved to the exact release SHA, and PM2 `lms` was online with zero restarts. The error-log scan contained historical WhatsApp `Bad MAC` entries but no new fatal or uncaught application error during the smoke checks. No production data was mutated.
- **Deployment:** Production was fast-forwarded and deployed atomically at SHA `41945a64e1cf8a52dadce918d44ce6a4eede41c8`; previous release `d12223f63e8a494e7783465ad5ea3c8c2d3cf14a` remains the rollback target. Authenticated visual click-through remains the only manual verification item because no logged-in Coder/report session was available in the browser.

## BUG-ED7CC787: Coach lesson extension boundary

- **Date / report:** 2026-08-22 / `BUG-ED7CC787` from Taziasalma (Coach), dashboard Coach.
- **Symptom:** Coach completed attendance before selecting `Perpanjang Lesson`, so a source lesson that still needed catch-up appeared impossible to extend.
- **Root cause:** The extension guard rejected any `COMPLETED` session after the source, without distinguishing the source attendance from the first session of the next lesson. It also did not inspect attendance rows on a next session that was still marked `SCHEDULED`.
- **Changed:** Additive migration `20260822090000_lesson_extension_boundary.sql`, `src/lib/services/lessonExtensionBoundary.ts`, Coach extension API/page, focused behavioral tests, and this documentation. No lesson, attendance, evaluation, invoice, or class row was changed by verification.
- **Fix:** Allow extension from a final lesson part even when the source session is completed or has attendance. Find the first assigned session of the next lesson and reject only when that session has started, is completed, or has any attendance record. The same boundary is enforced in the database transaction so the UI/API cannot bypass it.
- **Preserve:** Source and earlier lesson assignments, attendance, evaluations, and invoices remain untouched. Only future unstarted assignments in the same class are shifted; global templates and other classes are never changed. Cancelled sessions do not become an extension boundary.
- **Regression risks:** A scheduled session whose time has passed is treated as started even if a Coach has not yet completed it. The source lesson remains editable only before the next lesson boundary.
- **Verification:** Boundary behavior tests cover future untouched next lesson (allowed), exact start time (blocked), after start (blocked), next-session attendance (blocked), completed next lesson (blocked), and cancelled sessions (ignored). Full suite passed 48 files and 229 tests; `npx tsc --noEmit`, `git diff --check`, and safe-dummy-env production build passed with 109 generated routes. Production was fast-forwarded from `66add2a4801c573d55302c0c581ac4037fe5535d` to `7ed39d2d8def920eac26466c2807c09967c290a3` and deployed atomically; `/root/lms/current` and `deployed-sha` match the new SHA, PM2 `lms` is online, port 3005 and `https://lms.clev.io/login` returned `200`, and `/root/lms/releases/5c68e3f8fb8245ef1298d03e64a376f46f9ae665` remains the rollback release. The additive migration was then applied in the linked Supabase SQL Editor in one transaction; a read-only function-definition probe confirmed the next-session, attendance, and time guards. No production data rows were changed. Authenticated Coach click-through remains pending.

## INTERNAL-20260822-01: Coder popup dark mode and mobile navigation

- **Date / reference:** 2026-08-22 / `INTERNAL-20260822-01`.
- **Symptom:** Several Coder popup surfaces, including portal-rendered dialogs and legacy inline-style tutorials, stayed light while the Coder shell was in dark mode. The mobile Coder shell still relied on a generic hamburger menu despite having only a few primary destinations.
- **Root cause:** Dark-mode selectors were scoped to `.coder-app-shell`, but portal dialogs render under `document.body`; the Coder sidebar was also hidden on mobile without a dedicated replacement navigation.
- **Changed:** Coder sidebar/header, popup markers across Coder dialogs and chatbot, shared scoped theme CSS, and the focused extension tests/documentation. No database, production row, storage object, or environment setting changed.
- **Fix:** Add a shared `data-coder-modal` theme boundary for portal and legacy inline-style surfaces, and provide a fixed safe-area-aware five-item mobile bottom navigation with Dashboard, Materi, Tugas Susulan, Rapor & Portofolio, and Report Masalah. The generic Coder hamburger is no longer shown on mobile; desktop sidebar and non-Coder mobile navigation remain unchanged.
- **Preserve:** Coder report submission remains available on mobile, route ownership and active-link behavior use the existing stable hrefs, and Admin/Coach themes and shared navigation are unaffected.
- **Regression risks:** Authenticated visual verification is needed for every popup at both themes and for short mobile viewports; the current local browser was signed out and could only verify the login shell. The bottom bar reserves mobile content space and respects the device safe-area inset.
- **Verification:** Full suite passed 48 files and 229 tests; `npx tsc --noEmit`, `git diff --check`, and safe-dummy-env production build passed. Local browser page identity and non-blank login shell passed with no app errors; the deployed release is `5c68e3f8fb8245ef1298d03e64a376f46f9ae665`, PM2 is online, and the public login route returned `200`. Authenticated Coder popup and mobile click-through remain pending because no signed-in Coder session was available; no production data or runtime configuration changed.

## INTERNAL-20260822-02: Coder mobile report card and optional playable link

- **Date / reference:** 2026-08-22 / `INTERNAL-20260822-02`.
- **Symptom:** The Coder mobile bottom navigation used one slot for Bug Report, while a project without a playable link could not be submitted or displayed cleanly.
- **Root cause:** The report action was rendered as a fifth mobile navigation item, and the portfolio validation/database check treated `playable_url` as required outside drafts even though screenshots, description, and reflection are sufficient evidence for review.
- **Changed:** Coder dashboard/sidebar, issue-report trigger styles/tests, portfolio validation/form/Coach review/public gallery, additive migration `20260822130000_optional_portfolio_playable_url.sql`, and this documentation. No portfolio row or production user data is rewritten.
- **Fix:** Keep four primary destinations in the mobile bottom bar and render Bug Report as a dedicated card at the bottom of the mobile dashboard. Empty playable links are accepted through submit, stored as an empty optional value, and omitted from Coach/public action buttons.
- **Preserve:** Desktop sidebar reporting, Admin/Coach reporting, report fields and WhatsApp delivery, source/repository/video links, screenshot limits, approved public snapshots, and existing playable URLs remain unchanged.
- **Regression risks:** The additive constraint migration must be applied before a production Coder can submit a project without a playable link. Authenticated mobile click-through should confirm the card remains reachable below dashboard content and the four nav items remain usable.
- **Verification:** Focused behavioral tests cover the mobile report placement contract, complete portfolios without playable links, safe URL validation, and published snapshots. The full suite passed 48 files and 230 tests; `npx tsc --noEmit`, `git diff --check`, and safe-dummy-env production build passed. Production was fast-forwarded from `2b8597388c531e32f307948de9271e0aed378f6e` to `e5d79651b0268ae78ee79418a6775f5e3c1b3f0c` and deployed atomically; `/root/lms/current` and `deployed-sha` match the new SHA, PM2 `lms` is online, port 3005 and `https://lms.clev.io/login` returned `200`, and `/root/lms/releases/2b8597388c531e32f307948de9271e0aed378f6e` remains the rollback release. The additive playable-link constraint migration was applied in Supabase SQL Editor and a read-only constraint probe confirmed empty playable URLs are accepted for complete submissions. No production rows were changed. Authenticated Coder mobile click-through remains pending.

## BUG-ED7CC787 follow-up: Extended lesson last-part visibility and mobile scouting report scroll

- **Date / reference:** 2026-08-22 / follow-up to `BUG-ED7CC787`.
- **Symptom:** After a Coach had already completed attendance for an extended lesson, the `Perpanjang Lesson` action could still be missing even though the current session was the effective last part. On mobile, a long Coach's Scouting Report on slide 5 could be clipped instead of scrollable.
- **Root cause:** The extension appends a second marker to the existing title (for example, `(Part 4) (Part 5)`), while the schedule parser read the first marker and classified the session as Part 4. The report story's mobile coach layout did not have an explicit full-height touch scroll container.
- **Changed:** Extracted and fixed the lesson part-number resolver, added a behavioral regression test for appended part markers, and added a mobile-only scroll boundary for the Coach's Scouting Report slide. No migration, production row, attendance, or lesson data was changed.
- **Fix:** The last `(Part N)` marker is now authoritative, so an attended Part 5 remains eligible while the next lesson is still untouched; the existing next-lesson boundary and database guard remain unchanged. Mobile slide 5 now gets an explicit touch-friendly vertical scroll area with safe bottom padding.
- **Preserve:** Existing ordinary part titles, marker-less grouped lessons, cancelled-session handling, next-lesson attendance/time guards, global templates, and other report slides remain unchanged.
- **Regression risks:** Authenticated Coach click-through should confirm the action appears on the completed extended session and stays hidden after the next lesson starts. Authenticated mobile report verification should confirm slide 5 scrolls without affecting swipe navigation on other slides.
- **Verification:** Focused behavioral tests cover ordinary, marker-less, and appended-marker lesson titles plus the existing next-lesson boundary matrix. Full suite, TypeScript, diff check, production build, deployed SHA, PM2, route smoke test, and recent error log will be recorded after publication.
## INTERNAL-20260824-01: Coach mobile header controls clipped the greeting

- **Date / reference:** 2026-08-24 / INTERNAL-20260824-01
- **Symptom:** On mobile Coach dashboard, the greeting was clipped near the emoji and the profile controls competed with the available header width.
- **Root cause:** The Coach header did not reserve a flexible, truncating region for the greeting. The profile trigger also kept its desktop chevron spacing on narrow screens, while the Coach layout kept `px-8` on mobile and left the full-width header short of the viewport.
- **Changed:** `src/app/(coach)/coach/layout.tsx`, `src/components/coach/CoachDashboardHeader.tsx`, `src/lib/__tests__/coachMobileHeader.contract.spec.ts`, and this documentation. No migration, database row, or runtime setting changed.
- **Fix:** Give the greeting `min-w-0`, flexible width, and ellipsis; use `px-4 md:px-8` and `min-w-0` on the Coach main container; make the header full-width; keep notification and profile controls shrink-proof; hide only the non-essential profile chevron below the small-screen breakpoint.
- **Invariants preserved:** Notification and profile actions remain available, the full profile dropdown behavior is unchanged, desktop layout is preserved, and the existing MobileNav drawer is untouched.
- **Regression risks:** Very narrow devices may ellipsize a long first name, but the text will remain readable and no primary action will be pushed off-screen. Authenticated visual verification should cover Coach dashboard widths around 320px, 360px, and 390px.
- **Verification:** Focused header contract test passed; the full suite passed 51 files and 234 tests; `npx tsc --noEmit --incremental false`, `git diff --check`, and the safe-dummy-env production build passed with 109 generated routes. No authenticated Coach browser session was available for click-through verification.
- **Deployment:** Production was fast-forwarded from `86b139ac9c4775f006f336529518424b9b5b05af` to `e2108667cf7888a9758f2fa93b0f74888e30419c` and deployed atomically. `/root/lms/current` resolves to the exact release SHA, PM2 `lms` is online with zero restarts, `/` and `/login` returned `200`, signed-out `/admin/dashboard` returned `307`, and the recent error scan contained no new `uncaught`, `fatal`, or `exception` entries. Previous release `86b139ac9c4775f006f336529518424b9b5b05af` remains the rollback target.

## INTERNAL-20260824-02: Coder mobile navigation visible outside phone widths

- **Date / reference:** 2026-08-24 / INTERNAL-20260824-02
- **Symptom:** The Coder bottom navigation intended for phones appeared on desktop or tablet-sized viewports.
- **Root cause:** `.coder-mobile-nav` set `display: grid` unconditionally in shared CSS. That rule could override the component's `md:hidden` utility depending on generated stylesheet order.
- **Changed:** `src/app/globals.css`, `src/lib/__tests__/coderMobileNav.contract.spec.ts`, and this documentation. No data, migration, or runtime setting changed.
- **Fix:** Default the Coder mobile navigation to `display: none` and enable its grid layout only within the explicit `max-width: 767px` phone breakpoint.
- **Invariants preserved:** Phone navigation layout, links, active state, dark-mode colors, and desktop Coder sidebar remain unchanged.
- **Regression risks:** Devices between 768px and 1023px intentionally use the desktop sidebar; authenticated visual verification should cover phone, tablet, and desktop widths.
- **Verification:** The Coder mobile-nav contract test passed as part of the 51-file, 234-test suite; TypeScript, diff check, and safe-dummy-env production build passed. No authenticated Coder browser session was available for click-through verification.
- **Deployment:** Included in production SHA `e2108667cf7888a9758f2fa93b0f74888e30419c`; the release and rollback details are recorded in INTERNAL-20260824-01 above.

## INTERNAL-20260824-03: Branded PWA offline fallback

- **Date / reference:** 2026-08-24 / INTERNAL-20260824-03
- **Symptom:** When an installed LMS PWA was opened without internet, navigation could show the browser's generic offline/error page instead of a Clevio experience.
- **Root cause:** The service worker was registered only by the notification control and had no navigation fetch fallback or cached offline document. The PWA shell also did not register it from the root layout experience.
- **Changed:** `public/sw.js`, `public/offline.html`, `src/components/pwa/PwaStartupSplash.tsx`, `src/lib/__tests__/pwaOfflineFallback.contract.spec.ts`, and this documentation. No database, user data, notification payload, or runtime secret changed.
- **Fix:** Register the service worker from the root PWA splash, cache the branded offline document and Clevio assets, and serve that document for failed navigation requests. The offline page provides an accessible retry action, a home link, responsive layout, and Clevio branding without external dependencies.
- **Invariants preserved:** Existing push notification and notification-click behavior remains unchanged, online navigation still uses the network response, and dynamic LMS actions remain unavailable until connectivity returns.
- **Regression risks:** A first-time install still needs one successful online visit to install and populate the service-worker cache. The offline page's home link may remain unavailable while offline, so the retry action is the primary recovery path.
- **Verification:** Focused PWA contract test passed; the full suite passed 52 files and 236 tests; `npx tsc --noEmit --incremental false`, `git diff --check`, and the safe-dummy-env production build passed with 109 generated routes. The deployed release contains `public/offline.html` and `public/sw.js`; `/` and `/login` returned `200`, signed-out `/admin/dashboard` returned `307`, PM2 `lms` is online with zero restarts, and the recent error scan contained no new `uncaught`, `fatal`, or `exception` entries. No authenticated offline device click-through was available in this session.
- **Deployment:** Production was fast-forwarded from `4c40432656a3f33465e7b59b7570cc213a8141a5` to `a48066db0639d77bf8cfce42c10a39390d1ca0da` and deployed atomically. `/root/lms/current` resolves to the exact release SHA; previous release `4c40432656a3f33465e7b59b7570cc213a8141a5` remains the rollback target.

## INTERNAL-20260824-04: Mobile shell and Learning Journey follow-up

- **Date / reference:** 2026-08-24 / INTERNAL-20260824-04
- **Symptom:** The Coach mobile header still ended before the right viewport edge after the first spacing fix. The offline PWA's wide bitmap logo was difficult to read on a phone. In Coder dark mode, the Learning Journey title, current block name, and navigation hint had insufficient contrast, while its horizontal map was awkward on a narrow screen.
- **Root cause:** `w-full` measured the Coach main container's padded content box, and a negative left margin moved that fixed width without restoring the missing right-side width. The offline brand depended on a reduced horizontal logo bitmap. Learning Journey reused light-mode sky text on a dark portal and had only an absolute horizontal desktop map.
- **Changed:** Coach layout/header, PWA cache/offline brand, Coder Journey modal/map and scoped dark-mode styles, focused contracts, and this documentation. No class, lesson, progress, account, or database record changed.
- **Fix:** Keep the Coach header outside the padded page-content wrapper so it owns the full mobile viewport. Render the offline brand with the official Clevio mark plus crisp HTML brand text and bump the service-worker cache version. Add explicit high-contrast Journey dark-mode tokens and a phone-only vertical route whose first block sits at the bottom and subsequent blocks rise upward; desktop keeps the existing horizontal map.
- **Invariants preserved:** Coach desktop sidebar/header behavior, page content spacing, PWA push handling, online navigation, Journey block order/status/progress values, and the desktop Journey interaction remain unchanged.
- **Regression risks:** Long class names must wrap within mobile Journey cards, and devices with many blocks require vertical modal scrolling. A previously installed PWA needs the updated service worker to activate before the refreshed offline brand appears.
- **Verification:** Focused contracts passed 3 files and 6 tests; the full suite passed 52 files and 237 tests. `npx tsc --noEmit --incremental false`, `git diff --check`, and the safe-dummy-env production build passed with 109 generated routes. The offline page was rendered at 360x800 with no horizontal overflow, the official mark loaded at its natural resolution, the retry control worked, and the browser console was clean. In production, `/root/lms/current` resolved to `219f36a425d5d3a3743d83f03a52aa5fb70077d7`; PM2 `lms` was online with zero restarts; `/`, `/login`, `/offline.html`, `/sw.js`, and `/pwa-icon-192.png` returned `200`; signed-out `/admin/dashboard` returned `307`; and the recent fatal error scan returned zero matches. Authenticated Coach/Journey visual verification on a physical device remains a post-deploy observation because no authenticated browser session was available during implementation.
- **Deployment:** Production source was fast-forwarded from `00dc41937fc6444859796fed35708137e208bb12` to `219f36a425d5d3a3743d83f03a52aa5fb70077d7` and deployed atomically. The previous release `00dc41937fc6444859796fed35708137e208bb12` remains the rollback target.

## INTERNAL-20260824-05: Coach bottom navigation and public report boundaries

- **Date / reference:** 2026-08-24 / INTERNAL-20260824-05
- **Symptom:** The Coach top bar and Review Portofolio page still left visible gutters on phones. Coach navigation depended on a hamburger even though the five primary destinations fit a bottom navigation. Learning Journey used an unfinished mobile layout and its route could appear broken or dashed in dark mode. Trial and weekly report stories could overlap the logo or clip their content on short laptop screens, while the Coach report review retained its desktop split layout on narrow screens. Opening an Admin-only trial-report preview while signed out rendered the framework server-error screen instead of the LMS not-found page.
- **Root cause:** A late global mobile rule added horizontal padding directly to `.coach-main`, overriding the shell-level width fix and stacking with page padding. `CoachMobileNav` was not mounted and was missing the portfolio destination. Journey's mobile list was reversed after render and the desktop background SVG explicitly used `strokeDasharray`. The shared report story layout had no height-aware typography or bounded content region for short landscape viewports, and Coach report review always used a horizontal full-height shell. The trial preview called `getSessionOrThrow()`, whose anonymous exception bypassed the route's `notFound()` boundary.
- **Changed:** Coach layout, header, five-item mobile navigation, shared responsive styles, Coach portfolio and report-review shells, Coder Journey modal/map styling, shared trial/weekly report-story responsive rules, trial-report route guard, focused contracts, and this documentation. No class, lesson, report, assessment, account, or database record changed.
- **Fix:** Give `.coach-main` a true edge-to-edge phone width, keep page padding in its inner content wrapper, mount an Instagram-style five-destination bottom nav, and move the issue-report trigger above it. Expand the portfolio background across the shell padding. Render Journey mobile as a continuous vertical timeline that begins with the active milestone and continues to locked/final stages, while all SVG route strokes remain continuous in light and dark modes. Add shared viewport-height breakpoints so trial and weekly story typography scales against both width and height and dense slides scroll inside their own bounded stage instead of clipping or exposing the report underneath. Stack Coach report-review panels on narrow screens and restore the existing split view on desktop. For `?preview=1`, resolve the session before querying the token and call `notFound()` unless the session belongs to an Admin.
- **Invariants preserved:** Desktop Coach sidebar/header behavior and all five existing route destinations remain unchanged; bottom navigation is phone-only. Journey block IDs, order, status, progress values, and desktop horizontal scrolling remain unchanged. Report copy, scores, lesson data, sharing, PDF/print behavior, and published public links remain unchanged; only presentation and overflow handling changed. Published public trial reports remain accessible without login, while only Admins can see preview statuses. Portfolio review mutations and ownership filters are unchanged.
- **Regression risks:** Long mobile menu labels and Journey block names must remain readable at 320px width. Fixed bottom navigation requires sufficient content and floating-action clearance. Report slides with unusually long dynamic text must remain usable at both 641px width and 700-860px viewport heights. Trial-report preview must not hide published non-preview links or disclose preview-token existence to anonymous visitors.
- **Verification:** Focused responsive/access contracts passed (4 files, 7 tests). The full Vitest suite passed (54 files, 240 tests), `npx tsc --noEmit --incremental false` passed, `git diff --check` passed, and the Next.js production build completed. Browser render checks covered Trial Report at 390x844, 768x1024, 1366x700, and desktop dimensions, including the long Coach Insight slide; Weekly Report with 15 lessons was checked at phone and short-laptop dimensions. Titles did not intersect the logo, horizontal document overflow was absent, bottom navigation remained visible, and overflow stayed inside the story stage. Signed-out Coach routes continued to redirect, and the supplied Admin preview URL rendered the LMS not-found boundary without adding an application-error log entry. PM2 remained online with zero restarts after deployment.
- **Deployment:** Production source was fast-forwarded from `cce3e30540646b228875f7d9b3b0ab33de4c8fd1` to `b2afb59ca6c86c93d22310225c353e2d624369d8` and deployed atomically. The previous release `cce3e30540646b228875f7d9b3b0ab33de4c8fd1` remains the rollback target.

## INTERNAL-20260824-06: Coder Learning Journey mobile contrast and complete block list

- **Date / reference:** 2026-08-24 / `INTERNAL-20260824-06`.
- **Symptom:** In the mobile dark Journey view, some headings and supporting labels inherited light-mode sky/slate colors and were difficult to read. The mobile list also rendered only the current block and later milestones, so completed blocks were missing from the journey.
- **Root cause:** The Journey modal is rendered through a portal and its page-specific text classes were not covered by one explicit light/dark token set. The mobile projection used `blocks.slice(firstOpenIndex)`, which intentionally dropped earlier completed blocks instead of preserving them in the scrollable story.
- **Changed:** `src/app/(coder)/coder/dashboard/JourneyMap.tsx`, `src/app/(coder)/coder/dashboard/JourneyModal.tsx`, `src/app/globals.css`, `src/lib/__tests__/coderTheme.spec.ts`, and this documentation. No database, class, lesson, progress, account, or runtime setting changed.
- **Fix:** Add Journey-specific panel, heading, body, muted, card, and route tokens for both themes; apply them to the portal title, mobile intro, milestone cards, dates, progress labels, locked states, and navigation hint. Keep the active/current milestone first, append all earlier completed blocks afterward, and show the mobile exploration hint inside the phone layout. Desktop route strokes remain continuous and desktop horizontal behavior is unchanged.
- **Invariants preserved:** Block IDs, source order, status, progress values, dates, desktop map, horizontal navigation, and course data are unchanged. The mobile view now presents every block without mutating or deleting source data.
- **Regression risks:** A course with many blocks will require vertical scrolling inside the modal, and long block names still depend on normal wrapping at very narrow widths. Authenticated physical-device verification remains useful for device-specific font rendering.
- **Verification:** Focused Journey contract test passed (3 tests); full Vitest suite passed (54 files, 240 tests); `npx tsc --noEmit --incremental false`, `git diff --check`, and the safe-dummy-env production build passed with 109 generated routes. Local Browser QA used the temporary non-production fixture at 428x932 and 1440x900: dark and light text remained readable, the active Block 3 appeared first, all 5/5 blocks were present in the mobile DOM, and the mobile hint rendered without horizontal overflow. The temporary QA route was removed before commit. No production data or runtime configuration was changed.
- **Deployment:** Production was fast-forwarded from `ee05fb3ffec50d11e2ca1ba8fab94d3838aac815` to `2e323234b7af499c7884f2abed6da95cae98fb2d` and deployed atomically. `/root/lms/current` resolves to the exact new SHA; PM2 `lms` is online with zero restarts, `/login` returned `200`, and signed-out `/coder/dashboard` returned `307`. The PM2 tail contained existing `short_links` fallback and unrelated session/report probe messages, but no Journey build/runtime error. The previous release `ee05fb3ffec50d11e2ca1ba8fab94d3838aac815` remains the rollback target.

## INTERNAL-20260824-08: Coder Learning Journey mobile complete block visibility

- **Date / reference:** 2026-08-24 / `INTERNAL-20260824-08`.
- **Symptom:** The mobile Journey matched the compact reference layout but completed block names were hidden until the summary was tapped, which made the Journey look like it contained only the active and upcoming blocks.
- **Root cause:** Completed milestones were intentionally collapsed by default after being converted from full cards to a compact summary.
- **Changed:** `src/app/(coder)/coder/dashboard/JourneyMap.tsx`, `src/lib/__tests__/coderTheme.spec.ts`, and this documentation. No database, class, lesson, progress, account, or runtime setting changed.
- **Fix:** Keep the compact completed-block summary expanded by default so every completed block name is visible as a chip, while retaining the toggle for users who want to collapse that compact list. Active, upcoming, and Final Quest cards keep the supplied reference style.
- **Invariants preserved:** All block IDs, source order, status, progress values, dates, desktop layout, and Journey interactions remain unchanged. No block is deleted or hidden from the default mobile view.
- **Regression risks:** A class with many completed blocks will add a compact wrapped chip row, but it will not create the large repeated cards that caused the original layout problem.
- **Verification:** Focused Journey contract passed (3 tests); full Vitest suite passed (54 files, 240 tests); `npx tsc --noEmit --incremental false`, `git diff --check`, and safe-dummy-env production build passed with 109 generated routes. Local Browser QA at 390x844 confirmed both completed chips and all three primary cards are visible without horizontal overflow. No production data or runtime configuration changed.

## INTERNAL-20260824-09: Coder Learning Journey mobile vertical scroll

- **Date / reference:** 2026-08-24 / `INTERNAL-20260824-09`.
- **Symptom:** The mobile Learning Journey still differed from the supplied phone reference. Completed blocks were separated into a compact toggle area, later blocks appeared out of the expected timeline flow, and the scroll hint did not communicate vertical scrolling.
- **Root cause:** The mobile projection filtered completed blocks out of the main list and rendered them as a separate expandable summary. The modal body did not expose a dedicated mobile scroll region, and the hint used horizontal navigation language and icons.
- **Changed:** `src/app/(coder)/coder/dashboard/JourneyMap.tsx`, `src/app/(coder)/coder/dashboard/JourneyModal.tsx`, `src/app/globals.css`, `src/lib/__tests__/coderTheme.spec.ts`, and this documentation. No database, class, lesson, progress, account, or runtime setting changed.
- **Fix:** Render every source block in its original order inside one vertical mobile timeline. Completed blocks use compact cards, while the current block keeps the featured progress treatment and upcoming/final blocks keep their locked states. Add an internal overscroll-safe mobile region, phone-sized spacing and scrollbar treatment, responsive card sizing, and an explicit “Geser ke atas / bawah” indicator. Desktop horizontal Journey behavior is unchanged.
- **Invariants preserved:** Block IDs, source order, status, progress values, dates, course data, desktop map, desktop horizontal navigation, and light/dark theme scoping remain unchanged.
- **Regression risks:** Courses with many blocks now produce a longer vertical mobile list by design. Very narrow devices still depend on normal title wrapping; authenticated physical-device verification remains useful for font rendering and touch scrolling.
- **Verification:** Focused Journey contract passed (3 tests); full Vitest suite passed (54 files, 240 tests); `npx tsc --noEmit --incremental false` passed; `git diff --check` passed; safe-dummy-env production build passed with 109 generated routes. Local fixture route returned `200` and server-rendered all five block names at the mobile route; the fixture was removed before commit. No production data or runtime configuration changed.

## INTERNAL-20260824-07: Coder Learning Journey mobile reference layout and progress contrast

- **Date / reference:** 2026-08-24 / `INTERNAL-20260824-07`.
- **Symptom:** The mobile Journey still showed completed blocks as large cards after the active and upcoming blocks, unlike the supplied phone reference. The `77%` current-block label could become dark text on the dark card, while the completion percentage badge needed a different contrast treatment.
- **Root cause:** The mobile projection rendered every non-completed and completed block with the same card treatment and rotated completed blocks to the end. One dark-mode percentage rule was applied to both the light completion badge and the dark current-card label.
- **Changed:** `src/app/(coder)/coder/dashboard/JourneyMap.tsx`, `src/app/globals.css`, `src/lib/__tests__/coderTheme.spec.ts`, and this documentation. No database, class, lesson, progress, account, or runtime setting changed.
- **Fix:** Render completed milestones as a compact expandable summary, keep the active and upcoming milestones as the primary vertical cards, and preserve all completed block names behind the summary control. Split the percentage tokens so the summary badge uses dark text on its light badge while the current-card percentage uses high-contrast cyan in dark mode.
- **Invariants preserved:** Block IDs, source order, status, progress values, dates, desktop map, desktop horizontal navigation, and course data are unchanged. No completed block is deleted or omitted from the accessible mobile view; it is only collapsed by default for the reference layout.
- **Regression risks:** Courses with many completed blocks may produce a taller expanded summary. Very narrow devices still depend on normal title wrapping, and authenticated physical-device verification remains useful for font rendering.
- **Verification:** Focused Journey contract test passed (3 tests); full Vitest suite passed (54 files, 240 tests); `npx tsc --noEmit --incremental false`, `git diff --check`, and safe-dummy-env production build passed with 109 generated routes. Local Browser QA at 390x844 confirmed the reference order, three primary cards, no horizontal overflow, readable light/dark percentages (`40%` and `77%`), and expandable completed names. The temporary QA route was removed before commit. No production data or runtime configuration changed.

## INTERNAL-20260824-10: Role PWA onboarding and actionable notifications

- **Date / reference:** 2026-08-24 / `INTERNAL-20260824-10`.
- **Symptom:** Coach and Coder did not receive a native-style mobile onboarding path for installing the LMS PWA and enabling notifications. Bell entries were not consistently actionable, and Coder had no deduplicated H-1 or one-hour class reminder.
- **Root cause:** PWA setup was exposed only through an Admin-oriented control, notification rows had no action/category/priority/deduplication metadata, and role events were persisted without a consistent push-delivery contract.
- **Changed:** Coach/Coder dashboard onboarding, notification dropdown, Admin broadcast and operational event routes, Coach/Coder assessment and portfolio event routes, class/session reminder services, notification DAO and migration `20260824150000_role_notification_metadata.sql`, focused contracts, and this documentation. No class, enrollment, assessment, invoice, attendance, report, or account data was changed.
- **Fix:** Add a mobile-only install experience with iOS Add-to-Home-Screen guidance and an installed-app notification prompt. Persist safe internal action URLs plus category, priority, and per-user deduplication keys. Deliver actionable push notifications for role-relevant events, including Coder H-1 summaries and Coach/Coder one-hour class reminders, and expose the same destination from the website bell. Database uniqueness closes overlapping-cron races without duplicate reminders.
- **Invariants preserved:** Desktop dashboards do not show the mobile install prompt; dismissed prompts are locally suppressed for seven days; denied notification permission is not repeatedly requested; push remains best-effort while the website notification is authoritative; Coach one-hour reminders still honor `notif_session_reminder`; existing WhatsApp parent reminders remain separate; all action links are internal LMS paths.
- **Regression risks:** Delivery depends on a valid browser push subscription and a periodic class-reminder job. iOS installation still requires the system Share menu. The additive notification migration must be applied before the new runtime is deployed.
- **Verification:** Focused role-notification contracts passed (35 tests), the full Vitest suite passed (55 files, 245 tests), `npx tsc --noEmit` passed, `git diff --check` passed, and a clean safe-dummy-env production build passed with 109 generated routes. Production deployment and authenticated physical-device verification are recorded after release cutover.

## INTERNAL-20260824-11: Production candidate omitted build dependencies

- **Date / reference:** 2026-08-24 / `INTERNAL-20260824-11`.
- **Symptom:** The atomic deploy correctly rejected the PWA notification candidate before cutover because the fresh release could not resolve `autoprefixer` during its production build.
- **Root cause:** The VPS exports a production npm environment. Plain `npm ci` therefore omitted dev dependencies even though Next.js/PostCSS requires them while compiling a release candidate.
- **Changed:** `scripts/deploy-production.sh`, its focused contract test, and this documentation. No application behavior, runtime environment value, database row, or active PM2 process was changed by the failed candidate.
- **Fix:** Install the lockfile with `npm ci --include=dev` inside the isolated release build. The started application still uses the existing `NODE_ENV=production` command and only the release candidate receives build tooling.
- **Invariants preserved:** The deployment lock, exact `origin/production` SHA check, pre-switch candidate health check, atomic symlink cutover, shared runtime directories, release retention, and automatic rollback remain unchanged.
- **Regression risks:** Candidate installation uses additional disk and network during build, but release cleanup and retention remain authoritative. Lockfile integrity is unchanged.
- **Verification:** Deployment contract explicitly requires dev dependencies before build. Full tests, TypeScript, diff check, local production build, rejected-candidate isolation, and a second atomic deploy are verified before completion.
