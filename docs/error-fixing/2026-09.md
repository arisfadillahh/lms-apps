# Error-Fixing Log - September 2026

## INTERNAL-20260901-01: Portfolio CTA glow shape

- **Date / reference:** 2026-09-01 / `INTERNAL-20260901-01`.
- **Symptom:** The green CTA shadow on the public Portfolio Universe looked like a rigid rectangular patch and appeared visually cut off on mobile.
- **Root cause:** Green CTA buttons used large Tailwind `box-shadow` utilities alongside the animated sheen layer; the resulting blur did not consistently follow the button silhouette at narrow viewport edges.
- **Changed:** `src/components/portfolio/PublicPortfolioExperience.tsx`, `src/components/portfolio/PublicPortfolioExperience.module.css`, `src/lib/__tests__/publicPortfolioResponsive.contract.spec.ts`, and this log.
- **Fix:** Replaced the hard green box shadows on green Portfolio CTAs with shape-aware `drop-shadow` filters. Hover/focus keeps a slightly stronger glow, and reduced-motion removes the filter. The button radius, layout, colors, copy, and sheen behavior remain unchanged.
- **Invariants preserved:** Portfolio data, navigation, section structure, modal scroll lock, responsive bounds, and all non-Portfolio pages remain unchanged. No database, API, or production data was modified.
- **Regression risks:** Future CTA filter or transform utilities should be checked together because CSS filters and transforms can affect compositing and perceived shadow edges.
- **Verification:** Portfolio responsive contract passed 5 tests, clean-source `npx tsc --noEmit` passed, and `git diff --check` passed. Next webpack compiled successfully before the pre-existing unrelated Coach Ekskul `PageProps.params` type-generation error. Production mobile QA at 390x844 confirmed the CTA uses the shape-aware `drop-shadow`, keeps its expected bounds, and produces no horizontal overflow. The release smoke check is recorded after deployment.
- **Deployment:** Release `5413721ed807815fd718e6921b3bc5ab823ab5fd` was deployed through `/root/lms/deploy-production.sh`; rollback target is `9305debec72725adfd1a3d8852f750f211b8911e`. The deployed release directory and Git SHA matched, PM2 `lms` and `clevio-wa` were online, and the Portfolio and Login routes returned `200`.

## INTERNAL-20260901-02: Portfolio mobile single-scroll context

- **Date / reference:** 2026-09-01 / `INTERNAL-20260901-02`.
- **Symptom:** After entering the Portfolio Universe on mobile, scrolling could show a duplicate scrollbar or feel like both the page and its document shell were scrollable.
- **Root cause:** The global `MobileScrollFix` forced `html` and `body` into its body-scroll mode on every mobile route, including Portfolio. Portfolio therefore inherited global overflow rules even though its public page needs one normal document scroll context.
- **Changed:** `src/components/layout/MobileScrollFix.tsx`, `src/components/portfolio/PublicPortfolioExperience.tsx`, `src/components/portfolio/PublicPortfolioExperience.module.css`, `src/lib/__tests__/publicPortfolioResponsive.contract.spec.ts`, and this log.
- **Fix:** Portfolio marks its page mode before the global mobile effect runs. The mobile helper excludes that page from its body-scroll overrides and re-applies normal defaults on route exit; the existing intro/modal lock remains explicit and modal content remains independently scrollable. The CTA glow was also reduced to a smaller shape-aware drop-shadow so it cannot read as a clipped rectangular patch.
- **Invariants preserved:** Other Admin, Coach, and Coder mobile scroll behavior remains unchanged. Portfolio layout, color palette, content, published data, modal behavior, and database/API state are unchanged.
- **Regression risks:** Global mobile overflow rules must continue to exclude `data-portfolio-page="true"`; future public pages should use their own scroll mode marker if they need different document behavior.
- **Verification:** Portfolio responsive contract passed 5 tests, clean-source `npx tsc --noEmit` passed, and `git diff --check` passed. Production touch QA at 390x844 will verify one `HTML` scrolling context, one document width, and CTA filter after the release is deployed.
- **Deployment:** Release `13d84d61282119b5d610b5573d6d21987274db0f` was deployed through `/root/lms/deploy-production.sh`; rollback target is `b6009e64fa46bc7d0761a2608535b55b0b0116d9`. Production touch QA at 390x844 confirmed the Portfolio marker is active, CTA uses the reduced shape-aware glow, `HTML` is the only scrolling element, `body` is not forced into a second scrollbar, document width is 390/390, and the page scrolls normally after Enter Portfolio.

## INTERNAL-20260901-03: Portfolio desktop duplicate scrollbar

- **Date / reference:** 2026-09-01 / `INTERNAL-20260901-03`.
- **Symptom:** The public Portfolio page still displayed two vertical scrollbars on desktop after entering the experience.
- **Root cause:** The browser treated both `HTML` and `BODY` as scrollable because the global shell left body overflow in an auto context and its scroll height exceeded the viewport by the shell gutter.
- **Changed:** `src/components/portfolio/PublicPortfolioExperience.module.css`, `src/lib/__tests__/publicPortfolioResponsive.contract.spec.ts`, and this log.
- **Fix:** While the Portfolio page marker is active, `HTML` owns vertical scrolling and `BODY` uses visible vertical overflow. The existing mobile helper exclusion remains in place, so desktop and mobile share the same single-scroll contract without changing content layout.
- **Invariants preserved:** Other role dashboards keep their existing mobile scroll behavior. Portfolio content, layout, colors, animations, modal lock, and database/API state remain unchanged.
- **Verification:** Browser DOM audit found both `HTML` and `BODY` scrollable before the fix. After the fix, the production desktop audit found only `HTML` scrollable (`BODY` height equals its client height). Focused responsive contract tests (5/5), clean-source TypeScript, and `git diff --check` passed; the mobile single-scroll contract remains covered by the existing production touch QA.
- **Deployment:** Release `47141bf4af569e9881aacc0e630389cdb1383ae6` was deployed through `/root/lms/deploy-production.sh`; rollback target is `06a6f6eb54fa1b5616728bd159291d37966b8fff`. PM2 services remained online and the public Portfolio route returned HTTP 200.

## INTERNAL-20260901-04: Portfolio body overflow axis normalization

- **Date / reference:** 2026-09-01 / `INTERNAL-20260901-04`.
- **Symptom:** Two vertical scrollbar tracks were still visible on desktop at the user's browser size and zoom after entering the public Portfolio experience.
- **Root cause:** The page-specific rule only set `body` vertical overflow to `visible`, while the global stylesheet kept horizontal overflow `hidden`. CSS overflow-axis normalization converts `visible` on one axis to `auto` when the other axis is `hidden`, so both `HTML` and `BODY` remained independently scrollable when body content exceeded its client height.
- **Changed:** `src/components/portfolio/PublicPortfolioExperience.module.css`, `src/lib/__tests__/publicPortfolioResponsive.contract.spec.ts`, and this log.
- **Fix:** The Portfolio page now restores both body overflow axes to `visible`; `HTML` remains the sole vertical scrolling element. Horizontal clipping remains owned by the Portfolio root wrapper, so the layout does not gain horizontal overflow.
- **Invariants preserved:** Portfolio layout, motion, intro lock, modal lock, data, API state, and all non-Portfolio role pages remain unchanged. No database changes are required.
- **Regression risks:** Any future global overflow declaration must not turn Portfolio body overflow back into an independent scrolling context.
- **Verification:** Portfolio responsive contract passed 5/5 tests, clean-source `npx tsc --noEmit` passed, and `git diff --check` passed. Production browser QA at 1873x963 confirmed body overflow is `visible` on both axes, the only vertical scrollable element is `HTML`, document width equals viewport width, a scroll interaction moved the document from 0 to 688 px, and no console warnings or errors were emitted.
- **Deployment:** Release `5165db521425a035b7d185877ab90c193eaee8e6` was deployed through `/root/lms/deploy-production.sh`; rollback target is `99bb044bf0d553baf6e5812a7983c196eba69eb3`. PM2 and public route smoke checks are recorded after the final documentation release.

## BUG-2D2206B: Permanent class reschedule collapsed lessons onto one date

- **Date / reference:** 2026-09-01 / `BUG-2D2206B`.
- **Symptom:** On the Admin class detail page, permanent rescheduling could place several upcoming Creator lessons on the same date. The affected Coach Naufal Saturday classes showed multiple lessons competing for one session date.
- **Root cause:** The permanent schedule endpoint snapped every future session independently to the requested weekday. Sessions that were already in the same calendar week therefore collapsed onto one timestamp. Equal timestamps had no curriculum-order tie breaker, and weekday calculation used UTC instead of the class's WIB calendar date.
- **Changed:** `src/app/api/admin/classes/[id]/schedule/route.ts`, `src/app/(admin)/admin/classes/[id]/ChangeClassScheduleModal.tsx`, `src/lib/services/classScheduleReflow.ts`, `src/lib/__tests__/classScheduleReflow.spec.ts`, and this log.
- **Fix:** Future scheduled sessions are now ordered by their existing date and assigned lesson curriculum order, aligned using the WIB calendar weekday, and reflowed into strictly increasing weekly slots. Existing multi-week gaps are retained. The Admin confirmation copy now states that lesson order remains intact and only one meeting is scheduled per week.
- **Invariants preserved:** Only future `SCHEDULED` sessions in the selected class are updated. Completed/cancelled history, session IDs, lesson assignments, class blocks, global lesson templates, other classes, coach assignment, and database schema remain unchanged. Existing notification delivery after a permanent schedule change is preserved.
- **Regression risks:** Future changes to class lesson ordering must keep `class_block.start_date` and `class_lesson.order_index` available to the reflow. A class that intentionally needs more than one meeting on the same weekday must use a separate explicit workflow instead of this weekly recurring-schedule action.
- **Verification:** Focused reflow tests passed 4/4, and the full suite passed 294/294 tests across 66 files. `npx tsc --noEmit`, `git diff --check`, and the production build with non-secret build placeholders passed. The scoped repair changed eight future sessions across the two affected Creator Saturday classes and verified zero duplicate scheduled timestamps afterward. Production serves `/login` with HTTP 200, signed-out access to the affected Admin class redirects to login, and PM2 `lms` plus `clevio-wa` are online. Recent retained PM2 error history contains pre-existing outbound fetch timeouts, stream closures, and a Node deprecation warning; no class-schedule error appeared after deployment.
- **Deployment:** Release `aaf208c1ec1ed9f18ce818c07b1c2e39e266fad0` was deployed through `/root/lms/deploy-production.sh`; rollback target is `3e5f8c8f6cb4eaecca2949e96472df9ac81af033`. Before the scoped repair, the eight original timestamps were stored in `/root/lms/shared/bugfix-backups/BUG-2D2206B-1788259066007.json`. The issue report was marked resolved and the requested WhatsApp completion report was delivered successfully.

## BUG-2D2206B-FOLLOWUP: Pitching Day was correct but the class summary reported a skipped lesson

- **Date / reference:** 2026-09-01 / follow-up to `BUG-2D2206B`.
- **Symptom:** Creator Saturday class `3210b684-3f34-4d78-800f-f0ca25d0c2f2` correctly had Pitching Day on 5 September, but the Admin block summary still identified an earlier unassigned Poster lesson as the next lesson. This made the manual correction look incomplete.
- **Root cause:** The block summary selected the curriculum row immediately after the last completed lesson. It did not prefer the lesson attached to the earliest real future session. The original 27 August recurring-schedule mutation also predated a dedicated class-change audit table, so its exact request payload was unavailable; matching `updated_at` timestamps and the former independent weekday-snap algorithm show how several sessions collapsed onto 5 September. The 1 September repair then preserved curriculum order, while the later manual assignment restored the block's canonical `pitching_day_date` of 5 September.
- **Changed:** `src/app/(admin)/admin/classes/[id]/page.tsx`, `src/app/api/admin/classes/[id]/schedule/route.ts`, `src/app/api/admin/class-lessons/assign/route.ts`, `src/lib/services/classBlockProgress.ts`, `src/lib/dao/classChangeAuditDao.ts`, `src/types/supabase.ts`, `supabase/migrations/20260901190000_class_change_audit_logs.sql`, focused tests, and this log.
- **Fix:** The Admin summary now prefers the lesson assigned to the earliest upcoming scheduled session, falling back to curriculum order only when no future assignment exists. An additive audit table records actor, class, event, affected lesson/session, before/after state, source context, and timestamp for every future recurring-schedule or manual lesson-assignment change. Until the migration can be applied with the LMS Supabase owner account, the same structured records are appended to `/root/lms/shared/class-change-audit.jsonl`, which persists across releases.
- **Invariants preserved:** Pitching Day remains 5 September, Evaluasi dan Refleksi remains 12 September, and the next block begins 19 September. No current lesson assignment, session date/status, attendance, block metadata, global lesson template, enrollment, or other class is changed by the code release. Audit records contain identifiers and operational state only, not credentials or message payloads.
- **Regression risks:** Deliberately unassigned curriculum lessons remain available for later reassignment and are not deleted. Any future mutation path that changes session dates or lesson pairing must call the audit DAO as part of its implementation. The database migration remains pending until the correct Supabase owner session is available; the persistent JSONL fallback must remain writable until then.
- **Verification:** Focused progress, schedule, and audit tests passed, including the exact Tutorial-to-Pitching jump, curriculum fallback, both audited mutation paths, and append-only fallback. The full suite passed 298/298 tests across 68 files. TypeScript, `git diff --check`, and the production build with non-secret build placeholders passed. A production read-only probe confirmed Pitching Day on 5 September, Evaluasi dan Refleksi on 12 September, the next block lesson on 19 September, zero duplicate scheduled timestamps, and one reconstructed audit record. `/login` returns HTTP 200, signed-out class access redirects with 307, and PM2 `lms` plus `clevio-wa` are online. Authenticated Admin visual confirmation remains pending because the available browser session is not signed in to LMS.
- **Deployment:** Release `7bfa161ded531920c6a09d2ffd31a78b739fb5a2` was deployed through `/root/lms/deploy-production.sh`; rollback target is `c76b25240ccb5e6ac5b2efcd97775a27bcc4ab2e`. The reconstructed history is stored in `/root/lms/shared/class-change-audit.jsonl` with mode `600`. The additive database migration is committed but not applied because the available Supabase dashboard session only has the separate website project, not the LMS project; runtime audit logging safely uses the persistent fallback until the owner session becomes available.
## INTERNAL-20260902-01: Curriculum lesson snapshot and automatic block publishing

- **Date / reference:** 2026-09-02 / `INTERNAL-20260902-01`.
- **Symptom:** Curriculum blocks with active lessons could remain labelled Draft, while editing a global lesson could overwrite class lesson content that had already been taught. Meeting-count changes also rebuilt active class structures without first checking whether the class had started or passed that lesson.
- **Root cause:** Lesson reads replaced stored `class_lessons` snapshot fields with live `lesson_templates` values. Curriculum mutation routes used a global rebalancer for every current/upcoming class, and the rebalancer did not define a lesson-start boundary. Block publication was independent from lesson saves.
- **Changed:** `src/lib/lessonTemplateSyncPolicy.ts`, `src/lib/services/lessonRebalancer.ts`, `src/lib/dao/classLessonsDao.ts`, Admin curriculum lesson create/edit/bulk/example routes, `supabase/migrations/20260902090000_publish_blocks_with_active_lessons.sql`, focused regression tests, and this log.
- **Fix:** Class lesson fields now remain snapshots. A template change is synchronized only when the class has not reached that template position; once the first part starts or a later lesson has started, the class keeps its existing version and part count. Future lessons can still adopt content, order, and meeting-count changes, after which only scheduled future assignments are rebuilt. Saving a lesson automatically publishes its block, and the additive migration publishes existing blocks that already contain active lessons.
- **Invariants preserved:** Completed and in-progress lesson history, attendance, session dates, class-scoped extensions, and lessons after a started boundary are not rewritten. A lesson A duration edit cannot rewind a class already teaching lesson B. Changes remain scoped to classes linked to the edited global block; other classes and global templates are untouched.
- **Regression risks:** Any future curriculum mutation endpoint must call the snapshot-aware block synchronizer instead of directly updating all `class_lessons`. New ordering schemes must retain a stable relationship between template order and class lesson order.
- **Verification:** Focused snapshot and extension tests passed 12/12. The full suite passed 304/304 tests across 69 files, TypeScript and `git diff --check` passed, and the production build completed with non-secret build-only environment values (111 static pages generated).
- **Deployment:** Release `40d9b4f8bbf7c2693e0cb88fdcea1a7389c7c9dd` was deployed through `/root/lms/deploy-production.sh`; rollback target is `5c2d1783698ca12ef321f11a6eccfde070945181`. The additive publication migration updated 15 blocks with active lessons and left zero eligible drafts. The GDevelop block is published, Creator Sabtu still has Pitching Day on 5 September and Evaluasi dan Refleksi on 12 September, PM2 `lms` and `clevio-wa` are online, Login returned `200`, the protected class route returned `307` signed out, and recent LMS error output was empty.

## INTERNAL-20260902-02: Public portfolio journey hierarchy and compact viewport CTA

- **Date / reference:** 2026-09-02 / `INTERNAL-20260902-02`.
- **Symptom:** The public Portfolio's “Perjalanan Coder” repeated the latest project and presented program, level, and skill badges without a clear relationship. On short laptop and phone viewports, the intro CTA could fall below the fixed viewport and become unreachable. Project cards also stretched to a shared height while using an uneven editorial grid, making their proportions feel inconsistent.
- **Root cause:** The journey model used the latest project type and duplicated next-step story as generic traits. The intro copy retained desktop spacing at short heights, while the project grid used uneven column spans with a fixed minimum height rather than one consistent card ratio.
- **Changed:** `src/lib/publicPortfolioExperience.ts`, `src/components/portfolio/PublicPortfolioExperience.tsx`, `src/components/portfolio/PublicPortfolioExperience.module.css`, `src/components/portfolio/PublicPortfolioGallery.tsx`, focused portfolio behavior/responsive tests, and this log/index.
- **Fix:** “Perjalanan Coder” now reads as a profile snapshot: program and level, approved project count, primary skill, and next focus. The repeated callout was removed without changing the animation system. A short-height responsive rule keeps the intro CTA inside the viewport down to 320x480, and project cards use a uniform responsive grid, fixed aspect ratio, bounded title lines, and normalized screenshot frames so source image dimensions cannot dictate layout.
- **Invariants preserved:** Published portfolio snapshots, project ordering, public slug access, approved-only visibility, animation keyframes, reduced-motion behavior, navigation, modal scroll locking, and all database/API state remain unchanged. No migration or production data mutation is required.
- **Regression risks:** Extremely long coder names or project titles should continue to be checked at 320px width. The fixed screenshot crop intentionally prioritizes consistent gallery rhythm over preserving each source image's original aspect ratio; full images remain available in the project detail content.
- **Verification:** Focused portfolio behavior and responsive tests passed 7/7; the full suite passed 304/304 tests across 69 files. `npx tsc --noEmit`, `git diff --check`, and the production build with non-secret placeholders passed (111 routes). Local Browser QA used the real published portfolio data at 1280x900, 800x480, 640x480, 390x844, 320x568, and 320x480: intro CTA remained within each viewport, project cards had consistent dimensions per viewport, document width had no horizontal overflow, the updated journey text rendered from real data, and no application console errors appeared (the only local warning was Next development's CSP/eval warning).
- **Deployment:** Release `30082fa64ee4416b73a9642442f5ca530b899bdc` deployed through `/root/lms/deploy-production.sh`; rollback target is `7884ad0d945b413d79cf39bad982a5953528643d`. `/root/lms/current` points to the exact release, PM2 `lms` is online, `/login` and the real published portfolio route returned `200`, and recent `lms` logs contained no error/exception/fatal matches. No migration or production data mutation was performed.

## INTERNAL-20260902-03: Public Portfolio medium viewport type scale

- **Date / reference:** 2026-09-02 / `INTERNAL-20260902-03`.
- **Symptom:** On a small laptop viewport around 664px wide, the Portfolio intro and section headings appeared oversized and competed with the content, even though the CTA remained reachable.
- **Root cause:** The `sm` typography clamps applied their desktop-oriented minimums from 640px upward; at 664px the intro heading rendered at 64px, section headings at 48px, and project titles at 32px.
- **Changed:** `src/components/portfolio/PublicPortfolioExperience.tsx`, `src/components/portfolio/PublicPortfolioGallery.tsx`, `src/components/portfolio/PublicPortfolioExperience.module.css`, the responsive contract test, and this log/index.
- **Fix:** Added a bounded medium-viewport type scale for 640–1023px layouts that reduces h1/h2/h3 minimums while preserving the existing desktop scale at 1024px and above, the compact short-height CTA rules, and all animation/keyframe behavior.
- **Invariants preserved:** Portfolio data, public slug access, approved-only visibility, project ordering, card aspect ratios, modal behavior, navigation, reduced-motion behavior, and all database/API state remain unchanged. No migration or production data mutation is required.
- **Regression risks:** Very long coder names or project titles should still be checked at intermediate zoom levels; the medium scale intentionally prioritizes hierarchy and readability over desktop display sizing.
- **Verification:** Focused responsive contract and portfolio behavior tests passed; TypeScript, `git diff --check`, and the production build with non-secret placeholders passed (111 routes). Browser QA with real published portfolio data checked 320x480, 664x740, and 1280x900: headings reduced to 48px/39.2px/27.2px at 664px, desktop h1 remained 85.76px, CTA stayed within the 320x480 viewport, document width matched each viewport, and no app-specific console errors appeared (only the known Next development CSP/eval warning locally).
- **Deployment:** Release `fd8e004fd83fec2afe84b2a14ffc452e781d5c79` deployed through `/root/lms/deploy-production.sh`; rollback target is `92a8e8a2977d57f641df12f1f21809bdd94f13b7`. `/root/lms/current` points to the exact release, PM2 `lms` is online, and the live Portfolio route returned `200`. No migration or production data mutation was performed.

## INTERNAL-20260903-01: Admin action menus and tutorial data

- **Date / reference:** 2026-09-03 / `INTERNAL-20260903-01`.
- **Symptom:** Admin three-dot action menus opened inside table/card overflow contexts. When the last (or only) row was near the viewport edge, the popup could be clipped and its actions became hard to reach. Admin also needed a safe, clearly labelled Coder account with realistic history for tutorial screenshots.
- **Root cause:** `ActionDropdown` rendered its menu as an `absolute` child of the trigger wrapper. Ancestor `overflow-x: auto` and card stacking contexts could clip the menu instead of letting it escape to the visible viewport.
- **Changed:** `src/components/admin/ActionDropdown.tsx`, `src/lib/__tests__/adminActionDropdown.spec.ts`, this log, and the error-fixing index.
- **Fix:** Action menus now render through a document-body portal with fixed viewport coordinates. The positioner keeps the menu inside horizontal bounds, opens above a bottom-edge trigger when needed, limits scrolling height, and closes on outside click or Escape. Existing action handlers and row/card navigation remain unchanged.
- **Tutorial data:** An explicitly requested, labelled dummy Coder record was added without changing existing rows: inactive enrollment in a completed historical class, three attendance records, fifteen lesson-evaluation scores, one published report with five criterion descriptions, and completed block progress. The account has no parent phone, no payment, no WhatsApp delivery, and no real-person identifiers; it is intended only for Admin documentation.
- **Invariants preserved:** No existing user, class, enrollment, attendance, report, template, or production configuration was edited or deleted. No migration ran, no notification was sent, and no source was edited directly on the VPS.
- **Regression risks:** Portal positioning depends on viewport measurements and should be checked at short desktop/mobile heights; future action-menu variants must preserve the body-level portal boundary and keyboard dismissal.
- **Verification:** Focused dropdown boundary tests cover bottom-edge (opens above) and narrow-viewport (horizontal clamping) behavior. Full test, TypeScript, diff-check, production build, Admin route smoke, PM2 status, and recent log checks are recorded with the deployment result below. Authenticated Admin click-through may still require the owner's browser session.
- **Deployment:** Release `a071c5a5fc0feb8a92aeec3c3782281901f46279` deployed through `/root/lms/deploy-production.sh`; rollback target is `b5a455c04a8b6864bebaf93abde718522af95d0b`. `/root/lms/current` matches the release and PM2 `lms` is online. Public `/login` returned `200`; signed-out Admin dashboard/users routes redirected `307` to login. Recent retained logs contained only a pre-existing Server Reference ID validation error and Node URL deprecation warning; no new ActionDropdown error was observed. No migration ran; the explicitly requested dummy records were the only production data additions.

## INTERNAL-20260910-01: Coder transfer report ownership and reminder isolation

- **Date / reference:** 2026-09-10 / `INTERNAL-20260910-01`.
- **Symptom:** A Coder who had moved classes appeared in the Admin report inbox with a newly generated report from the previous class and previous Coach even though the current class had not reached its reporting point. Separately, inactive Coders and classes that had been discontinued could still be selected by scheduled WhatsApp or PWA reminders.
- **Root cause:** Class moves were represented by deleting or manually changing enrollment rows, so the system had no effective transfer boundary or immutable transfer audit. Report generation trusted historical lesson evaluations without proving that the Coder belonged to that class at the block reporting date or had submitted the final block reflection. Public report Coach identity was resolved from the class's current Coach instead of a report-time snapshot. Reminder queries selected active enrollment rows once but did not consistently check user activity, class lifecycle, enrollment dates, or revalidate eligibility immediately before delivery.
- **Changed:** enrollment/class/report schema and generated Supabase types; Admin class transfer and lifecycle APIs and controls; enrollment setup, eligibility, report generation/review/publish/public display, WhatsApp and PWA reminder services; focused regression contracts; and this log/index. Migration: `supabase/migrations/20260910090000_enrollment_transfer_lifecycle.sql`.
- **Fix:** Admin now has an atomic same-program class-transfer flow that closes the source enrollment, activates the target enrollment, updates the active Weekly payment class, records an immutable transfer audit, initializes the target journey, and notifies the affected Coder and Coaches. Removing a Coder preserves enrollment history instead of deleting it. Class lifecycle can be Active, Paused, Ended, or Cancelled; terminal states cancel future sessions and close active enrollments, while terminal classes cannot be silently reactivated. Reports require enrollment coverage at the class-block reporting date plus a completed Coder block reflection and store the submitting Coach snapshot. Admin publication and WhatsApp delivery revalidate that ownership. H-1 WhatsApp/PWA and one-hour PWA reminders require an active class, active user, current enrollment, and a final eligibility check immediately before creation or send. Browser push endpoint ownership remains exclusive to the latest signed-in account.
- **Invariants preserved:** Transfer is scoped to one Coder, one source class, and one target class of the same program type. It does not mutate global lesson templates, another class's schedule, historical attendance/evaluations, legitimate historical reports, invoices, or unrelated enrollments. Report eligibility uses the historical enrollment boundary, so reports earned before a later transfer remain valid. Notifications are best effort and cannot roll back a successful transfer. No real message or transfer is triggered by automated tests.
- **Regression risks:** Target Weekly journey initialization occurs after the database transfer transaction and returns an explicit warning if setup needs operational review. Ending or cancelling a class is intentionally terminal because future sessions and enrollments are closed. Future report or reminder entry points must reuse enrollment-at-time and class-lifecycle validation rather than querying status alone.
- **Verification:** Focused transfer/report/reminder tests passed, including the responsive dialog and terminal-lifecycle contracts. The full suite passed 313/313 tests across 72 files, `npx tsc --noEmit` passed, `git diff --check` passed, and the production build completed with non-secret placeholders and generated 111 routes. The production schema probe confirmed six required columns, two transactional functions, two audit tables, zero inactive enrollments without an end boundary, and one migration-ledger record. `/login` returned `200`; signed-out Admin class access redirected `307`; signed-out transfer and lifecycle APIs returned `401`; PM2 `lms` was online. Recent retained errors were pre-existing Server Reference ID, invoice gateway timeout, and invalid manual lesson-reflow entries, with no new transfer/report/reminder error.
- **Deployment:** Release `c729cf4119cf6d276978c42c0aed6a493e875888` was deployed through `/root/lms/deploy-production.sh`; rollback target is `97217acc18101b44511f80f38852a87ba8924c4f`. Migration `20260910090000` was applied through the authenticated Supabase Management API and recorded in the migration ledger before the app switch. The one confirmed unsent invalid old-class report and its five child descriptions were backed up, then removed with exact report/class/coder/status guards; the valid historical published report and current-class data were unchanged. Backup: `/root/lms/shared/bugfix-backups/INTERNAL-20260910-01-prechange.json` with mode `600`.

## INTERNAL-20260912-01: Admin lesson-report detail and premature Coach attendance

- **Date / reference:** 2026-09-12 / `INTERNAL-20260912-01`.
- **Symptom:** Admin lesson-report cards only exposed a two-line summary and an incorrect lesson anchor, so the full report and lesson context could not be reviewed or edited from a dedicated page. Coach attendance and session-completion endpoints allowed a future session to be marked before its start time. The Coach lesson-report textarea could also inherit a light text color in the installed PWA and become unreadable on its white field.
- **Root cause:** Lesson reports had no Admin detail route or multi-field update contract; the card linked with the report ID instead of the lesson ID. Attendance authorization checked Coach ownership but did not enforce the session start boundary in either mutation endpoint or the UI. The report field relied on inherited theme colors instead of setting an explicit foreground and WebKit text fill.
- **Changed:** Admin lesson-report board, detail page, editor and PATCH endpoint; Weekly/Ekskul lesson editor deep links; Coach lesson-report modal; Coach attendance page/list/wrapper and both attendance/session-status APIs; `src/lib/sessionTiming.ts`; focused regression tests; this log and index. No schema or environment setting changed.
- **Fix:** Every report card now opens a responsive Admin detail page with full report metadata, lesson summary, meeting count, make-up instructions, slide/example links, editable report type/description/status, and a direct lesson-editor action when the historical reference is unambiguous. A shared start-time predicate disables future attendance controls and rejects direct attendance or premature completion requests server-side. Coach report controls now use explicit high-contrast text, caret, placeholder, and WebKit fill colors in a viewport-bounded modal.
- **Invariants preserved:** Existing report IDs, Coach ownership, report status workflow, embedded Ekskul title marker, curriculum snapshots, lesson templates, class assignments, completed attendance history, reminders, and other classes remain unchanged. Cleanup is restricted to the confirmed future `Creator Jumat` session and its exact derived access record after a mode-600 backup.
- **Regression risks:** Legacy Ekskul reports identify lessons by title; when several Ekskul plans contain the same title, the detail page intentionally withholds the direct lesson-editor link instead of guessing. Client-side disabled controls are convenience only; both mutation APIs remain authoritative.
- **Verification:** Focused timing, attendance UI, Admin report workflow, and PWA contrast tests passed 9/9; the full suite passed 322/322 tests across 74 files. TypeScript, `git diff --check`, and the production build passed with non-secret placeholders and generated 111 pages. `/login` returned `200`; signed-out report list/detail and Coach attendance routes redirected `307`; signed-out attendance and status APIs returned `401`. The deployed SHA matched its release directory and PM2 `lms` plus `clevio-wa` remained online. The available browser profile was signed out, so authenticated Admin/Coach visual confirmation remains pending; responsive structure, viewport-bounded form behavior, and interaction contracts are covered by source-level tests.
- **Deployment:** Application release `9b67637d4244895175688eab270dbdcf89640c3e` was deployed through `/root/lms/deploy-production.sh`; rollback target is `fcdc8cc508cc16a8c83d266fb4120b20006e2617`. Before cleanup, the exact future session, attendance, and derived access rows were stored at `/root/lms/shared/bugfix-backups/INTERNAL-20260912-01-prechange.json` with mode `600`. Cleanup deleted one premature attendance and one `SESSION_COMPLETED` access row, restored the 18 September `Creator Jumat` session to `SCHEDULED`, and verified zero remaining attendance/access rows for that session. A documentation-only release follows so deployed source and Git production remain identical.
## INTERNAL-20260912-02 Individual Coder level progression

- Date: 2026-09-12.
- Symptom: a Coder who had completed every block in the personal level journey could remain active in the same rolling Weekly class and repeat completed blocks. Admin had no explicit placement queue, while the Coder had no level-up state. The lesson issue screen was also labelled ambiguously as `Laporan Lesson`.
- Root cause: class curriculum intentionally cycles for mid-cycle admissions, but individual completion only directly changed an enrollment to `INACTIVE`. It had no idempotent progression record, placement state, complete enrollment boundary, or dedicated LMS notifications. Existing journeys could also absorb later global curriculum blocks.
- Production evidence: the read-only audit found one deterministic candidate with all five personal blocks completed while the enrollment remained active. Identifying data remains outside the committed log. Before correction, the exact user, enrollment, personal progress, and empty progression state were backed up in a mode-700 directory whose files use mode 600.
- Changed areas: `coder_level_progressions`, the `finalize_coder_level` and `place_coder_level_progression` service-role RPCs, personal progress reconciliation, Admin `/admin/level-progressions`, Coder dashboard status, Admin/Coder LMS notifications, and lesson-problem labels. Migration: `20260912150000_coder_level_progressions.sql`.
- Implementation: each Coder keeps a personal block snapshot ordered from the entry block. Completion counts unique blocks in that snapshot, including valid migration-completed blocks without requiring an LMS evaluation. The atomic finalizer records either `WAITING_PLACEMENT` or `PROGRAM_COMPLETED`, closes active enrollments in the source level with `ended_at` and `exit_reason = COMPLETED`, and never changes class schedules or another Coder's progress. Admin placement validates the exact next level and an active Weekly destination, creates/reactivates one enrollment, initializes the next journey at the destination class's current block, and records `PLACED`.
- Invariants: class cycles remain available for staggered admissions; completed personal blocks never reset; level order never wraps to the first level; accounts and historical reports remain available; old-class reminders stop at the enrollment boundary; level-up and placement alerts go only to LMS Admin and LMS Coder with dedupe keys, with no Coach or WhatsApp recipient; old report URLs and data remain unchanged.
- Regression risks: the additive database objects remain compatible with the previous application release, but a code rollback does not remove the table or functions. Historical candidates must be reconciled only after their personal progress is verified; placement remains an explicit Admin action.
- Verification: 75 Vitest files / 336 tests passed, including 14 new progression contracts; `npx tsc --noEmit`, `git diff --check`, and the production build passed. The migration ran successfully in the production Supabase project and its table returned `200` through the service-role REST path. Release `c350695ee8250980f0359426dcf103796e57f23c` matched `/root/lms/current`; PM2 `lms` was online with zero restarts. `/login` returned `200`, signed-out `/admin/level-progressions` redirected `307`, and the signed-out placement API returned `401`. The available browser profile was signed out of LMS, so authenticated visual confirmation remains pending; responsive and interaction contracts are covered by the focused tests.
- Production correction: the verified historical candidate was reconciled once after backup. The progression is `WAITING_PLACEMENT`, the source enrollment is closed with `exit_reason = COMPLETED`, and no active or target enrollment was created. Five deduplicated LMS notifications were stored and pushed to the Coder plus four active Admin accounts; zero Coach notifications were created and no WhatsApp level-up message was sent. Rollback application target: `1d44aa965eb65aa7ce4188c927615a70f383409f`.
