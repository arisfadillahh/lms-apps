# Error-Fixing Log - September 2026

## INTERNAL-20260923-10: Compact report journey cards

- **Date / reference:** 2026-09-23 / `INTERNAL-20260923-10`.
- **Symptom:** Journey lesson names appeared below their sequence numbers, making cards taller than necessary and harder to scan.
- **Root cause:** The number and title were vertically stacked and cards enforced a 96px minimum height.
- **Changed:** `src/app/report/[id]/page.tsx`, A4 print contract coverage, and this log/index. No report data changes.
- **Fix:** Put the number and lesson title on one horizontal row, allow long titles to wrap beside the fixed number, remove the oversized minimum height, and increase both text sizes slightly.
- **Invariants:** Lesson sequence and report data are unchanged; responsive grids and print pagination are preserved.
- **Verification:** Focused report print contract passed 4/4, full suite passed 385/385 across 84 files, TypeScript, `git diff --check`, and production build passed. `/login` returned HTTP 200 after deployment.
- **Deployment:** Release `2f71e728dcf47b64ab5067c456d1654ffe7a9478` is deployed; rollback target is `51fae82cb1390921000b75864e5960ec9d77ae89`. PM2 `lms` and `clevio-wa` are online with zero restarts; the LMS WhatsApp auth directory remains present. No database or WhatsApp session data changed.

## INTERNAL-20260923-09: Portrait print stacked Overall score

- **Date / reference:** 2026-09-23 / `INTERNAL-20260923-09`.
- **Symptom:** In portrait printing, the Overall score moved below the student details and increased the hero height, pushing the learning journey farther down.
- **Root cause:** A4 portrait's print viewport is narrower than the responsive `md` breakpoint, so the web hero's desktop two-column layout was not activated.
- **Changed:** `src/app/report/[id]/page.tsx`, A4 print contract coverage, and this log/index. No report data or screen/mobile layout changes.
- **Fix:** Force a two-column hero in print and remove the score card's stacked-layout top margin. Overall stays at the right of the student details and the hero consumes less vertical space.
- **Invariants:** Screen and mobile responsive layout stays unchanged; A4 portrait remains the default and print content/pagination remains intact.
- **Verification:** Focused report print contract passed 4/4, full suite passed 385/385 across 84 files, TypeScript, diff check, and production build passed. `/login` returned HTTP 200 after release.
- **Deployment:** Release `51fae82cb1390921000b75864e5960ec9d77ae89` is deployed; rollback target is `c10659d0772eb74d37c35e0f34be8271c7d221b2`. PM2 `lms` and `clevio-wa` are online with zero restarts, and the LMS WhatsApp auth directory remains present. No database or WhatsApp session data changed.

## INTERNAL-20260923-08: Report print orientation was locked

- **Date / reference:** 2026-09-23 / `INTERNAL-20260923-08`.
- **Symptom:** The browser's print dialog did not allow choosing portrait for the report.
- **Root cause:** The report's `@page` rule explicitly forced `A4 landscape`.
- **Changed:** `src/app/report/[id]/page.tsx`, A4 print contract coverage, and this log/index. No report data changes.
- **Fix:** Leave the page size at A4 without forcing orientation. Use a portrait-fit desktop canvas scale by default and apply the landscape-fit scale only when print media reports landscape, so the browser dialog controls orientation.
- **Invariants:** A4 paper, desktop report composition, pagination behavior, and print-only hiding of navigation/share controls remain unchanged.
- **Verification:** Focused print contract passed 4/4, full suite passed 385/385 across 84 files, TypeScript, `git diff --check`, and production build passed. A live print-dialog visual check was not possible from this workstation.
- **Deployment:** Release `c10659d0772eb74d37c35e0f34be8271c7d221b2` is deployed through `/root/lms/deploy-production.sh`; rollback target is `e7d3d52ccfb274664302684cbe47239838f36f5c`. `/login` returned HTTP 200; PM2 `lms` and `clevio-wa` are online with zero restarts, and `/root/lms/shared/baileys_auth_info` remains present. No database or WhatsApp session data was changed.

## INTERNAL-20260918-05: Public Portfolio v4 data binding

- **Date / reference:** 2026-09-18 / `INTERNAL-20260918-05`.
- **Symptom:** The public Portfolio Universe still used a project-publication timeline where the revised experience needs the Coder's actual level and block journey. The requested featured card and mockup project details could also be mistaken for placeholder content.
- **Root cause:** The public model only received approved portfolio snapshots and basic enrollment metadata; it did not receive the authoritative personal `coder_block_progress` records.
- **Changed:** `src/app/portfolio/[slug]/page.tsx`, `src/lib/publicPortfolioExperience.ts`, `src/components/portfolio/PublicPortfolioExperience.tsx`, `src/components/portfolio/PublicPortfolioGallery.tsx`, focused portfolio tests, and this log/index.
- **Fix:** The public page continues to load only non-null `published_snapshot` records for projects. It additionally reads the Coder's level/block progress, groups it by curriculum level and journey order, and renders actual status labels. The featured card uses the latest approved project by default; its existing accessible modal receives the approved title, screenshots, tools, skills, project story, reflection, next steps, and permitted URLs.
- **Invariants preserved:** No portfolio, enrollment, progress, screenshot, or profile record is modified. Draft, revision, and unapproved project fields are not exposed. School visibility remains controlled by `school_visible`; project links stay limited to snapshot values approved for public publication.
- **Regression risks:** A Coder with no personal progress receives a truthful empty journey state. A Coder with no approved project keeps the existing empty gallery rather than a featured placeholder. Future featured-project selection may be added as an optional profile preference without altering this fallback.
- **Verification:** Focused portfolio tests, TypeScript, diff check, production build, and post-deployment route checks are recorded with release verification.
- **Deployment:** Release `85eef097cfd75d07626063bab3a5718ced2cad9e` was fast-forwarded to `production` and deployed through `/root/lms/deploy-production.sh`. `/root/lms/current` matched that SHA, PM2 `lms` was online, `/login` returned `200`, signed-out `/admin/portfolios` redirected `307`, and a public portfolio route backed by an existing published profile returned `200`. Browser inspection confirmed the public Portfolio Universe entry view rendered with the Clevio logo and responsive intro action. Rollback target is `72419e6ebcf113ce067d90cdd9239ea874cc6ff3`.

## INTERNAL-20260901-01: Portfolio CTA glow shape

- **Date / reference:** 2026-09-01 / `INTERNAL-20260901-01`.
- **Symptom:** The green CTA shadow on the public Portfolio Universe looked like a rigid rectangular patch and appeared visually cut off on mobile.
- **Root cause:** Green CTA buttons used large Tailwind `box-shadow` utilities alongside the animated sheen layer; the resulting blur did not consistently follow the button silhouette at narrow viewport edges.
- **Changed:** `src/components/portfolio/PublicPortfolioExperience.tsx`, `src/components/portfolio/PublicPortfolioExperience.module.css`, `src/lib/__tests__/publicPortfolioResponsive.contract.spec.ts`, and this log.
- **Fix:** Replaced the hard green box shadows on green Portfolio CTAs with shape-aware `drop-shadow` filters. Hover/focus keeps a slightly stronger glow, and reduced-motion removes the filter. The button radius, layout, colors, copy, and sheen behavior remain unchanged.
- **Invariants preserved:** Portfolio data, navigation, section structure, modal scroll lock, responsive bounds, and all non-Portfolio pages remain unchanged. No database, API, or production data was modified.
- **Regression risks:** Future CTA filter or transform utilities should be checked together because CSS filters and transforms can affect compositing and perceived shadow edges.
- **Verification:** Portfolio responsive contract passed 5 tests, clean-source `npx tsc --noEmit` passed, and `git diff --check` passed. Next webpack compiled successfully before the pre-existing unrelated Coach Ekskul `PageProps.params` type-generation error. Production mobile QA at 390x844 confirmed the CTA uses the shape-aware `drop-shadow`, keeps its expected bounds, and produces no horizontal overflow. The release smoke check is recorded after deployment.
- **Deployment:** Release `5413721ed807815fd718e6921b3bc5ab823ab5fd` was deployed through `/root/lms/deploy-production.sh`; rollback target is `9305debec72725adfd1a3d8852f750f211b8911e`. The deployed release directory and Git SHA matched, PM2 `lms` and `clevio-wa` were online, and the Portfolio and Login routes returned `200`.

## INTERNAL-20260901-02: Portfolio mobile single-scroll context

- **Date / reference:** 2026-09-01 / `INTERNAL-20260901-02`.
- **Symptom:** After entering the Portfolio Universe on mobile, scrolling could show a duplicate scrollbar or feel like both the page and its document shell were scrollable.
- **Root cause:** The global `MobileScrollFix` forced `html` and `body` into its body-scroll mode on every mobile route, including Portfolio. Portfolio therefore inherited global overflow rules even though its public page needs one normal document scroll context.
- **Changed:** `src/components/layout/MobileScrollFix.tsx`, `src/components/portfolio/PublicPortfolioExperience.tsx`, `src/components/portfolio/PublicPortfolioExperience.module.css`, `src/lib/__tests__/publicPortfolioResponsive.contract.spec.ts`, and this log.
- **Fix:** Portfolio marks its page mode before the global mobile effect runs. The mobile helper excludes that page from its body-scroll overrides and re-applies normal defaults on route exit; the existing intro/modal lock remains explicit and modal content remains independently scrollable. The CTA glow was also reduced to a smaller shape-aware drop-shadow so it cannot read as a clipped rectangular patch.
- **Invariants preserved:** Other Admin, Coach, and Coder mobile scroll behavior remains unchanged. Portfolio layout, color palette, content, published data, modal behavior, and database/API state are unchanged.
- **Regression risks:** Global mobile overflow rules must continue to exclude `data-portfolio-page="true"`; future public pages should use their own scroll mode marker if they need different document behavior.
- **Verification:** Portfolio responsive contract passed 5 tests, clean-source `npx tsc --noEmit` passed, and `git diff --check` passed. Production touch QA at 390x844 will verify one `HTML` scrolling context, one document width, and CTA filter after the release is deployed.
- **Deployment:** Release `13d84d61282119b5d610b5573d6d21987274db0f` was deployed through `/root/lms/deploy-production.sh`; rollback target is `b6009e64fa46bc7d0761a2608535b55b0b0116d9`. Production touch QA at 390x844 confirmed the Portfolio marker is active, CTA uses the reduced shape-aware glow, `HTML` is the only scrolling element, `body` is not forced into a second scrollbar, document width is 390/390, and the page scrolls normally after Enter Portfolio.

## INTERNAL-20260901-03: Portfolio desktop duplicate scrollbar

- **Date / reference:** 2026-09-01 / `INTERNAL-20260901-03`.
- **Symptom:** The public Portfolio page still displayed two vertical scrollbars on desktop after entering the experience.
- **Root cause:** The browser treated both `HTML` and `BODY` as scrollable because the global shell left body overflow in an auto context and its scroll height exceeded the viewport by the shell gutter.
- **Changed:** `src/components/portfolio/PublicPortfolioExperience.module.css`, `src/lib/__tests__/publicPortfolioResponsive.contract.spec.ts`, and this log.
- **Fix:** While the Portfolio page marker is active, `HTML` owns vertical scrolling and `BODY` uses visible vertical overflow. The existing mobile helper exclusion remains in place, so desktop and mobile share the same single-scroll contract without changing content layout.
- **Invariants preserved:** Other role dashboards keep their existing mobile scroll behavior. Portfolio content, layout, colors, animations, modal lock, and database/API state remain unchanged.
- **Verification:** Browser DOM audit found both `HTML` and `BODY` scrollable before the fix. After the fix, the production desktop audit found only `HTML` scrollable (`BODY` height equals its client height). Focused responsive contract tests (5/5), clean-source TypeScript, and `git diff --check` passed; the mobile single-scroll contract remains covered by the existing production touch QA.
- **Deployment:** Release `47141bf4af569e9881aacc0e630389cdb1383ae6` was deployed through `/root/lms/deploy-production.sh`; rollback target is `06a6f6eb54fa1b5616728bd159291d37966b8fff`. PM2 services remained online and the public Portfolio route returned HTTP 200.

## INTERNAL-20260901-04: Portfolio body overflow axis normalization

- **Date / reference:** 2026-09-01 / `INTERNAL-20260901-04`.
- **Symptom:** Two vertical scrollbar tracks were still visible on desktop at the user's browser size and zoom after entering the public Portfolio experience.
- **Root cause:** The page-specific rule only set `body` vertical overflow to `visible`, while the global stylesheet kept horizontal overflow `hidden`. CSS overflow-axis normalization converts `visible` on one axis to `auto` when the other axis is `hidden`, so both `HTML` and `BODY` remained independently scrollable when body content exceeded its client height.
- **Changed:** `src/components/portfolio/PublicPortfolioExperience.module.css`, `src/lib/__tests__/publicPortfolioResponsive.contract.spec.ts`, and this log.
- **Fix:** The Portfolio page now restores both body overflow axes to `visible`; `HTML` remains the sole vertical scrolling element. Horizontal clipping remains owned by the Portfolio root wrapper, so the layout does not gain horizontal overflow.
- **Invariants preserved:** Portfolio layout, motion, intro lock, modal lock, data, API state, and all non-Portfolio role pages remain unchanged. No database changes are required.
- **Regression risks:** Any future global overflow declaration must not turn Portfolio body overflow back into an independent scrolling context.
- **Verification:** Portfolio responsive contract passed 5/5 tests, clean-source `npx tsc --noEmit` passed, and `git diff --check` passed. Production browser QA at 1873x963 confirmed body overflow is `visible` on both axes, the only vertical scrollable element is `HTML`, document width equals viewport width, a scroll interaction moved the document from 0 to 688 px, and no console warnings or errors were emitted.
- **Deployment:** Release `5165db521425a035b7d185877ab90c193eaee8e6` was deployed through `/root/lms/deploy-production.sh`; rollback target is `99bb044bf0d553baf6e5812a7983c196eba69eb3`. PM2 and public route smoke checks are recorded after the final documentation release.

## BUG-2D2206B: Permanent class reschedule collapsed lessons onto one date

- **Date / reference:** 2026-09-01 / `BUG-2D2206B`.
- **Symptom:** On the Admin class detail page, permanent rescheduling could place several upcoming Creator lessons on the same date. The affected Coach Naufal Saturday classes showed multiple lessons competing for one session date.
- **Root cause:** The permanent schedule endpoint snapped every future session independently to the requested weekday. Sessions that were already in the same calendar week therefore collapsed onto one timestamp. Equal timestamps had no curriculum-order tie breaker, and weekday calculation used UTC instead of the class's WIB calendar date.
- **Changed:** `src/app/api/admin/classes/[id]/schedule/route.ts`, `src/app/(admin)/admin/classes/[id]/ChangeClassScheduleModal.tsx`, `src/lib/services/classScheduleReflow.ts`, `src/lib/__tests__/classScheduleReflow.spec.ts`, and this log.
- **Fix:** Future scheduled sessions are now ordered by their existing date and assigned lesson curriculum order, aligned using the WIB calendar weekday, and reflowed into strictly increasing weekly slots. Existing multi-week gaps are retained. The Admin confirmation copy now states that lesson order remains intact and only one meeting is scheduled per week.
- **Invariants preserved:** Only future `SCHEDULED` sessions in the selected class are updated. Completed/cancelled history, session IDs, lesson assignments, class blocks, global lesson templates, other classes, coach assignment, and database schema remain unchanged. Existing notification delivery after a permanent schedule change is preserved.
- **Regression risks:** Future changes to class lesson ordering must keep `class_block.start_date` and `class_lesson.order_index` available to the reflow. A class that intentionally needs more than one meeting on the same weekday must use a separate explicit workflow instead of this weekly recurring-schedule action.
- **Verification:** Focused reflow tests passed 4/4, and the full suite passed 294/294 tests across 66 files. `npx tsc --noEmit`, `git diff --check`, and the production build with non-secret build placeholders passed. The scoped repair changed eight future sessions across the two affected Creator Saturday classes and verified zero duplicate scheduled timestamps afterward. Production serves `/login` with HTTP 200, signed-out access to the affected Admin class redirects to login, and PM2 `lms` plus `clevio-wa` are online. Recent retained PM2 error history contains pre-existing outbound fetch timeouts, stream closures, and a Node deprecation warning; no class-schedule error appeared after deployment.
- **Deployment:** Release `aaf208c1ec1ed9f18ce818c07b1c2e39e266fad0` was deployed through `/root/lms/deploy-production.sh`; rollback target is `3e5f8c8f6cb4eaecca2949e96472df9ac81af033`. Before the scoped repair, the eight original timestamps were stored in `/root/lms/shared/bugfix-backups/BUG-2D2206B-1788259066007.json`. The issue report was marked resolved and the requested WhatsApp completion report was delivered successfully.

## BUG-2D2206B-FOLLOWUP: Pitching Day was correct but the class summary reported a skipped lesson

- **Date / reference:** 2026-09-01 / follow-up to `BUG-2D2206B`.
- **Symptom:** Creator Saturday class `3210b684-3f34-4d78-800f-f0ca25d0c2f2` correctly had Pitching Day on 5 September, but the Admin block summary still identified an earlier unassigned Poster lesson as the next lesson. This made the manual correction look incomplete.
- **Root cause:** The block summary selected the curriculum row immediately after the last completed lesson. It did not prefer the lesson attached to the earliest real future session. The original 27 August recurring-schedule mutation also predated a dedicated class-change audit table, so its exact request payload was unavailable; matching `updated_at` timestamps and the former independent weekday-snap algorithm show how several sessions collapsed onto 5 September. The 1 September repair then preserved curriculum order, while the later manual assignment restored the block's canonical `pitching_day_date` of 5 September.
- **Changed:** `src/app/(admin)/admin/classes/[id]/page.tsx`, `src/app/api/admin/classes/[id]/schedule/route.ts`, `src/app/api/admin/class-lessons/assign/route.ts`, `src/lib/services/classBlockProgress.ts`, `src/lib/dao/classChangeAuditDao.ts`, `src/types/supabase.ts`, `supabase/migrations/20260901190000_class_change_audit_logs.sql`, focused tests, and this log.
- **Fix:** The Admin summary now prefers the lesson assigned to the earliest upcoming scheduled session, falling back to curriculum order only when no future assignment exists. An additive audit table records actor, class, event, affected lesson/session, before/after state, source context, and timestamp for every future recurring-schedule or manual lesson-assignment change. Until the migration can be applied with the LMS Supabase owner account, the same structured records are appended to `/root/lms/shared/class-change-audit.jsonl`, which persists across releases.
- **Invariants preserved:** Pitching Day remains 5 September, Evaluasi dan Refleksi remains 12 September, and the next block begins 19 September. No current lesson assignment, session date/status, attendance, block metadata, global lesson template, enrollment, or other class is changed by the code release. Audit records contain identifiers and operational state only, not credentials or message payloads.
- **Regression risks:** Deliberately unassigned curriculum lessons remain available for later reassignment and are not deleted. Any future mutation path that changes session dates or lesson pairing must call the audit DAO as part of its implementation. The database migration remains pending until the correct Supabase owner session is available; the persistent JSONL fallback must remain writable until then.
- **Verification:** Focused progress, schedule, and audit tests passed, including the exact Tutorial-to-Pitching jump, curriculum fallback, both audited mutation paths, and append-only fallback. The full suite passed 298/298 tests across 68 files. TypeScript, `git diff --check`, and the production build with non-secret build placeholders passed. A production read-only probe confirmed Pitching Day on 5 September, Evaluasi dan Refleksi on 12 September, the next block lesson on 19 September, zero duplicate scheduled timestamps, and one reconstructed audit record. `/login` returns HTTP 200, signed-out class access redirects with 307, and PM2 `lms` plus `clevio-wa` are online. Authenticated Admin visual confirmation remains pending because the available browser session is not signed in to LMS.
- **Deployment:** Release `7bfa161ded531920c6a09d2ffd31a78b739fb5a2` was deployed through `/root/lms/deploy-production.sh`; rollback target is `c76b25240ccb5e6ac5b2efcd97775a27bcc4ab2e`. The reconstructed history is stored in `/root/lms/shared/class-change-audit.jsonl` with mode `600`. The additive database migration is committed but not applied because the available Supabase dashboard session only has the separate website project, not the LMS project; runtime audit logging safely uses the persistent fallback until the owner session becomes available.
## INTERNAL-20260902-01: Curriculum lesson snapshot and automatic block publishing

- **Date / reference:** 2026-09-02 / `INTERNAL-20260902-01`.
- **Symptom:** Curriculum blocks with active lessons could remain labelled Draft, while editing a global lesson could overwrite class lesson content that had already been taught. Meeting-count changes also rebuilt active class structures without first checking whether the class had started or passed that lesson.
- **Root cause:** Lesson reads replaced stored `class_lessons` snapshot fields with live `lesson_templates` values. Curriculum mutation routes used a global rebalancer for every current/upcoming class, and the rebalancer did not define a lesson-start boundary. Block publication was independent from lesson saves.
- **Changed:** `src/lib/lessonTemplateSyncPolicy.ts`, `src/lib/services/lessonRebalancer.ts`, `src/lib/dao/classLessonsDao.ts`, Admin curriculum lesson create/edit/bulk/example routes, `supabase/migrations/20260902090000_publish_blocks_with_active_lessons.sql`, focused regression tests, and this log.
- **Fix:** Class lesson fields now remain snapshots. A template change is synchronized only when the class has not reached that template position; once the first part starts or a later lesson has started, the class keeps its existing version and part count. Future lessons can still adopt content, order, and meeting-count changes, after which only scheduled future assignments are rebuilt. Saving a lesson automatically publishes its block, and the additive migration publishes existing blocks that already contain active lessons.
- **Invariants preserved:** Completed and in-progress lesson history, attendance, session dates, class-scoped extensions, and lessons after a started boundary are not rewritten. A lesson A duration edit cannot rewind a class already teaching lesson B. Changes remain scoped to classes linked to the edited global block; other classes and global templates are untouched.
- **Regression risks:** Any future curriculum mutation endpoint must call the snapshot-aware block synchronizer instead of directly updating all `class_lessons`. New ordering schemes must retain a stable relationship between template order and class lesson order.
- **Verification:** Focused snapshot and extension tests passed 12/12. The full suite passed 304/304 tests across 69 files, TypeScript and `git diff --check` passed, and the production build completed with non-secret build-only environment values (111 static pages generated).
- **Deployment:** Release `40d9b4f8bbf7c2693e0cb88fdcea1a7389c7c9dd` was deployed through `/root/lms/deploy-production.sh`; rollback target is `5c2d1783698ca12ef321f11a6eccfde070945181`. The additive publication migration updated 15 blocks with active lessons and left zero eligible drafts. The GDevelop block is published, Creator Sabtu still has Pitching Day on 5 September and Evaluasi dan Refleksi on 12 September, PM2 `lms` and `clevio-wa` are online, Login returned `200`, the protected class route returned `307` signed out, and recent LMS error output was empty.

## INTERNAL-20260902-02: Public portfolio journey hierarchy and compact viewport CTA

- **Date / reference:** 2026-09-02 / `INTERNAL-20260902-02`.
- **Symptom:** The public Portfolio's “Perjalanan Coder” repeated the latest project and presented program, level, and skill badges without a clear relationship. On short laptop and phone viewports, the intro CTA could fall below the fixed viewport and become unreachable. Project cards also stretched to a shared height while using an uneven editorial grid, making their proportions feel inconsistent.
- **Root cause:** The journey model used the latest project type and duplicated next-step story as generic traits. The intro copy retained desktop spacing at short heights, while the project grid used uneven column spans with a fixed minimum height rather than one consistent card ratio.
- **Changed:** `src/lib/publicPortfolioExperience.ts`, `src/components/portfolio/PublicPortfolioExperience.tsx`, `src/components/portfolio/PublicPortfolioExperience.module.css`, `src/components/portfolio/PublicPortfolioGallery.tsx`, focused portfolio behavior/responsive tests, and this log/index.
- **Fix:** “Perjalanan Coder” now reads as a profile snapshot: program and level, approved project count, primary skill, and next focus. The repeated callout was removed without changing the animation system. A short-height responsive rule keeps the intro CTA inside the viewport down to 320x480, and project cards use a uniform responsive grid, fixed aspect ratio, bounded title lines, and normalized screenshot frames so source image dimensions cannot dictate layout.
- **Invariants preserved:** Published portfolio snapshots, project ordering, public slug access, approved-only visibility, animation keyframes, reduced-motion behavior, navigation, modal scroll locking, and all database/API state remain unchanged. No migration or production data mutation is required.
- **Regression risks:** Extremely long coder names or project titles should continue to be checked at 320px width. The fixed screenshot crop intentionally prioritizes consistent gallery rhythm over preserving each source image's original aspect ratio; full images remain available in the project detail content.
- **Verification:** Focused portfolio behavior and responsive tests passed 7/7; the full suite passed 304/304 tests across 69 files. `npx tsc --noEmit`, `git diff --check`, and the production build with non-secret placeholders passed (111 routes). Local Browser QA used the real published portfolio data at 1280x900, 800x480, 640x480, 390x844, 320x568, and 320x480: intro CTA remained within each viewport, project cards had consistent dimensions per viewport, document width had no horizontal overflow, the updated journey text rendered from real data, and no application console errors appeared (the only local warning was Next development's CSP/eval warning).
- **Deployment:** Release `30082fa64ee4416b73a9642442f5ca530b899bdc` deployed through `/root/lms/deploy-production.sh`; rollback target is `7884ad0d945b413d79cf39bad982a5953528643d`. `/root/lms/current` points to the exact release, PM2 `lms` is online, `/login` and the real published portfolio route returned `200`, and recent `lms` logs contained no error/exception/fatal matches. No migration or production data mutation was performed.

## INTERNAL-20260902-03: Public Portfolio medium viewport type scale

- **Date / reference:** 2026-09-02 / `INTERNAL-20260902-03`.
- **Symptom:** On a small laptop viewport around 664px wide, the Portfolio intro and section headings appeared oversized and competed with the content, even though the CTA remained reachable.
- **Root cause:** The `sm` typography clamps applied their desktop-oriented minimums from 640px upward; at 664px the intro heading rendered at 64px, section headings at 48px, and project titles at 32px.
- **Changed:** `src/components/portfolio/PublicPortfolioExperience.tsx`, `src/components/portfolio/PublicPortfolioGallery.tsx`, `src/components/portfolio/PublicPortfolioExperience.module.css`, the responsive contract test, and this log/index.
- **Fix:** Added a bounded medium-viewport type scale for 640–1023px layouts that reduces h1/h2/h3 minimums while preserving the existing desktop scale at 1024px and above, the compact short-height CTA rules, and all animation/keyframe behavior.
- **Invariants preserved:** Portfolio data, public slug access, approved-only visibility, project ordering, card aspect ratios, modal behavior, navigation, reduced-motion behavior, and all database/API state remain unchanged. No migration or production data mutation is required.
- **Regression risks:** Very long coder names or project titles should still be checked at intermediate zoom levels; the medium scale intentionally prioritizes hierarchy and readability over desktop display sizing.
- **Verification:** Focused responsive contract and portfolio behavior tests passed; TypeScript, `git diff --check`, and the production build with non-secret placeholders passed (111 routes). Browser QA with real published portfolio data checked 320x480, 664x740, and 1280x900: headings reduced to 48px/39.2px/27.2px at 664px, desktop h1 remained 85.76px, CTA stayed within the 320x480 viewport, document width matched each viewport, and no app-specific console errors appeared (only the known Next development CSP/eval warning locally).
- **Deployment:** Release `fd8e004fd83fec2afe84b2a14ffc452e781d5c79` deployed through `/root/lms/deploy-production.sh`; rollback target is `92a8e8a2977d57f641df12f1f21809bdd94f13b7`. `/root/lms/current` points to the exact release, PM2 `lms` is online, and the live Portfolio route returned `200`. No migration or production data mutation was performed.

## INTERNAL-20260903-01: Admin action menus and tutorial data

- **Date / reference:** 2026-09-03 / `INTERNAL-20260903-01`.
- **Symptom:** Admin three-dot action menus opened inside table/card overflow contexts. When the last (or only) row was near the viewport edge, the popup could be clipped and its actions became hard to reach. Admin also needed a safe, clearly labelled Coder account with realistic history for tutorial screenshots.
- **Root cause:** `ActionDropdown` rendered its menu as an `absolute` child of the trigger wrapper. Ancestor `overflow-x: auto` and card stacking contexts could clip the menu instead of letting it escape to the visible viewport.
- **Changed:** `src/components/admin/ActionDropdown.tsx`, `src/lib/__tests__/adminActionDropdown.spec.ts`, this log, and the error-fixing index.
- **Fix:** Action menus now render through a document-body portal with fixed viewport coordinates. The positioner keeps the menu inside horizontal bounds, opens above a bottom-edge trigger when needed, limits scrolling height, and closes on outside click or Escape. Existing action handlers and row/card navigation remain unchanged.
- **Tutorial data:** An explicitly requested, labelled dummy Coder record was added without changing existing rows: inactive enrollment in a completed historical class, three attendance records, fifteen lesson-evaluation scores, one published report with five criterion descriptions, and completed block progress. The account has no parent phone, no payment, no WhatsApp delivery, and no real-person identifiers; it is intended only for Admin documentation.
- **Invariants preserved:** No existing user, class, enrollment, attendance, report, template, or production configuration was edited or deleted. No migration ran, no notification was sent, and no source was edited directly on the VPS.
- **Regression risks:** Portal positioning depends on viewport measurements and should be checked at short desktop/mobile heights; future action-menu variants must preserve the body-level portal boundary and keyboard dismissal.
- **Verification:** Focused dropdown boundary tests cover bottom-edge (opens above) and narrow-viewport (horizontal clamping) behavior. Full test, TypeScript, diff-check, production build, Admin route smoke, PM2 status, and recent log checks are recorded with the deployment result below. Authenticated Admin click-through may still require the owner's browser session.
- **Deployment:** Release `a071c5a5fc0feb8a92aeec3c3782281901f46279` deployed through `/root/lms/deploy-production.sh`; rollback target is `b5a455c04a8b6864bebaf93abde718522af95d0b`. `/root/lms/current` matches the release and PM2 `lms` is online. Public `/login` returned `200`; signed-out Admin dashboard/users routes redirected `307` to login. Recent retained logs contained only a pre-existing Server Reference ID validation error and Node URL deprecation warning; no new ActionDropdown error was observed. No migration ran; the explicitly requested dummy records were the only production data additions.

## INTERNAL-20260910-01: Coder transfer report ownership and reminder isolation

- **Date / reference:** 2026-09-10 / `INTERNAL-20260910-01`.
- **Symptom:** A Coder who had moved classes appeared in the Admin report inbox with a newly generated report from the previous class and previous Coach even though the current class had not reached its reporting point. Separately, inactive Coders and classes that had been discontinued could still be selected by scheduled WhatsApp or PWA reminders.
- **Root cause:** Class moves were represented by deleting or manually changing enrollment rows, so the system had no effective transfer boundary or immutable transfer audit. Report generation trusted historical lesson evaluations without proving that the Coder belonged to that class at the block reporting date or had submitted the final block reflection. Public report Coach identity was resolved from the class's current Coach instead of a report-time snapshot. Reminder queries selected active enrollment rows once but did not consistently check user activity, class lifecycle, enrollment dates, or revalidate eligibility immediately before delivery.
- **Changed:** enrollment/class/report schema and generated Supabase types; Admin class transfer and lifecycle APIs and controls; enrollment setup, eligibility, report generation/review/publish/public display, WhatsApp and PWA reminder services; focused regression contracts; and this log/index. Migration: `supabase/migrations/20260910090000_enrollment_transfer_lifecycle.sql`.
- **Fix:** Admin now has an atomic same-program class-transfer flow that closes the source enrollment, activates the target enrollment, updates the active Weekly payment class, records an immutable transfer audit, initializes the target journey, and notifies the affected Coder and Coaches. Removing a Coder preserves enrollment history instead of deleting it. Class lifecycle can be Active, Paused, Ended, or Cancelled; terminal states cancel future sessions and close active enrollments, while terminal classes cannot be silently reactivated. Reports require enrollment coverage at the class-block reporting date plus a completed Coder block reflection and store the submitting Coach snapshot. Admin publication and WhatsApp delivery revalidate that ownership. H-1 WhatsApp/PWA and one-hour PWA reminders require an active class, active user, current enrollment, and a final eligibility check immediately before creation or send. Browser push endpoint ownership remains exclusive to the latest signed-in account.
- **Invariants preserved:** Transfer is scoped to one Coder, one source class, and one target class of the same program type. It does not mutate global lesson templates, another class's schedule, historical attendance/evaluations, legitimate historical reports, invoices, or unrelated enrollments. Report eligibility uses the historical enrollment boundary, so reports earned before a later transfer remain valid. Notifications are best effort and cannot roll back a successful transfer. No real message or transfer is triggered by automated tests.
- **Regression risks:** Target Weekly journey initialization occurs after the database transfer transaction and returns an explicit warning if setup needs operational review. Ending or cancelling a class is intentionally terminal because future sessions and enrollments are closed. Future report or reminder entry points must reuse enrollment-at-time and class-lifecycle validation rather than querying status alone.
- **Verification:** Focused transfer/report/reminder tests passed, including the responsive dialog and terminal-lifecycle contracts. The full suite passed 313/313 tests across 72 files, `npx tsc --noEmit` passed, `git diff --check` passed, and the production build completed with non-secret placeholders and generated 111 routes. The production schema probe confirmed six required columns, two transactional functions, two audit tables, zero inactive enrollments without an end boundary, and one migration-ledger record. `/login` returned `200`; signed-out Admin class access redirected `307`; signed-out transfer and lifecycle APIs returned `401`; PM2 `lms` was online. Recent retained errors were pre-existing Server Reference ID, invoice gateway timeout, and invalid manual lesson-reflow entries, with no new transfer/report/reminder error.
- **Deployment:** Release `c729cf4119cf6d276978c42c0aed6a493e875888` was deployed through `/root/lms/deploy-production.sh`; rollback target is `97217acc18101b44511f80f38852a87ba8924c4f`. Migration `20260910090000` was applied through the authenticated Supabase Management API and recorded in the migration ledger before the app switch. The one confirmed unsent invalid old-class report and its five child descriptions were backed up, then removed with exact report/class/coder/status guards; the valid historical published report and current-class data were unchanged. Backup: `/root/lms/shared/bugfix-backups/INTERNAL-20260910-01-prechange.json` with mode `600`.

## INTERNAL-20260912-01: Admin lesson-report detail and premature Coach attendance

- **Date / reference:** 2026-09-12 / `INTERNAL-20260912-01`.
- **Symptom:** Admin lesson-report cards only exposed a two-line summary and an incorrect lesson anchor, so the full report and lesson context could not be reviewed or edited from a dedicated page. Coach attendance and session-completion endpoints allowed a future session to be marked before its start time. The Coach lesson-report textarea could also inherit a light text color in the installed PWA and become unreadable on its white field.
- **Root cause:** Lesson reports had no Admin detail route or multi-field update contract; the card linked with the report ID instead of the lesson ID. Attendance authorization checked Coach ownership but did not enforce the session start boundary in either mutation endpoint or the UI. The report field relied on inherited theme colors instead of setting an explicit foreground and WebKit text fill.
- **Changed:** Admin lesson-report board, detail page, editor and PATCH endpoint; Weekly/Ekskul lesson editor deep links; Coach lesson-report modal; Coach attendance page/list/wrapper and both attendance/session-status APIs; `src/lib/sessionTiming.ts`; focused regression tests; this log and index. No schema or environment setting changed.
- **Fix:** Every report card now opens a responsive Admin detail page with full report metadata, lesson summary, meeting count, make-up instructions, slide/example links, editable report type/description/status, and a direct lesson-editor action when the historical reference is unambiguous. A shared start-time predicate disables future attendance controls and rejects direct attendance or premature completion requests server-side. Coach report controls now use explicit high-contrast text, caret, placeholder, and WebKit fill colors in a viewport-bounded modal.
- **Invariants preserved:** Existing report IDs, Coach ownership, report status workflow, embedded Ekskul title marker, curriculum snapshots, lesson templates, class assignments, completed attendance history, reminders, and other classes remain unchanged. Cleanup is restricted to the confirmed future `Creator Jumat` session and its exact derived access record after a mode-600 backup.
- **Regression risks:** Legacy Ekskul reports identify lessons by title; when several Ekskul plans contain the same title, the detail page intentionally withholds the direct lesson-editor link instead of guessing. Client-side disabled controls are convenience only; both mutation APIs remain authoritative.
- **Verification:** Focused timing, attendance UI, Admin report workflow, and PWA contrast tests passed 9/9; the full suite passed 322/322 tests across 74 files. TypeScript, `git diff --check`, and the production build passed with non-secret placeholders and generated 111 pages. `/login` returned `200`; signed-out report list/detail and Coach attendance routes redirected `307`; signed-out attendance and status APIs returned `401`. The deployed SHA matched its release directory and PM2 `lms` plus `clevio-wa` remained online. The available browser profile was signed out, so authenticated Admin/Coach visual confirmation remains pending; responsive structure, viewport-bounded form behavior, and interaction contracts are covered by source-level tests.
- **Deployment:** Application release `9b67637d4244895175688eab270dbdcf89640c3e` was deployed through `/root/lms/deploy-production.sh`; rollback target is `fcdc8cc508cc16a8c83d266fb4120b20006e2617`. Before cleanup, the exact future session, attendance, and derived access rows were stored at `/root/lms/shared/bugfix-backups/INTERNAL-20260912-01-prechange.json` with mode `600`. Cleanup deleted one premature attendance and one `SESSION_COMPLETED` access row, restored the 18 September `Creator Jumat` session to `SCHEDULED`, and verified zero remaining attendance/access rows for that session. A documentation-only release follows so deployed source and Git production remain identical.
## INTERNAL-20260912-02 Individual Coder level progression

- Date: 2026-09-12.
- Symptom: a Coder who had completed every block in the personal level journey could remain active in the same rolling Weekly class and repeat completed blocks. Admin had no explicit placement queue, while the Coder had no level-up state. The lesson issue screen was also labelled ambiguously as `Laporan Lesson`.
- Root cause: class curriculum intentionally cycles for mid-cycle admissions, but individual completion only directly changed an enrollment to `INACTIVE`. It had no idempotent progression record, placement state, complete enrollment boundary, or dedicated LMS notifications. Existing journeys could also absorb later global curriculum blocks.
- Production evidence: the read-only audit found one deterministic candidate with all five personal blocks completed while the enrollment remained active. Identifying data remains outside the committed log. Before correction, the exact user, enrollment, personal progress, and empty progression state were backed up in a mode-700 directory whose files use mode 600.
- Changed areas: `coder_level_progressions`, the `finalize_coder_level` and `place_coder_level_progression` service-role RPCs, personal progress reconciliation, Admin `/admin/level-progressions`, Coder dashboard status, Admin/Coder LMS notifications, and lesson-problem labels. Migration: `20260912150000_coder_level_progressions.sql`.
- Implementation: each Coder keeps a personal block snapshot ordered from the entry block. Completion counts unique blocks in that snapshot, including valid migration-completed blocks without requiring an LMS evaluation. The atomic finalizer records either `WAITING_PLACEMENT` or `PROGRAM_COMPLETED`, closes active enrollments in the source level with `ended_at` and `exit_reason = COMPLETED`, and never changes class schedules or another Coder's progress. Admin placement validates the exact next level and an active Weekly destination, creates/reactivates one enrollment, initializes the next journey at the destination class's current block, and records `PLACED`.
- Invariants: class cycles remain available for staggered admissions; completed personal blocks never reset; level order never wraps to the first level; accounts and historical reports remain available; old-class reminders stop at the enrollment boundary; level-up and placement alerts go only to LMS Admin and LMS Coder with dedupe keys, with no Coach or WhatsApp recipient; old report URLs and data remain unchanged.
- Regression risks: the additive database objects remain compatible with the previous application release, but a code rollback does not remove the table or functions. Historical candidates must be reconciled only after their personal progress is verified; placement remains an explicit Admin action.
- Verification: 75 Vitest files / 336 tests passed, including 14 new progression contracts; `npx tsc --noEmit`, `git diff --check`, and the production build passed. The migration ran successfully in the production Supabase project and its table returned `200` through the service-role REST path. Release `c350695ee8250980f0359426dcf103796e57f23c` matched `/root/lms/current`; PM2 `lms` was online with zero restarts. `/login` returned `200`, signed-out `/admin/level-progressions` redirected `307`, and the signed-out placement API returned `401`. The available browser profile was signed out of LMS, so authenticated visual confirmation remains pending; responsive and interaction contracts are covered by the focused tests.
- Production correction: the verified historical candidate was reconciled once after backup. The progression is `WAITING_PLACEMENT`, the source enrollment is closed with `exit_reason = COMPLETED`, and no active or target enrollment was created. Five deduplicated LMS notifications were stored and pushed to the Coder plus four active Admin accounts; zero Coach notifications were created and no WhatsApp level-up message was sent. Rollback application target: `1d44aa965eb65aa7ce4188c927615a70f383409f`.

## INTERNAL-20260916-01: Portfolio save recovery and clearer learning journey

- **Date / reference:** 2026-09-16 / `INTERNAL-20260916-01`.
- **Symptom:** Save/upload failures could leave controls loading or create a second draft on retry; spelling variants inflated skill counts; two journey sections repeated statistics, names were truncated, and primary project actions appeared only below long content.
- **Root cause:** The form did not retain confirmed save/upload progress or handle rejected network requests. Skill aggregation keyed on raw strings. Journey content combined generic summaries instead of dated published works; oversized headings and floating modal controls reduced usable space.
- **Changed:** `PortfolioForm.tsx`, `PortfolioTagInput.tsx`, `PublicPortfolioExperience.tsx`, `PublicPortfolioGallery.tsx`, `savePortfolioDraft.ts`, `portfolioTags.ts`, `publicPortfolioExperience.ts`, and portfolio behavioral/responsive tests.
- **Fix:** Retain saved IDs and uploaded images across recoverable retries; release loading in finally; require inspecting the saved draft after ambiguous create/upload/review results instead of blindly retrying. Warn before refresh/link navigation with unsaved input. Group fields into three anchored steps and offer deduplicated tag suggestions while allowing custom input. Normalize spelling per project for display statistics without rewriting stored snapshots. Show works before a single publication-date timeline, preserve historical program attribution, omit unknown historical levels, replace skill percentage display with project counts, and show actual how-to-play instructions. Make names wrap, reduce display text, improve small-screen navigation, surface playable links near the title, preserve full screenshots in their frames, and trap/restore modal keyboard focus.
- **Invariants:** Approved-only published snapshots, stable share slugs, approval/version boundaries, optional playable URLs, ownership validation, immutable class origin, deletion safeguards, existing animations/keyframes/reduced-motion and document scroll-lock behavior remain intact. No API/schema migration, credential change, real notification, or production data mutation.
- **Regression risks:** Ambiguous requests intentionally require reopening the saved workspace; unsaved-change protection is a warning, not autosave. Historical levels are not present in published snapshots and are not fabricated. Publication dates are explicitly labelled and are not represented as class attendance dates.
- **Verification:** 76 Vitest files / 348 tests passed; TypeScript and diff check passed. Behavioral coverage includes save/upload/review operation order, same-ID retry, no duplicate successful upload, validation stop, network/server ambiguity, skill normalization and chronology. Browser QA used a local fixture/API-mocked harness at 320x568, 390x844, 664x600 and 1280x720: entry CTA reachable, name wraps, uniform cards, project action visible, scrollable modal, keyboard trap/Escape restore, successful draft navigation and offline recovery state. No relevant console errors in exercised flows. Node 22 build uses dummy build-only environment without an env file.
- **Deployment:** Implementation release `48976c3903807e305fecc29e1419914e370c58eb` was fast-forwarded from `96a5d2b0dc5521edf8d05032720b4386bea3b827` and deployed through the locked atomic script. Current symlink and deployed-SHA marker matched. PM2 LMS and WhatsApp were online with zero restarts. Login and the public portfolio returned `200`, signed-out Coder portfolio returned `307`, and its API returned `401`. The LMS error log had not changed since before deployment. Live mobile QA at 390x844 confirmed the above-content playable action, modal focus, no horizontal overflow, all five project screenshots loaded, and no console errors. Initial rollback target: `96a5d2b0dc5521edf8d05032720b4386bea3b827`. A documentation-only follow-up records these outcomes; it does not alter runtime behavior.
- **Remaining manual verification:** Authenticated real-device upload/review is not exercised against production to avoid operational data or notification mutations.

## INTERNAL-20260917-01: Tablet portfolio entry action and render work

- **Date / reference:** 2026-09-17 / `INTERNAL-20260917-01`, user confirmed the affected action is the opening “Lihat Portofolio” button.
- **Symptom / evidence:** On production at 1280x720 the opening action was clipped by its middle grid row; at 1366x720 its bottom reached approximately 792px. The intro locked document scrolling, leaving the clipped action unreachable.
- **Root cause:** The action shared an overflow-hidden, height-constrained row with a responsive long heading. Font-only short-height patches did not cover landscape widths above the desktop breakpoint. The hidden main experience also mounted images and a second animated backdrop before entry; pointer events wrote four inherited style properties on every event.
- **Changed:** `PublicPortfolioExperience.tsx`, `PublicPortfolioExperience.module.css`, `PublicPortfolioGallery.tsx`, `latestFrame.ts`, and its behavioral test. No route/API/schema/environment changes.
- **Fix:** Reserve an independent bottom action row with safe-area padding; allow only the middle copy area to scroll; use bounded heading scale and wrap long names. Contain orbit overflow so rotating bounds do not introduce an unnecessary scrollbar. Keep the navy/green/cyan design, real logo, portal, and existing title; the action footer identifies the real coder instead of a decorative slogan. Mount the main experience on entry, preserving its fade/scale entrance, load offscreen screenshots lazily with asynchronous decoding, and coalesce pointer writes to the latest input once per animation frame. Restore keyboard focus to the main project action after the intro exits.
- **Invariants:** All 12 original animation keyframe definitions remain byte-identical; 18 visible stars, portal motion, reduced-motion support, intro/document locks, approved snapshots, stable share URLs, and gallery behavior remain. No production data changes or migrations.
- **Regression risks:** Tablet browser chrome, text scaling and long names require reserved control space rather than font-only fixes. Gallery images load when near the viewport. Mounting on entry intentionally avoids hidden work, not visible animation. Physical iPad/Safari hardware performance remains a manual check; no universal FPS claim is made.
- **Verification:** 77 files / 350 tests pass, including latest-input frame coalescing and cancellation before a queued frame can overwrite reset/unmounted state; TypeScript and diff check pass. Browser QA used the actual components with local fixtures at 1366x720, 1280x720, 1024x768, 1024x600, 820x1180, 768x1024, 320x480, 390x844, and 640x320: action bounds inside viewport, center hit-test on the button, no horizontal overflow, only 18 stars and no hidden project DOM before entry. Long-name 320x480 copy scrolls independently; clicking enters, unlocks document, mounts projects and focuses “Lihat Karya”; no console warnings/errors in that flow. Build and exact-SHA production deployment are verified in the task handoff after this source commit.
- **Deployment:** Pending at source-commit time; use the locked atomic deployment script after latest-production rebase and verification. Actual deployed/previous SHA and smoke results are recorded by the deployment markers and final task report. No database rollback is involved.

## INTERNAL-20260917-02: Coder migration progress and Admin users table

- **Date / reference:** 2026-09-17 / `INTERNAL-20260917-02`.
- **Symptom:** Admin could mark historical Coder blocks as completed for migration from the pre-LMS program, but the Coder's personal progress and material access did not reliably change. The Users table also required horizontal scrolling on mobile and made row actions difficult to scan.
- **Root cause:** The Users-page override still inserted into the legacy `coder_block_completions` table, while current journey, level progression, and material authorization use `coder_block_progress`. Its UI also loaded a global block list without binding the selection to the Coder's active Weekly enrollment and level. The Users table retained its desktop column layout at phone widths. The action dropdown is intentionally rendered through a body-level portal to avoid clipping inside the table, but the first visual-menu selectors were scoped under the Admin shell, so portal content never received the intended panel style.
- **Changed:** `src/app/api/admin/coders/[id]/progress/route.ts`, `src/app/(admin)/admin/users/ProgressOverrideButton.tsx`, `src/app/(admin)/admin/users/UsersTable.tsx`, user-action controls, `src/components/admin/ActionDropdown.tsx`, `src/lib/services/coder.ts`, `src/app/globals.css`, focused regression contracts, and this log/index. No schema, migration, or environment setting changed.
- **Fix:** The Admin-only migration endpoint now resolves the Coder's active Weekly enrollment, validates every selected block against that exact level, initializes the immutable personal journey when needed, and only promotes newly selected rows to `COMPLETED`. Existing completed rows cannot be downgraded. The next unfinished block is normalized and the established level-completion reconciler still runs. Completed migrated blocks immediately authorize their lessons; they remain visible as historical materials after the source enrollment closes, while inactive enrollment schedule position cannot expose unfinished or future lessons. The modal supports multi-block migration per active class and explains its one-way behavior. On mobile, the existing table rows become labelled, responsive user records with the three-dot action kept at the top-right; desktop retains the compact table. The portal now receives an explicit Admin-user-menu class and the panel CSS is global only for that class, so it actually applies after portal rendering. The menu is constrained to the viewport on mobile and presents large icon-and-label rows: red Nonaktifkan, muted-purple Migrasi progress, and divider-separated red Hapus.
- **Invariants preserved:** The operation is scoped to one Coder, one active Weekly class, and the level attached to that class. It does not change class schedules, global lesson templates, another Coder, historical evaluations, enrollment history, or production data during verification. Existing personal journey snapshots remain authoritative, level-up reconciliation remains intact, completed progress cannot be undone here, Ekskul access rules are unchanged, and inactive classes expose only completed-block lessons or explicit stored-session grants.
- **Regression risks:** A Coder with no active Weekly enrollment must be placed before migration can be applied. Database updates are service-side and sequential; errors are returned and logged instead of silently claiming success. Authenticated Admin and Coder visual confirmation on a physical phone remains useful after deployment, although responsive and authorization boundaries have focused contract coverage.
- **Verification:** Focused migration/progression tests passed 20/20 after adding the action-menu layout contract. The prior full suite passed 355/355 across 78 files after installing the exact lockfile dependencies. `npx tsc --noEmit`, `git diff --check`, and the production build passed; the build generated 111 pages. No migration or production data mutation was run. Authenticated visual verification of the Admin menu remains device/session-dependent; the reported screenshot issue is covered by the fixed-width/fixed-height source contract and will be checked again after deployment.
- **Deployment:** Release `d9842f27ef768512e9aa38599fe59dd54eb7c4ec` was fast-forwarded to `production` and deployed through `/root/lms/deploy-production.sh`; initial rollback target is `ac657899a19aac6daefe0db07f21dd6d7f0aa9cf`. `/root/lms/current` and the release Git HEAD matched the exact SHA, PM2 `lms` was online with zero restarts, `/login` returned `200`, signed-out `/admin/users` redirected `307` to login, and the signed-out migration API returned `401`. Recent retained logs contained only the pre-existing malformed Server Reference ID entries and no `CoderMigrationProgress` error. No migration or production data mutation was performed. A documentation-only follow-up keeps this deployment record in the deployed source.

## INTERNAL-20260918-02: Compact Admin user action menu

- **Date / reference:** 2026-09-18 / `INTERNAL-20260918-02`.
- **Symptom:** The repaired three-dot action panel was readable but visually oversized for routine user-table actions.
- **Root cause:** Its first post-portal design retained a large reference-panel width, icon size, padding, and row height after the clipping problem had already been solved.
- **Changed:** `src/app/globals.css`, the action-menu responsive contract, and this log/index. No API, schema, or data changed.
- **Fix:** The portal menu is constrained to 292px (or viewport width minus gutters), with compact 56px normal rows, a 60px destructive row, 38px icon boxes, and preserved viewport clamping, label visibility, and destructive color treatment.
- **Invariants preserved:** The portal stays outside table overflow, Escape/outside-click dismissal and short-viewport scrolling remain, all action handlers are unchanged, and no user/account data is mutated.
- **Verification:** Focused menu contracts passed 8/8, `npx tsc --noEmit` and `git diff --check` passed, and the production build completed successfully with 111 generated routes. Authenticated visual confirmation remains part of post-deployment device QA.

## INTERNAL-20260918-03: Ekskul midterm report cycle

- **Date / reference:** 2026-09-18 / `INTERNAL-20260918-03`.
- **Symptom:** Some schools require a midterm report for an Ekskul class while the existing automatic end-of-semester report must remain unchanged. Admin had no controlled way to open a separate review cycle.
- **Root cause:** The report model only represented a block-bound report, so a class-wide midterm report would otherwise overwrite an automatic report or require a fictitious global curriculum block.
- **Changed:** Additive migration `supabase/migrations/20260918090000_ekskul_midterm_report_cycles.sql`; report DAO and draft service; Admin class action/API; existing Coach review, Admin inbox/public report/Coder report labels and guards; focused contract; and this log/index.
- **Fix:** Admin can create exactly one `MIDTERM` cycle for an Ekskul class. It only aggregates completed scheduled lessons at or before the trigger time and participants enrolled at that period, then creates separate `DRAFT` reports with `block_id = null`, cycle ID, and `EKSKUL_MIDTERM` type. Coach and Admin use the established review, submit, publish, and WhatsApp workflow. Midterm reports bypass only the unrelated block-reflection prerequisite and never delete source lesson evaluations. Automatic reports remain untouched and are labelled `Rapor Akhir Semester`; the new cycle is `Rapor Tengah Semester`.
- **Invariants preserved:** No global lesson template, class schedule, enrollment history, completed evaluation, or existing report is rewritten. The cycle is rejected for Weekly classes, future cutoff times, a class without completed lessons, or a second midterm cycle. Regular reports still require a real block and reflection validation.
- **Regression risks:** Generation is limited to Coders with saved lesson evaluation data in the period. AI-description failure falls back to manual-review text. Schema is additive; app rollback leaves new columns unused.
- **Verification:** Focused contracts cover schema isolation, Admin/Ekskul authorization, completed-session cutoff, report type, Coach reflection bypass, and source-evaluation protection (11/11 passed). `npx tsc --noEmit`, `git diff --check`, and production build passed with 111 routes. Migration was run successfully in Supabase SQL Editor with RLS enabled; deployment verification is recorded after exact-SHA release.
- **Deployment:** App release `fd2cfb52207622776fdd7d94ed7ce5553db8a7f8` was fast-forwarded to `production` and deployed through `/root/lms/deploy-production.sh` after the additive schema migration completed. `/root/lms/current` matched that exact SHA, PM2 `lms` was online, `/login` returned `200`, signed-out Admin redirected `307`, the new Admin midterm API returned `401` signed out, and the service-role schema probe for `ekskul_report_cycles` returned `200`. Rollback target is `6ad7350a3ec080fd7b2aa49b635ffadfb4546889`. Recent retained logs showed only pre-existing short-link fallback and destination-stream entries, with no midterm-report exception.

## INTERNAL-20260918-04: Persistent LMS WhatsApp authentication

- **Date / reference:** 2026-09-18 / `INTERNAL-20260918-04`.
- **Symptom:** The WhatsApp connection configured in LMS could request a QR scan again after deployments or repeated transient connection failures.
- **Root cause:** The Baileys client used a release-relative `baileys_auth_info` path. Atomic releases start LMS from a new directory, so any auth files recreated there were not available to the next release. In addition, repeated QR emission deleted credentials even when no confirmed WhatsApp logout occurred.
- **Changed:** `src/lib/services/whatsappClient.ts`, `scripts/deploy-production.sh`, a focused persistence contract, and this log/index. The shared auth store stays outside releases at the deployment runtime path.
- **Fix:** The deployment script creates and passes `WHATSAPP_AUTH_DIR`, defaulting to `$LMS_DEPLOY_ROOT/shared/baileys_auth_info`. Baileys uses that explicit directory. QR retries and all automatic authentication-close statuses preserve credentials and notify Admin; only the explicit Admin reset operation can clear them.
- **Invariants preserved:** Release build directories remain disposable; deployment never copies, deletes, or resets the shared WhatsApp session. No automatic reconnect, QR retry, or authentication-close event may delete it. The explicit Admin reset retains its intended fresh-pairing behavior. No user messages or payment data are mutated.
- **Regression risks:** A currently invalid WhatsApp pairing can still require one QR scan, but once paired it persists across future LMS deployments. The folder is deployment-owned with mode `700`; it must never be committed or bundled into a release.
- **Verification:** Focused persistence contract passed 4/4; `npx tsc --noEmit` and `git diff --check` passed; production build completed with 111 routes. Turbopack reports its dynamic-filesystem tracing warning only on the explicit manual credential-reset branch; the release model already contains the full worktree and runtime behavior is unchanged. VPS symlink/session inspection and release checks are recorded with deployment verification.
- **Deployment:** Release `87617de2d28d8c0e5af4fc5a8fd0d0964159efcf` correctly used `/root/lms/shared/baileys_auth_info`, preserving the existing credential file during that release. A subsequent restart exposed the remaining automatic `401` path, which removed the invalidated credential file; this follow-up closes that path so no automatic event can erase the shared session. The release path resolves to the shared directory. Rollback target before the follow-up is `304b95eed59335c8b730c8e165298020cf5699ce`.
- **Final deployment:** Release `bc082021311725e062570128233256ec2f2cb194` was deployed through `/root/lms/deploy-production.sh`. `/root/lms/current` matched that SHA, PM2 `lms` was `online`, `/login` returned `200`, and signed-out `/admin/whatsapp` redirected `307`. The process received `WHATSAPP_AUTH_DIR=/root/lms/shared/baileys_auth_info`; the directory remained present and no new automatic credential-delete log was emitted by this release. Because the prior automatic `401` behavior had already removed the old credential file, an Admin must scan one new QR code to restore the connection. Rollback target is `304b95eed59335c8b730c8e165298020cf5699ce`.

## INTERNAL-20260918-06: Public portfolio v4 reference parity

- **Date / reference:** 2026-09-18 / `INTERNAL-20260918-06`.
- **Symptom:** The public portfolio bound real LMS data, but retained the prior dark Universe presentation rather than the supplied v4 reference. Its page hierarchy, light palette, project cards, and modal treatment did not match the requested design.
- **Root cause:** The earlier change treated the reference as an animation direction layered onto the existing layout instead of treating the reference layout as the visual source of truth.
- **Changed:** `PublicPortfolioExperience.tsx`, `PublicPortfolioGallery.tsx`, the portfolio stylesheet, and the responsive contract. No API, schema, access policy, or production data changed.
- **Fix:** Rebuilt the public page with the v4 hierarchy: light sticky navigation, creator hero/universe stage, statistics row, featured latest approved project, tool-filtered project grid, level/block roadmap, skills/reflection cards, and an orbit closing panel. The official Clevio logo remains in the header and the closing card uses the existing Lucide rocket. Project cards and modals bind only approved snapshot fields; block cards bind personal `coder_block_progress`; counts are derived from actual projects, tools, and completed blocks. The document scroll mode is explicitly notified on entry/exit, and the portfolio root normalizes the root/body axes to retain one document scrollbar.
- **Invariants preserved:** The public route still selects only non-null `published_snapshot` records; no draft, revision, internal report, or unrelated Coder data is exposed. Featured remains the newest approved project. Modal Escape/outside-close, document locking, external-link safety, and responsive single-column layouts remain available. No placeholder personal traits or project content is presented as real data.
- **Regression risks:** Viewport-specific visual confirmation needs a deployed public route because the local checkout lacks runtime authentication/Supabase environment values. Projects without screenshots intentionally use the neutral project icon treatment rather than invented imagery.
- **Verification:** Focused portfolio behavior and v4 responsive contracts passed 10/10; `npx tsc --noEmit` and `git diff --check` passed. A local production build compiled and typechecked, then stopped during route data collection because this checkout has no `NEXTAUTH_SECRET`; that is an environment prerequisite unrelated to this page. Deployment, public-route smoke, and representative desktop/mobile browser verification are recorded after release.
- **Deployment:** Release `c2b3297836dade376f012ca39bcf26baa78f69de` was fast-forwarded to `production` and deployed through the locked atomic script. `/root/lms/current` and its release Git HEAD matched that SHA; PM2 `lms` was online with zero restarts. `/login` and the public portfolio returned `200`. Browser accessibility inspection on the live public URL confirmed the v4 hierarchy and actual Khansa data: five approved projects, two tools, personal block progress, approved-project latest achievement, reflection, and next step. Rollback target is `731476a9dfb1a93539011dfb7a2976a5557fd40a`. LMS WhatsApp reported `not connected`, so no completion notification was sent through an unrelated WhatsApp service.

## INTERNAL-20260918-07: Public portfolio v4 modal and footer parity

- **Date / reference:** 2026-09-18 / `INTERNAL-20260918-07`.
- **Symptom:** The rebuilt public portfolio still used a simplified project-detail popup and a combined closing section, while the supplied v4 reference has a two-column project story modal plus a separate closing banner and identity footer. The first corrected desktop modal also stretched its visual vertically and cropped typical landscape project screenshots.
- **Root cause:** The first v4 pass matched the main page hierarchy but retained the previous modal information architecture and stopped before reproducing the reference footer composition.
- **Changed:** `src/components/portfolio/PublicPortfolioExperience.tsx`, `src/components/portfolio/PublicPortfolioGallery.tsx`, `src/components/portfolio/PublicPortfolioExperience.module.css`, `src/components/portfolio/PublicPortfolioModalLandscape.module.css`, the focused responsive contract, and this log/index.
- **Fix:** The project popup now follows the reference's visual/content hierarchy: real project visual and project-story label on the left; title, approved summary, What I Made, What I Learned, Skills Practiced, reflection/next step, and permitted project links on the right. Desktop uses a bounded media pane with a full-frame 16:10 landscape canvas and real project metadata beside independently scrollable story details, so long entries such as Guru Les Datang do not stretch the image column. Tablet stacks a 16:9 canvas above the details; mobile keeps the compact single-column modal. The closing banner now uses the bright reference treatment with the existing Lucide rocket and is followed by a separate Clevio identity footer.
- **Invariants preserved:** Every value continues to come from the approved `published_snapshot`; no placeholder project story, invented skill, draft field, database record, API, or access policy is introduced. Featured-project ordering, modal keyboard/outside dismissal, document scroll locking, and safe external links remain unchanged.
- **Regression risks:** Long approved project text must remain independently scrollable inside the modal at short viewport heights. Projects without screenshots retain the neutral icon fallback. Future footer changes must keep the identity row separate from the closing CTA banner.
- **Verification:** Focused portfolio behavior and responsive contracts passed 11/11; `npx tsc --noEmit` and `git diff --check` passed. The local production build compiled and typechecked before stopping at route data collection because this checkout intentionally has no `NEXTAUTH_SECRET`. Production browser QA specifically opened the long-form Guru Les Datang project: at 1440x900 its 1920x1080 screenshot remained fully visible in the bounded media pane while the 1,271px story scrolled independently; at 820x1000 the image stacked above the scrollable details; at 390x844 the modal kept one column, a visible close control, locked document body, and no horizontal overflow. The shorter Help My Mom project, separate closing/footer composition, and approved real-data mapping also remained intact. No browser console warnings/errors were emitted.
- **Deployment:** Release `d59de7475e9630111ad0b0046bc6202c6f64a77f` was fast-forwarded to `production` and deployed through `/root/lms/deploy-production.sh`; rollback target is `624ff7e9be4d3aca643cc2eb0eccef4c2dd7e8da`. `/root/lms/current` matched the exact release, PM2 `lms` was online with zero restarts, and `/login` plus the real public portfolio route returned `200`. Retained PM2 output includes pre-existing stream-close, Node URL deprecation, and class-lesson constraint messages; browser QA emitted no new portfolio error. LMS WhatsApp remained disconnected, so no completion report was sent through another WhatsApp service.

## INTERNAL-20260919-01: Completed report block hid pending Coach scores

- **Date / reference:** 2026-09-19 / `INTERNAL-20260919-01`.
- **Symptom:** A recently completed Weekly block had attendance and Coder reflection completed, but its lesson-score task disappeared from Coach > Rubrik & Penilaian when the next block became current. Without the Coach scores, no report draft could be generated. The global Coach issue-report trigger also floated above attendance actions.
- **Root cause:** `getPendingLessonEvaluationsForCoach` only admitted lessons whose class block status was `CURRENT`. The reporting block became `COMPLETED` immediately after its evaluation/reflection session even though its 21-day report window was still open. Report generation was then only retried by session completion or the nightly reconciliation cron; saving the missing Coach scores or Coder reflection did not trigger an immediate eligibility recheck.
- **Changed:** `src/lib/services/reportWindows.ts`, `src/lib/services/coach.ts`, Coach and Coder evaluation APIs, Coach sidebar/header/layout, the shared issue-report trigger, focused report-window and reconciliation tests, and this log/index. No schema or migration changed.
- **Fix:** A completed class block remains eligible for Coach lesson scoring only while its regular report window is active; current blocks remain eligible as before, while stale completed and upcoming blocks stay hidden. Saving Coach lesson scores or a Coder block reflection now schedules the existing idempotent report generator with Next.js `after()`, so either prerequisite can be the last event without waiting for the nightly cron. The Coach issue reporter is no longer floating: it sits at the bottom of the desktop sidebar and remains available from the mobile profile menu. Its modal renders through a body portal so the mobile menu transform cannot clip it.
- **Invariants preserved:** Report generation still requires a passed final block session, Coach lesson evaluations, report-period enrollment coverage, and the Coder block reflection. Existing manual, submitted, published, or sent reports are never replaced; the nightly cron remains a fallback. Ekskul midterm isolation, class schedules, block status, lesson order, attendance, scores, reflections, and production data are unchanged. Stale historical blocks do not reappear.
- **Regression risks:** AI draft generation runs after the response lifecycle and may still fail independently; failures are logged and the nightly cron retries them. A Coach must provide real lesson scores; the system does not invent or backfill scores. Mobile access to issue reporting moves into the profile menu rather than occupying a sixth bottom-navigation slot.
- **Verification:** A read-only production audit confirmed the hidden items had completed Coder reflections and active report windows but no saved Coach lesson evaluations; generator logs showed `No evaluations exist yet`. The helper tests cover current, recently completed, and stale completed blocks. Focused report tests passed 25/25; the full suite passed 371/371 across 81 files; `npx tsc --noEmit` and `git diff --check` passed. The production build completed with 111 routes using non-secret build placeholders; only the pre-existing WhatsApp dynamic-filesystem tracing warning remained.
- **Deployment:** Release `4c83b0c2e245450c5d8b803bac03f4b7f3245b88` was fast-forwarded to `production` and deployed through `/root/lms/deploy-production.sh`; rollback target is `22c2ee51df98a5786c791f736254f50f8976d415`. `/root/lms/current` matched the exact release, PM2 `lms` was online with zero restarts, `/login` returned `200`, signed-out `/coach/rubrics` redirected with `307`, and signed-out `/api/coach/evaluations` returned `401`. Recent runtime logs contained no reconciliation or fatal application error. The shared LMS WhatsApp authentication directory remained present and the deployed release still linked to it. Authenticated Coach visual verification remains pending because the available browser session redirected to LMS login; no production values, reflections, reports, or database rows were mutated for QA.

## INTERNAL-20260922-01: PRESENT Coder enrolled after session was missing from Coach scoring

- **Date / reference:** 2026-09-22 / `INTERNAL-20260922-01`.
- **Symptom:** Alzano Neamata Aluwira appeared as `PRESENT` for the 21 September Solution Ideation session in Technopreneur Lower Al Azhar Syifa Budi Cibubur, but Coach could not see the Coder in the lesson-scoring form.
- **Root cause:** The active enrollment was created on 22 September, one day after the completed session. Attendance correctly retained the backfilled `PRESENT` evidence, while the Rubrik queue, session-data API, direct scoring page, and score-submission validation admitted only enrollments whose `enrolled_at` was at or before the session.
- **Changed:** Shared enrollment-evaluation eligibility, Coach pending-evaluation service, Coach scoring page and APIs, focused eligibility/contract tests, and this log/index. No schema, migration, attendance, enrollment, score, report, or lesson data changed.
- **Fix:** An active Coder is eligible for a completed lesson when the enrollment covered the session as before, or when that exact session has authoritative `PRESENT` attendance. A late enrollment with `ABSENT` attendance remains excluded, so migration/backfill absences do not reopen historical assessments. The same helper now protects every read and write path for Coach lesson scoring.
- **Invariants preserved:** Inactive/transferred Coders stay excluded; ordinary enrollment date boundaries remain authoritative; attendance is still required before scoring; `ABSENT` history never becomes a scoring task; Coach/class ownership checks, lesson mapping, schedules, and existing evaluations are unchanged.
- **Regression risks:** Legitimate historical `PRESENT` records for currently active Coders can now surface unfinished scoring that was previously hidden. This is intentional and limited to sessions the Coder was explicitly marked present for.
- **Verification:** A read-only production audit confirmed enrollment `2026-09-22T03:22:18Z`, Solution Ideation session `2026-09-21T06:00:00Z`, `PRESENT` attendance, and zero saved lesson evaluations for the affected Coder. The audit also found other historical late-enrollment `PRESENT` records that follow the same rule. Focused eligibility and Coach rubric contracts passed 8/8; TypeScript and `git diff --check` passed. Full-suite, build, deployment, runtime smoke, and authenticated roster verification are recorded after release.
- **Deployment:** Release `3cf583edb83b7bd69ef75b99696d1546fd050f6d` was fast-forwarded to `production` and deployed through `/root/lms/deploy-production.sh`; rollback target was `103416045e87973e4c44613a04ab58fac6875906`. `/root/lms/current` matched the exact release, PM2 `lms` was online with zero restarts, `/login` returned `200`, signed-out `/coach/rubrics` redirected `307`, and the signed-out evaluation API returned `401`. The shared LMS WhatsApp authentication directory remained present. Authenticated Coach visual verification was unavailable because the browser session redirected to login.

## INTERNAL-20260922-02: Ekskul midterm report timeout and story content

- **Date / reference:** 2026-09-22 / `INTERNAL-20260922-02`.
- **Symptom:** Admin received `504 Gateway Timeout` while generating an Ekskul midterm cycle. The generated story showed `0 materi`, displayed a Weekly-only reflection step, and long competency labels overlapped the radar chart.
- **Root cause:** The Admin request synchronously waited for serial AI description generation for every Coder, exceeding the reverse-proxy timeout even though the server continued and completed all reports. Public report lessons were derived only from a block-bound lesson list, but an Ekskul midterm deliberately has no `block_id`; some Ekskul classes also have `level_id = NULL` while their `ekskul_lesson_plan_id` is valid, and the page incorrectly required the level ID before reading that plan. The shared seven-scene story always included reflection and rendered radar labels as one unbounded SVG line.
- **Changed:** Ekskul midterm Admin API/button, report generator and DAO resume lookup, public report data preparation, interactive report story, focused contract, and this log/index. No schema, migration, class schedule, evaluation, attendance, or enrollment data changed.
- **Fix:** The endpoint validates synchronously, returns `202`, and runs generation after the response. Generation reuses the immutable cycle cutoff, resumes existing reports, skips complete or already-reviewed reports, and tolerates a concurrent retry without duplicating a cycle/report. Ekskul stories derive unique lesson titles from actual `COMPLETED` scheduled sessions through the report-cycle cutoff, omit the reflection scene and full-report reflection section, label the count as lessons, and wrap long radar labels across SVG lines.
- **Invariants preserved:** Exactly one midterm cycle remains allowed per class; existing submitted/published reports and descriptions are not overwritten; automatic final reports, Weekly block reports/reflections, source lesson evaluations, schedules, and production data remain unchanged. A retry only completes a partial draft cycle.
- **Regression risks:** Background AI generation can finish after the Admin page refresh; its failures are logged and a later request safely resumes missing draft descriptions. A report only lists sessions that are both `COMPLETED` and mapped to the class lesson schedule at or before its fixed cutoff.
- **Verification:** The existing production cycle for Technopreneur Kinderfield Lower was audited read-only: all 14 expected reports and five descriptions per report existed despite the 504, and generation timestamps spanned about 72 seconds. Focused contract, full suite, TypeScript, diff check, production build, deployed route smoke, and responsive browser checks are recorded after release.
- **Deployment:** Release `5fd7ff4ab9a50a5cd99944952669998f7adcf6b6` was fast-forwarded to `production` and deployed through `/root/lms/deploy-production.sh`; rollback target is `7ae06f02a29ef62619e11e1cf1f27985cdffa06d`. `/root/lms/current` matched the exact SHA, PM2 `lms` was online with zero restarts, `/login` returned `200`, signed-out report access remained fail-closed through the branded not-found view, and the midterm API returned `401` signed out. The shared LMS WhatsApp authentication directory remained present and recent PM2 logs contained no new report-generation failure.

## INTERNAL-20260922-03: Backfilled ABSENT Coder was missing from Coach scoring

- **Date / reference:** 2026-09-22 / `INTERNAL-20260922-03`.
- **Symptom:** Technopreneur Lower Al Azhar Syifa Budi Cibubur had 19 active Coders, but historical lesson scoring exposed only 18 to the Coach.
- **Root cause:** The nineteenth active enrollment was created after the historical sessions. The school had explicitly backfilled an attendance row for that Coder, but the shared evaluation-eligibility helper accepted late enrollment evidence only when its status was `PRESENT`. This differed from ordinary session rosters, where an enrolled Coder remains part of evaluation regardless of whether attendance is `PRESENT` or `ABSENT`.
- **Changed:** Shared session evaluation eligibility, Coach scoring comments, focused eligibility tests, and this log/index. No schema, migration, enrollment, attendance, evaluation, report, or class data changed.
- **Fix:** Any explicit attendance row now proves that an active late-joining Coder belongs to the historical roster. Active late joiners without an attendance row remain excluded, and inactive enrollments remain excluded even when old attendance exists.
- **Invariants preserved:** The fix does not invent attendance, revive inactive/transferred Coders, change lesson dates, or write scores. Coach queue, direct page, session-data API, and submission validation continue to use the same shared eligibility helper.
- **Regression risks:** Intentional attendance backfills marked `ABSENT` can now expose unfinished historical scoring. This is expected because those Coders were explicitly added to those session rosters; sessions with no backfill remain closed to late joiners.
- **Verification:** A read-only production audit found 19 active enrollments, with the late Coder carrying explicit attendance rows on the older sessions; the previous helper calculated 18 eligible Coders because those rows were `ABSENT`. The latest session had `PRESENT` attendance and all five criteria saved. Focused eligibility and Coach rubric contracts passed 9/9. The full suite passed 377/377 across 81 files; TypeScript, `git diff --check`, and the production build with non-secret placeholders passed with 111 routes.
- **Deployment:** Release `c43e106d775e7501e7e066fd5f657fd20c1f0c70` was fast-forwarded to `production` and deployed through `/root/lms/deploy-production.sh`; rollback target is `5fd7ff4ab9a50a5cd99944952669998f7adcf6b6`. `/root/lms/current` matched the exact SHA, PM2 `lms` was online with zero restarts, `/login` returned `200`, signed-out Coach Rubrics redirected `307`, and the evaluation API returned `401` signed out. Deployed source contains the attendance-evidence eligibility rule and no production records were mutated.

## INTERNAL-20260922-04: Report story content was clipped on short viewports

- **Date / reference:** 2026-09-22 / `INTERNAL-20260922-04`.
- **Symptom:** Parts of interactive Weekly/Ekskul report slides could be cut off on laptops and small phones, especially long lesson grids, Coach notes, and the final slide.
- **Root cause:** The report shell correctly fixed itself to the viewport and hid outer overflow, but several inner slide variants overrode their scroll container with `overflow: visible`. Content taller than the stage therefore escaped into the clipped shell and could not be reached.
- **Changed:** Shared Weekly/trial report story layout CSS, responsive contract, and this log/index. Report content, report data, navigation order, colors, and database state are unchanged.
- **Fix:** The actual stage height between header and bottom navigation is now the single boundary for every slide. All slide variants use vertical inner scrolling, hide horizontal spill, preserve touch momentum and scroll padding, and retain extra bottom padding for buttons and Coach cards.
- **Invariants preserved:** Header, progress indicator, previous/next navigation, full-report overlay, responsive typography, print mode, and story animations remain unchanged. No document-level second scrollbar is introduced.
- **Regression risks:** Very long report content intentionally scrolls inside the story while navigation stays fixed. Future slide variants must not restore visible overflow inside the fixed story shell.
- **Verification:** Focused responsive contracts passed 2/2. Browser QA used an 18-lesson stress fixture at 1366x768, 1024x600, 390x844, and 320x568. Every viewport kept the story within screen width, preserved fixed navigation, exposed vertical scrolling where needed, and reached lesson 18 without document horizontal overflow. The full suite passed 377/377 across 81 files; TypeScript, `git diff --check`, and the production build with non-secret placeholders passed with 111 routes.
- **Deployment:** The responsive fix shipped in release `c43e106d775e7501e7e066fd5f657fd20c1f0c70` with rollback target `5fd7ff4ab9a50a5cd99944952669998f7adcf6b6`. The exact release was active, PM2 `lms` was online with zero restarts, protected routes remained fail-closed while signed out, and recent filtered logs contained no report or evaluation failure. The shared LMS WhatsApp authentication directory remained present.
## INTERNAL-20260922-05: Saved Coach scores appeared missing when reopened

- **Date / reference:** 2026-09-22 / `INTERNAL-20260922-05`.
- **Symptom:** Previously submitted lesson scores appeared blank in both the direct Coach evaluation page and the Rubrik & Penilaian dashboard modal. Reopening a completed lesson therefore looked as though historical scores had disappeared and could invite an accidental overwrite.
- **Root cause:** Both Coach clients always initialized every Coder and criterion cell with an empty string. The direct server page and the modal session-data endpoint fetched roster and criteria data but never loaded existing `lesson_evaluations`.
- **Changed:** `src/lib/services/evaluationScores.ts`, both Coach evaluation clients, the direct evaluation page, the modal session-data endpoint, focused unit and contract tests, and this log/index. No schema, migration, production score, attendance, enrollment, report, lesson, or WhatsApp authentication data changed.
- **Fix:** Existing session evaluations are fetched through `reportsDao.getLessonEvaluationsBySession`, filtered into the current roster and criteria matrix, and supplied as the initial form state. Missing cells remain empty, so newly added Coders can be scored without hiding or replacing saved values for existing Coders.
- **Invariants:** Coach ownership, completed-session, attendance-completeness, active-enrollment, score-range, and full-roster submission validation remain unchanged. The existing upsert endpoint still writes only after an explicit Coach submission.
- **Production data audit:** A read-only audit of all lesson evaluations for the affected Al Azhar class found every historical score row still present. All rows had identical `created_at` and `updated_at`, proving that the saved values had not been overwritten after creation; no recovery write was necessary.
- **Regression risks:** A future evaluation UI or endpoint must continue returning and consuming the saved-score matrix. Rows outside the current roster or retired criteria are intentionally excluded from the editable matrix but remain untouched in the database.
- **Verification:** Focused score-map and dual-entry-point contract tests passed, followed by the full suite with 381/381 tests across 83 files. `npx tsc --noEmit`, `git diff --check`, and the production build with non-secret placeholders passed. The production read-only timestamp audit covered every affected Al Azhar lesson-evaluation row and found zero rows changed after creation.
- **Deployment:** Functional release `68b89c3d4f901ea1a54aefd93790c38ff741bf54` was fast-forwarded to `production` and deployed through `/root/lms/deploy-production.sh`; rollback target is `db1801289af073e288a26776cf507423de0bf656`. The active release matched the exact commit, PM2 `lms` was online with zero restarts, `/login` returned `200`, signed-out `/coach/rubrics` redirected with `307`, and signed-out `/api/coach/evaluations` returned `401`. Recent LMS error logs contained no matching runtime failure. The LMS WhatsApp auth symlink still targeted `/root/lms/shared/baileys_auth_info`, and `clevio-wa` remained online without restart.
## INTERNAL-20260923-01: A4 report materials were clipped and Ekskul copy used block terminology

- **Date / reference:** 2026-09-23 / `INTERNAL-20260923-01`.
- **Symptom:** Browser-generated A4 reports could end the materials section mid-content, the printed document had no Clevio identity header, and visible Ekskul report copy used the Weekly term `block`.
- **Root cause:** The report story overlay applies an inline document scroll lock while open, including `position: fixed` on `body`. The story itself is hidden for print, but its body lock remained active and forced Chromium to clip the complete report to one A4 page. The materials panel also used an indivisible print boundary, the only logo lived in the hidden screen toolbar, and main report copy embedded `block` instead of deriving the period term from class type.
- **Changed:** `src/app/report/[id]/page.tsx`, focused A4 report contract coverage, and this log/index. No report, score, lesson, enrollment, invoice, notification, or WhatsApp data changed.
- **Fix:** Printing is explicitly A4 portrait and resets the story scroll lock only inside print media. The Clevio logo now sits inside the report hero instead of adding a separate header. Competencies stay together on their own page, materials start on a fresh page and flow by indivisible lesson rows, and Weekly reflections start with their content on a fresh page. Visible period copy resolves to `semester` for Ekskul and remains `block` for Weekly.
- **Invariants:** The interactive report story, report data selection, score calculations, Ekskul reflection exclusion, Weekly terminology, browser layout, and `window.print()` download flow remain unchanged.
- **Regression risks:** Very long lesson titles may increase row height but each row must remain intact. Future print styles must not restore `break-inside: avoid` on the complete materials panel or allow a modal scroll lock to override print pagination.
- **Verification:** Focused print contracts passed 4/4, the full suite passed 385/385 across 84 files, TypeScript and `git diff --check` passed. Browser QA generated a four-page portrait A4 PDF from a real published 13-lesson report. Visual inspection confirmed the logo is inside the hero, all five competency cards share one page, all 13 lesson rows are present without clipping, and the Weekly reflection heading stays with its cards.
- **Deployment:** Functional release `398e3abe073b9fd52ba6e2433483e00a056cf051` was deployed through `/root/lms/deploy-production.sh`; rollback target is `ad1c17466844b4135b3b8f037a9143a339559ec1`. PM2 `lms` returned online after deployment. No report, score, lesson, enrollment, invoice, notification, or WhatsApp data was written by this fix.

## INTERNAL-20260923-02: Report A4 competencies were split into columns

- **Date / reference:** 2026-09-23 / `INTERNAL-20260923-02`.
- **Symptom:** The five competency ratings appeared as two side-by-side columns when printed, making the first report page feel crowded and harder to scan.
- **Root cause:** The shared responsive grid retained its desktop two-column layout in print.
- **Changed:** `src/app/report/[id]/page.tsx`, focused A4 print contract coverage, and this log/index. No report or evaluation data changed.
- **Fix:** Print-only styling stacks the competency cards as five compact full-width rows with icons, scores, progress bars, and observations. Screen layout remains unchanged. The learning journey still begins on page 1 and flows intact across pages.
- **Invariants:** A4 portrait sizing, report data, score calculations, Clevio branding in the hero, indivisible competency/material cards, and Weekly/Ekskul period terminology remain unchanged.
- **Regression risks:** Print CSS grid fragmentation can stretch or split full-width rows; the print competency list uses block flow and each row stays together.
- **Verification:** Focused print contracts passed 4/4, the full suite passed 385/385 across 84 files, TypeScript and `git diff --check` passed. `npm run build` passed with a local-only build secret. A rendered A4 preview showed all five competency rows and five material entries on page 1 without overlap; PDF remains 3 pages.
- **Deployment:** Application release `c130a0f8a50cdebeec18310abd99efefb9283347` was integrated into `production` and deployed through `/root/lms/deploy-production.sh`; rollback target is `bbf136de733e0a57d75820d085b6c61ff156b7df`. PM2 `lms` and `clevio-wa` were online with zero restarts, the shared LMS WhatsApp auth directory remained present, and the login/report smoke checks returned HTTP 200. No user or WhatsApp session data was changed.

## INTERNAL-20260923-03: Report hero and learning journey visual polish

- **Date / reference:** 2026-09-23 / `INTERNAL-20260923-03`.
- **Symptom:** The report repeated the Clevio logo in both the top bar and hero on screen, lesson items looked like nested dashboard cards, and the printed hero label did not stay aligned with the logo.
- **Root cause:** The hero brand was shared between screen and print, while lesson titles were presented as a grid of individually framed cards.
- **Changed:** `src/app/report/[id]/page.tsx`, focused A4 print contract coverage, and this log/index. Report data is unchanged.
- **Fix:** The hero logo is hidden on screen and shown inside the hero only for print. The learning journey is now a numbered vertical list with a subtle connecting rail and row dividers instead of nested cards. Print styles keep the brand group anchored to the left and preserve intact lesson rows.
- **Invariants:** The top-bar logo remains, the printed report retains a Clevio logo inside its hero, the lesson order and titles are unchanged, and the page remains responsive.
- **Regression risks:** Long lesson names must wrap without colliding with the timeline number or divider; print pagination must keep each list row together.
- **Verification:** Focused print contracts passed 4/4, the full suite passed 385/385 across 84 files, TypeScript, `git diff --check`, and the production build passed. Live desktop QA confirmed the hero logo is hidden on screen and visible in print, the print label is left-aligned, all 13 lesson rows render, and the A4 PDF remains three pages without clipped rows.
- **Deployment:** Release `2bbb46a3a17652e4779cd2d987362e07bf584612` was integrated into `production` and deployed through `/root/lms/deploy-production.sh`; rollback target is `86ce135ef1951c7164766442d2355c817789dad1`. PM2 `lms` and `clevio-wa` were online with zero restarts, and login/report smoke checks returned HTTP 200. No report data or WhatsApp auth state changed.

## INTERNAL-20260923-04: Report print layout did not follow web

- **Date / reference:** 2026-09-23 / `INTERNAL-20260923-04`.
- **Symptom:** Printing used a more compressed hierarchy than the web report and forced reflections onto a mostly empty page.
- **Root cause:** Print overrides reduced hero, competency, and timeline content, then explicitly started reflections on a new page.
- **Changed:** `src/app/report/[id]/page.tsx`, A4 print contract coverage, and this log/index. Screen layout and report data are unchanged.
- **Fix:** Print now retains the web report's hero emphasis, two-column competency cards, and vertical learning timeline at readable A4 sizing. Reflection content flows after the learning journey instead of forcing an unnecessary page break. Cards and lesson rows remain indivisible across page boundaries; action controls stay hidden in print.
- **Invariants:** Output remains A4, all five competency areas and all lessons remain present, screen rendering is unchanged, and no report data is modified.
- **Verification:** Focused print contracts passed 4/4; full suite passed 385/385 across 84 files; TypeScript, `git diff --check`, and production build passed. Rendered live report QA confirmed a two-page A4 PDF with the hero, all competencies, the beginning of the journey on page 1, and the remaining lessons and reflections on page 2 without clipped content.
- **Deployment:** Release `b150a2bdc1cf0c66524d663edb49652bdd09cac3` was integrated into `production` and deployed through `/root/lms/deploy-production.sh`; rollback target is `2bbb46a3a17652e4779cd2d987362e07bf584612`. PM2 `lms` and `clevio-wa` remained online with zero restarts; login/report smoke checks returned HTTP 200. No database, report, or WhatsApp session data was changed.

## INTERNAL-20260923-05: Learning journey cards restored

- **Date / reference:** 2026-09-23 / `INTERNAL-20260923-05`.
- **Symptom:** The learning journey timeline did not match the previous cleaner card design requested by the user.
- **Root cause:** The numbered vertical list had replaced the earlier panel of lesson cards.
- **Changed:** `src/app/report/[id]/page.tsx`, A4 print contract coverage, and this log/index. Hero and competency layouts are unchanged.
- **Fix:** Restored the numbered lesson cards inside the learning-journey panel, with a two-column layout on A4 and responsive columns on screen. Cards and panel can flow across pages without splitting an individual lesson card.
- **Invariants:** Lesson names/order, report data, semester terminology, and screen/mobile responsive behavior remain unchanged; print remains A4 and action controls stay hidden.
- **Verification:** Focused report print contracts passed 4/4, the full suite passed 385/385 across 84 files, TypeScript and `git diff --check` passed, and the production build passed. The A4 contract verifies two-column print cards, intact card breaks, and natural continuation of reflections.
- **Deployment:** Release `fcecab5d7ec50faffdffa1fb6590533033df5304` was integrated into `production` and deployed through `/root/lms/deploy-production.sh`; rollback target is `b150a2bdc1cf0c66524d663edb49652bdd09cac3`. PM2 `lms` and `clevio-wa` were online with zero restarts, login/report smoke checks returned HTTP 200, and `/root/lms/shared/baileys_auth_info` remained present. No database, report, or WhatsApp session data was changed.

## INTERNAL-20260923-06: Report print layout diverged from web

- **Date / reference:** 2026-09-23 / `INTERNAL-20260923-06`.
- **Symptom:** The print layout still differed from the report as seen at the user's 1920x1080 desktop screen size.
- **Root cause:** Portrait A4 reduced the print media width below the web `lg` breakpoint, switching the journey from five desktop columns to two.
- **Changed:** `src/app/report/[id]/page.tsx`, A4 print contract coverage, and this log/index. No report data changes.
- **Fix:** Print uses A4 landscape and a 1180px desktop content canvas scaled proportionally to the printable width, so desktop responsive breakpoints, columns, typography, and spacing match the 1920x1080 web layout. Kept only print behavior: overflow reset, top navigation and share-story footer hidden, Clevio logo shown in the hero, and individual cards protected from splitting.
- **Invariants:** Report layout follows the desktop web composition on paper; only the share-story footer is hidden. A4 paper size is retained in landscape orientation.
- **Verification:** Focused print contract passed 4/4, full suite passed 385/385 across 84 files, TypeScript, `git diff --check`, and production build passed. Existing public QA report ID returned the app's 404 page, so a data-backed visual print capture could not be completed.
- **Deployment:** Release `1f759b564ea4615eadc0ba9244fed652fa49602f` was integrated into `production` and deployed through `/root/lms/deploy-production.sh`; rollback target is `dfa7c8a3d8fff35b15d479990c199132513b05d9`. PM2 `lms` and `clevio-wa` were online with zero restarts, `/login` returned HTTP 200, and the shared LMS WhatsApp auth directory remained present. No database or report data was changed.

## INTERNAL-20260923-07: Fifth competency lost full width in print

- **Date / reference:** 2026-09-23 / `INTERNAL-20260923-07`.
- **Symptom:** The fifth competency card did not consistently render as a full-width row when printed, unlike the 1920x1080 web layout.
- **Root cause:** Full-width behavior relied only on a responsive utility class, which was not being preserved reliably in print pagination.
- **Changed:** `src/app/report/[id]/page.tsx`, A4 print contract coverage, and this log/index. Report data and lesson layout are unchanged.
- **Fix:** Added an explicit print two-column competency grid and a semantic full-span marker for an odd final competency. The final card is forced across both columns while each card remains intact; the learning section continues after the grid.
- **Invariants:** Four competency cards remain in two rows and the fifth spans the full row, matching web; A4 landscape desktop print canvas remains; Story share footer stays hidden.
- **Verification:** Full suite passed 385/385, focused print contract passed, TypeScript, `git diff --check`, and production build passed. The live QA report ID was unavailable (404), so visual confirmation against its report data remains unavailable.
- **Deployment:** Release `e7d3d52ccfb274664302684cbe47239838f36f5c` was integrated into `production` and deployed through `/root/lms/deploy-production.sh`; rollback target is `1f759b564ea4615eadc0ba9244fed652fa49602f`. PM2 `lms` and `clevio-wa` were online with zero restarts, `/login` returned HTTP 200, and the shared LMS WhatsApp auth directory remained present. No database or report data was changed.
